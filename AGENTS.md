# Koza TV Proje Çalışma Kuralları

Bu dosya Koza TV projesinin ortak çalışma hafızasıdır. İnsan veya yapay zekâ fark etmeksizin projede çalışacak herkes işe başlamadan önce bu dosyanın tamamını okumalıdır.

## ALTIN KURAL — TESTSİZ HİÇBİR İŞ TAMAMLANMIŞ SAYILMAZ

> **Yapılan her değişikliğin test senaryosu yazılacak, test çalıştırılacak ve sonucu çalışma günlüğüne kaydedilecektir. Testsiz kod commit edilemez, push edilemez, yayına alınamaz. “Küçük değişiklik” istisnası yoktur.**

- Her hata düzeltmesi, hatayı önce yakalayan ve düzeltmeden sonra geçen bir regresyon testi içermelidir.
- Her yeni özellik; başarılı kullanım, başarısız kullanım, yetkisiz kullanım ve sınır durum senaryolarına sahip olmalıdır.
- Görsel değişiklikler masaüstü, tablet ve mobil görünümde kontrol edilmelidir.
- Formlar ve etkileşimler klavye, odak, boş değer, hatalı değer, yükleniyor, başarı ve hata durumlarıyla test edilmelidir.
- Test başarısızsa iş bitmiş kabul edilmez. Başarısız test atlanmaz, susturulmaz veya gerekçesiz silinmez.
- Test kapsamı değişiklikle aynı commit içinde yer almalıdır.
- Çalışma günlüğünde çalıştırılan komut, geçen test sayısı ve bilinen eksikler açıkça yazılmalıdır.

## 1. Projenin tek kaynağı

- Çalışma dizini: Her geliştiricinin kendi bilgisayarındaki Git repository köküdür; bu dokümana kişiye veya makineye özel mutlak klasör yolu yazılmaz.
- GitHub: `https://github.com/umtco12/kozawebsite`
- Ana branch: `main`
- Tasarım referansı: `https://kozatv-haber.kozatv.workers.dev/`
- Canlı marka alan adı: `https://www.kozatv.com.tr/`
- Ortak çalışma yalnızca GitHub repository içeriği üzerinden yürütülür.
- Kişisel yedek, arşiv veya bilgisayara özel dizin bilgileri bu ortak dokümana yazılmaz.

## 2. Her çalışmada zorunlu sıra

1. Bu `AGENTS.md` dosyasını tamamen oku.
2. `git status --short --branch` ile branch ve değişiklikleri kontrol et.
3. Kullanıcının mevcut değişikliklerini silme, üzerine yazma veya habersizce commit kapsamına alma.
4. İstenen işi küçük, anlaşılır ve geri alınabilir değişikliklerle gerçekleştir.
5. Değişiklik için test senaryolarını yaz veya mevcut testleri genişlet.
6. `npm test` çalıştır; tek bir başarısız test varsa işi tamamlanmış sayma.
7. En az `npm run build` çalıştır ve sonucu doğrula.
8. Değiştirilen sayfalar için gerekli rota/API ve tarayıcı kontrollerini yap.
9. Bu dosyanın **Çalışma Günlüğü** bölümüne yapılan işi, değişen ana dosyaları ve doğrulama sonucunu ekle.
10. Yalnızca ilgili dosyaları seçerek stage et. `git add .` ve `git add -A` kullanma.
11. Commit veya push işlemini yalnızca kullanıcı açıkça istediğinde yap.

## 3. Dosya ve klasör düzeni

- Aktif uygulama kodu yalnızca bu GitHub projesinde tutulur.
- `report/`, `backup/`, müşteri teklifleri, PDF'ler ve eski HTML tasarımları aktif repoya eklenmez.
- Haber sitesi kodu `app/`, statik medya `public/`, veri erişimi `db/` ve Hetzner yayın dosyaları `deployment/hetzner/` altında tutulur.
- Şifre, API anahtarı, erişim anahtarı veya kişisel veri Git'e yazılmaz.
- Derleme çıktıları ve yerel bağımlılıklar commit edilmez.

## 4. Tasarım yaklaşımı

- Görsel yön, referans sitedeki kurumsal haber merkezi yaklaşımıdır.
- Koza TV ulusal bir haber markası olarak ele alınır; içerik ve tasarım tek bir şehirle sınırlandırılmaz.
- Öncelikler: güçlü manşet, son dakika görünürlüğü, canlı yayın, video, yazarlar, reklam alanları ve mobil okunabilirlik.
- Tasarım masaüstü, tablet ve mobilde çalışmalıdır.
- Klavye odağı, kontrast, azaltılmış hareket tercihi ve anlamlı alternatif metinler korunmalıdır.

## 5. SEO ve yayın kuralları

- Canonical alan adı `https://www.kozatv.com.tr` olarak kabul edilir.
- Sayfa başlığı, açıklama, Open Graph ve X/Twitter alanları gerçek içeriği yansıtmalıdır.
- Haber detaylarında benzersiz metadata, haber görseli ve yapılandırılmış veri kullanılmalıdır.
- Eski haber URL'leri taşıma sırasında korunmalı veya birebir 301 yönlendirmesi yapılmalıdır.
- Yapay zekâ tarafından hazırlanan hiçbir haber editör onayı olmadan yayınlanmamalıdır.

## 6. Admin paneli ve içerik güvenliği

- Admin paneli anonim kullanıcılara açık bırakılmaz.
- Roller en az yönetici, yayın yönetmeni, editör, muhabir ve sadece görüntüleme olarak ayrılır.
- Yapay zekâ haberleri önce taslak kuyruğuna gelir; kaynak, telif, görsel ve doğrulama kontrollerinden sonra editör yayınlar veya zamanlar.
- Her yayınlama, güncelleme ve silme işlemi denetim kaydına yazılmalıdır.
- Harici haber kaynağı ekleme ve otomatik içerik çekme yalnızca yetkili kullanıcılar tarafından yapılabilir.

## 7. Git çalışma şekli

- `main` her zaman çalışır durumda tutulur.
- Geliştirme için kısa ve açıklayıcı feature branch kullanılır.
- Commit mesajı yapılan işi açıkça anlatır.
- Bir commit yalnızca ilgili işin dosyalarını içermelidir.
- Push öncesinde diff, build sonucu ve çalışma günlüğü tekrar kontrol edilir.
- Başka kişilerin değişiklikleri izinsiz geri alınmaz veya yeniden yazılmaz.

## 7.1 Hetzner üretim altyapısı

- Üretim hedefi Debian 13 üzerinde Node.js 22 standalone servisidir.
- Uygulama `kozatv` sistem kullanıcısı ile yalnızca `127.0.0.1:8201` adresinde çalışır; dış trafik Caddy üzerinden gelir.
- Kalıcı SQLite verisi kod sürümlerinden ayrı `/srv/kozatv/data/` dizininde tutulur.
- Admin kimlik doğrulaması tamamlanıncaya kadar `/admin` ve içerik yazma istekleri Caddy katmanında dış erişime kapalı kalır.
- Sunucu, hesap, SSH, veritabanı veya üçüncü taraf servis parolaları hiçbir dosyaya ya da Git geçmişine yazılmaz.
- DNS geçişi tamamlanmadan eski alan adı yayınının kapandığı varsayılmaz; IP ve alan adı ayrı ayrı test edilir.

## 8. Koza TV zorunlu test matrisi

Her işte yalnızca değişiklikten etkilenen senaryolar değil, kritik yayın akışı regresyonları da düşünülmelidir.

### Genel ziyaretçi sitesi

- Ana sayfa, kategori, haber detayı, arama, yazarlar, video ve canlı yayın rotaları.
- Header, menü, son dakika bandı, manşet slider, haber kartları ve footer bağlantıları.
- Boş içerik, eksik görsel, uzun başlık, Türkçe karakter ve yoğun son dakika akışı.
- Masaüstü, tablet ve mobil kırılımları; yatay taşma ve okunabilirlik.

### Haber ve medya

- Haber başlığı, özet, gövde, yazar, yayın/güncelleme tarihi, kategori ve etiketler.
- Görsel/video yükleme, alternatif metin, kırık medya, farklı en-boy oranları ve telif alanları.
- Video oynatma, canlı yayın erişimi, yayın kesintisi ve yedek yayın mesajı.

### Admin ve editör akışı

- Giriş, çıkış, oturum süresi, yetkisiz erişim ve rol bazlı izinler.
- Taslak oluşturma, düzenleme, önizleme, editör onayı, zamanlama, yayınlama ve geri çekme.
- Eş zamanlı düzenleme, kaydetme hatası, bağlantı kesintisi ve denetim kaydı.
- Yönetici, yayın yönetmeni, editör, muhabir ve görüntüleyici rollerinin ayrı testleri.

### Yapay zekâ haber masası

- Kaynak URL ekleme, kaynağı çekme, tekrar haber tespiti ve kaynak erişim hatası.
- AI özet/başlık üretimi, yanlış veya eksik sonuç, kaynak gösterimi ve editör reddi.
- AI içeriğinin editör onayı olmadan yayınlanamadığının testi.
- Görsel/video aktarımı, telif uyarısı, zamanlama ve işlem günlüğü.

### SEO, erişilebilirlik ve performans

- Benzersiz title/description, canonical, Open Graph, X/Twitter ve haber yapılandırılmış verisi.
- Sitemap, RSS, robots, eski URL yönlendirmeleri ve 404 davranışı.
- Klavye kullanımı, görünür odak, kontrast, alternatif metin ve azaltılmış hareket.
- LCP, CLS ve INP bütçeleri; görsel boyutları, önbellek ve yavaş bağlantı davranışı.

### Güvenlik ve veri

- Admin/API yetkilendirmesi, istek doğrulama, rate limit ve CSRF yaklaşımı.
- XSS, zararlı HTML, uygunsuz dosya yükleme ve hassas veri sızıntısı kontrolleri.
- Veritabanı yedeği, geri yükleme, içerik taşıma ve tekrar çalıştırılabilir migration testleri.

## 9. Tamamlanmış sayılma kriteri

Bir iş ancak aşağıdakiler tamamlandığında bitmiş kabul edilir:

- İstenen davranış uygulanmıştır.
- Değişikliğin otomatik test senaryoları yazılmış ve tamamı geçmiştir.
- Proje başarıyla derlenmiştir.
- İlgili rota veya API kontrol edilmiştir.
- Mobil ve erişilebilirlik etkisi değerlendirilmiştir.
- Bu dosyadaki çalışma günlüğü güncellenmiştir.
- Kullanıcıya ne yapıldığı ve varsa kalan kararlar açıkça bildirilmiştir.

## 10. Çalışma Günlüğü

### 2026-08-20 — GitHub proje kurulumu ve ilk iyileştirmeler

- `umtco12/kozawebsite` deposunun `main` branch'i ortak projenin tek kaynak noktası olarak belirlendi.
- Ana sayfadaki sabit tarih İstanbul saat dilimine göre dinamik hale getirildi.
- SEO metadata temeli düzenlendi; canonical taban alan adı, Open Graph ve X/Twitter paylaşım görseli tanımlandı.
- Haber kartlarına hafif etkileşim, klavye odak görünümü ve azaltılmış hareket desteği eklendi.
- Ana sayfa, `/admin` ve `/api/content` başarılı yanıt verdi.
- `npm run build` başarılı tamamlandı.

### 2026-08-20 — Altın test kuralı ve Koza TV regresyon paketi

- İstek: Yapılan her iş için test senaryosu yazılması, testin zorunlu hale getirilmesi ve uygulamanın yerelde ayrıntılı kontrol edilmesi.
- Yapılanlar: Bu dosyanın başına testsiz işin tamamlanamayacağını belirten **ALTIN KURAL** eklendi; Koza TV ziyaretçi sitesi, haber/medya, admin, AI haber masası, SEO, erişilebilirlik, performans, güvenlik ve veri başlıklarında zorunlu test matrisi tanımlandı.
- Değişen ana dosyalar: `AGENTS.md`, `tests/rendered-html.test.mjs`, `app/api/content/content-model.mjs`, `app/api/content/route.ts`, `app/admin/page.tsx`, `app/site-client.tsx`, `app/yazarlar/page.tsx`, `app/page.tsx`, `app/globals.css`.
- Otomatik doğrulama: `npm test` ile build ve 10 regresyon senaryosu çalıştı; **10 geçti, 0 başarısız, 0 atlandı**.
- Kod kalitesi: `npm run lint` tamamlandı; **0 hata**, mevcut `<img>` kullanımları için **9 performans uyarısı** bulunuyor.
- Tarayıcı doğrulaması: 1440 px masaüstünde yatay taşma yok; 390 px mobilde yatay taşma yok; mobil menü açıldı ve 9 bağlantı gösterdi; canlı yayın sayfasına geçiş ve geri dönüş çalıştı; admin panelinde 8 bölüm düğmesi, kayıt düğmesi ve yazar düzenleme alanları göründü; konsolda hata oluşmadı.
- API doğrulaması: Yerel `/api/content` isteği HTTP 200 döndürdü; 3 manşet ve 4 yazar kaydı doğrulandı.
- Kalan karar veya risk: Admin kimlik doğrulaması ve rol yetkilendirmesi henüz uygulanmadı; gerçek canlı yayın oynatıcısı henüz bağlı değil; bazı kategori/kurumsal bağlantılar gerçek sayfalara bağlanmadı; Next Image dönüşümüyle kapatılacak 9 görsel performans uyarısı var.

### 2026-08-20 — Ortak dokümanın makineden bağımsız hale getirilmesi

- İstek: İki geliştiricinin farklı bilgisayarlarında çalışacağı dikkate alınarak ortak dokümandan kişisel klasör ve yerel arşiv bilgilerinin kaldırılması.
- Yapılanlar: Makineye özel mutlak çalışma yolu ve yerel yedek/arşiv konumu kaldırıldı; çalışma dizini repository kökü olarak tanımlandı.
- Değişen ana dosyalar: `AGENTS.md`, `tests/rendered-html.test.mjs`.
- Doğrulama: Ortak dokümanda mutlak kullanıcı yolu ve makineye özel yedek klasör adı bulunmadığını kontrol eden regresyon testi eklendi.
- Kalan karar veya risk: Yok.

### 2026-08-20 — Canlı haber platformu yol haritası

- İstek: Koza TV'yi gerçek verisi, yönetim paneli, medya, yapay zekâ haber masası, SEO, güvenlik ve canlı yayın operasyonu olan uçtan uca bir projeye dönüştürmek; yapılacakları aşamalı bir ortak dokümana bağlamak.
- Yapılanlar: Mevcut prototip ile üretim hedefi ayrıştırıldı; hedef mimari, D1/R2 veri sahipliği, üretim veri modeli, ziyaretçi sayfaları, editoryal akış, AI kaynak toplama, medya, SEO, güvenlik, test, ortamlar ve beş fazlı teslim planı `YAPILACAKLAR.md` dosyasında tanımlandı.
- Değişen ana dosyalar: `YAPILACAKLAR.md`, `AGENTS.md`, `tests/rendered-html.test.mjs`.
- Doğrulama: `npm test` içinde üretim derlemesi ve **12 test geçti; 0 başarısız, 0 atlandı**. `npm run lint` **0 hata** ile tamamlandı; optimize edilecek ham `<img>` kullanımları için mevcut **9 performans uyarısı** devam ediyor. Yol haritasındaki kritik ürün alanlarının ve veri güvenliği ilkelerinin korunmasını kontrol eden regresyon testi eklendi.
- Kalan karar veya risk: Repoda doğrulanmış Jenkins/GitHub Actions hattı bulunmuyor. Kimlik sağlayıcısı, canlı yayın kaynağı, eski CMS erişimi, medya saklama politikası ve CI/CD sağlayıcısı ilgili fazlardan önce netleştirilmelidir.

### 2026-08-20 — Hetzner Debian taşıma temeli

- İstek: Koza TV'yi mevcut Hetzner Debian sunucusuna taşımak, kalıcı veri katmanını hazırlamak, yapılanları belgelemek ve sunucudaki eski Parsel Kontrol yayınını kaldırmak.
- Yapılanlar: Hetzner'deki Debian 13 sunucu kaynakları ve mevcut servisler incelendi. Node.js 22 kuruldu. Uygulama Cloudflare D1/Worker bağımlılığından çıkarılarak standalone Node.js ve WAL modlu SQLite'a geçirildi. Ayrı `kozatv` kullanıcısı, sürüm dizini, kalıcı veri dizini ve sıkılaştırılmış systemd servisi kuruldu. Caddy IP trafiğini Koza TV'ye yönlendirdi; admin ve içerik yazma istekleri gerçek kimlik doğrulama tamamlanana kadar dışarıya kapatıldı. Parsel Kontrol web servisi ve üç zamanlayıcısı durdurulup devre dışı bırakıldı.
- Değişen ana dosyalar: `db/index.ts`, `app/api/content/route.ts`, `next.config.ts`, `vite.config.ts`, `package.json`, `package-lock.json`, `deployment/hetzner/*`, `README.md`, `YAPILACAKLAR.md`, `tests/rendered-html.test.mjs`, `.gitignore`, `AGENTS.md`.
- Doğrulama: Yerel ve Debian sunucuda standalone build başarılı oldu; **14 test geçti, 0 başarısız, 0 atlandı**. Lint **0 hata** ile tamamlandı ve mevcut 9 görsel performans uyarısı kaydedildi. Dış IP üzerinden `/`, `/canli`, `/yazarlar` ve `/api/content` HTTP 200; `/admin` HTTP 404 ve yetkisiz içerik yazma isteği HTTP 403 döndürdü. `kozatv` ve Caddy servisleri aktif doğrulandı.
- Kalan karar veya risk: `kozatv.com.tr` DNS kayıtları halen eski sunucuya yöneliyor ve DNS yönetimi Krihost nameserver'larında. HTTPS ancak DNS geçişinden sonra açılabilir. Hetzner Firewall ve otomatik backup kapalı. Parsel Kontrol servisleri durmuş olsa da dosya, sistem kullanıcısı, günlük ve unit dosyalarının kalıcı silinmesi ayrıca açık kapsam onayı bekliyor. Repodaki bağımlılık denetimi mevcut transitif paketlerde güvenlik bulguları bildiriyor; canlı admin açılmadan önce ele alınmalıdır.

### 2026-08-20 — Main branch'ten Hetzner staging CI/CD

- İstek: `main` branch'ine gelen her commit sonrasında testlerden geçen sürümün `http://46.225.169.52/` staging ortamına otomatik aktarılması.
- Yapılanlar: GitHub Actions üzerinde temiz kurulum, production bağımlılık denetimi, test/build, lint, sabit SSH host anahtarı doğrulaması, sınırlı yetkili `koza-deploy` hesabı, atomik sürüm bağlantısı, iç/dış sağlık kontrolleri ve hata halinde önceki sürüme dönüş akışı hazırlandı. Sunucuda deploy kullanıcısı, root sahipli deploy betiği ve yalnız bu betiğe izin veren dar sudo kuralı kuruldu.
- Değişen ana dosyalar: `.github/workflows/staging.yml`, `deployment/hetzner/deploy.sh`, `deployment/hetzner/known_hosts`, `deployment/hetzner/kozatv-deploy.sudoers`, `deployment/hetzner/README.md`, `tests/rendered-html.test.mjs`, `AGENTS.md`.
- Doğrulama: `npm test` ile standalone build ve **14 test geçti; 0 başarısız, 0 atlandı**. `npm run lint` **0 hata** ve mevcut **9 görsel performans uyarısı** ile tamamlandı. `npm audit --omit=dev` **0 production açığı** bildirdi. Workflow YAML ayrıştırıldı; deploy kullanıcısının SSH erişimi, incoming dizinine yazması, geçersiz SHA'yı reddetmesi ve genel sudo komutlarını çalıştıramaması doğrulandı. İlk Actions denemesinde üst dizin grup geçiş yetkisi eksikliği yakalandı; `koza-deploy` yalnız `kozatv` grubuna eklenerek düzeltildi. Sonraki workflow çalışmaları bütün adımlarıyla başarılı oldu; sunucudaki aktif sürümün güncel `main` commit SHA'sına eşit olduğu doğrulandı. Dışarıdan `/`, `/canli`, `/yazarlar` ve `/api/content` HTTP 200; `/admin` HTTP 404 ve yetkisiz içerik yazma isteği HTTP 403 döndürdü.
- Kalan karar veya risk: CI/CD staging hattı çalışır durumda. `main`e her push staging dağıtımını tetikler; bu nedenle `main`e yalnız test edilmiş değişiklik gönderilmelidir. HTTPS, Hetzner Firewall, otomatik yedek ve root SSH girişinin kapatılması ayrı güvenlik işleri olarak devam ediyor.

### 2026-08-20 — Veritabanı destekli tam haber deneyimi ve editör masası

- İstek: Koza TV için elle tutulur, güçlü bir haber sitesi tasarımı; içerik girişinin merkezde olduğu editör paneli, gerçek veritabanı ve kontrollü dış kaynak kurgusu hazırlanması.
- Yapılanlar: Ana sayfa premium editoryal düzende yeniden kuruldu; manşet, son dakika, günün akışı, öne çıkanlar, yazarlar, haber ve video alanları kalıcı SQLite haberlerinden beslenir hale getirildi. Haber/kategori sayfaları, NewsArticle metadata, sitemap, robots ve RSS eklendi. `articles`, `news_sources` ve `audit_logs` tabloları; taslak, inceleme, zamanlama ve yayın durumları; doğrulamalı haber/kaynak API'leri; dashboard, haber arşivi, canlı önizleme, SEO, medya/kaynak alanları ve editör onayı zorunlu AI kuyruğu içeren admin deneyimi hazırlandı. Dış yazma rotaları kimlik doğrulama tamamlanana kadar hem Caddy hem uygulama katmanında kapalı tutuldu.
- Değişen ana dosyalar: `app/page.tsx`, `app/site-client.tsx`, `app/globals.css`, `app/admin/*`, `app/api/articles/*`, `app/api/sources/*`, `app/haber/*`, `app/kategori/*`, `app/sitemap.ts`, `app/robots.ts`, `app/rss.xml/*`, `db/index.ts`, `db/article-model.mjs`, `db/seed.ts`, `deployment/hetzner/Caddyfile.*`, `tests/rendered-html.test.mjs`, `README.md`, `YAPILACAKLAR.md`, `AGENTS.md`.
- Doğrulama: `npm test` içinde production build ve **19 test geçti; 0 başarısız, 0 atlandı**. `npm run lint` **0 hata**, optimize edilecek ham görseller için **17 uyarı** ile tamamlandı. Tarayıcıda ana sayfa 1440, 768 ve 390 px genişliklerde yatay taşma olmadan doğrulandı; kırık görsel bulunmadı. Admin dashboard ve 15 alanlı haber editörü açıldı, konsolda hata görülmedi. İlk staging kontrolünde sunucunun eski Caddy yapılandırmasını kullandığı görülünce uygulama katmanı yazma koruması eklendi ve dış POST istekleri için 403 regresyonu tanımlandı.
- Kalan karar veya risk: Staging `/admin` girişi gerçek kimlik doğrulama/rol sistemi tamamlanana kadar kapalıdır. Medya dosya yükleme ve dönüştürme hattı, dış kaynakları zamanlanmış görevle çekme, AI üretimi, revizyon ekranı ve gerçek analitik verisi sonraki fazlardadır. `<img>` yüzeyleri nesne depolama/CDN kararıyla birlikte optimize edilmelidir.

### 2026-08-20 — Admin kategori merkezi ve kalıcı medya kütüphanesi

- İstek: Tüm haber kategorilerinin admin panelinden yönetilmesi; profesyonel haber sitesi admin düzeni; görsellerin sunucuda nasıl ve ne kadar yer tutacağının güvenli biçimde çözülmesi.
- Yapılanlar: `categories` tablosu, sıralama, görünürlük, vurgu rengi, açıklama ve SEO alanları eklendi. Kategori oluşturma/düzenleme API'si, admin kategori merkezi, veritabanından üretilen masaüstü/mobil menüler, kategori sitemap kayıtları ve gizli kategori davranışı tamamlandı. `media_assets` tablosu ve medya kütüphanesi kuruldu; JPG/PNG/WebP/GIF dosyaları içerik imzasıyla doğrulanıp yıl/ay klasörlerinde SHA-256 tabanlı adla kalıcı veri alanına yazılıyor. Alt metin, fotoğraf kredisi, dosya boyutu ve kullanım URL'si SQLite'ta tutuluyor. Dosya başına 12 MB, toplam 10 GB kota ve diskte en az 1 GB boş alan koruması eklendi. Büyük videolar için dosya yüklemek yerine harici video/HLS URL alanı korunuyor.
- Değişen ana dosyalar: `db/index.ts`, `db/seed.ts`, `db/media-storage.ts`, `app/api/categories/*`, `app/api/media/*`, `app/media/*`, `app/admin/panel.tsx`, `app/page.tsx`, `app/site-client.tsx`, `app/kategori/*`, `app/sitemap.ts`, `app/globals.css`, `deployment/hetzner/*`, `README.md`, `YAPILACAKLAR.md`, `tests/rendered-html.test.mjs`, `AGENTS.md`.
- Doğrulama: `npm test` içinde production build ve **21 test geçti; 0 başarısız, 0 atlandı**. Kategori ekleme/gizleme/sıralama, tekrarlı kategori, dış yazma koruması, geçerli/sahte görsel, medya yeniden sunumu, metadata ve kota sınırı test edildi. `npm run lint` **0 hata** ile tamamlandı. Tarayıcıda kategori merkezi ve medya kütüphanesi masaüstünde; kategori merkezi 390 px mobilde yatay taşma ve kırık görsel olmadan doğrulandı; mobil yönetim menüsünün kaydırma çubuğu gizlendi.
- Kalan karar veya risk: Admin kimlik doğrulaması tamamlanana kadar staging'de `/admin` ve yazma rotaları dışarıya kapalıdır. İlk aşamada görseller aynı Hetzner sunucusundaki kalıcı veri diskindedir; trafik ve medya hacmi büyüdüğünde aynı metadata modeli korunarak S3 uyumlu nesne depolama/CDN'e taşınmalıdır. Video dosyaları mevcut sunucu diskine yüklenmemelidir.

### 2026-08-20 — Güvenli admin girişi ve rol sistemi

- İstek: Yönetim panelini kullanıcı girişi ve rol sistemiyle güvenli açmak; proje sahibine yönetici rolü tanımlamak.
- Yapılanlar: Scrypt parola özeti, 12 saatlik hash'lenmiş oturum, `HttpOnly`/`SameSite` çerezi, beş hatalı denemede 15 dakikalık hesap kilidi ve ilk girişte zorunlu parola değişimi eklendi. Yönetici, yayın yönetmeni, editör, muhabir ve görüntüleyici rolleri tüm yönetim API'lerinde sunucu tarafında uygulanır hale getirildi. Yetkisiz `/admin` erişimi girişe yönlendirildi; giriş, parola yenileme ve yöneticiye özel kullanıcı/rol ekranları tasarlandı. İlk yönetici hesabını açık parolayı repoya yazmadan sunucuda oluşturacak araç hazırlandı; Caddy'nin eski toplu engelleri uygulama katmanı yetkilendirmesiyle değiştirildi.
- Değişen ana dosyalar: `db/index.ts`, `db/auth-model.mjs`, `app/api/auth/*`, `app/api/users/*`, `app/api/write-access.ts`, yönetim API rotaları, `app/admin/*`, `app/globals.css`, `scripts/create-admin.mjs`, `deployment/hetzner/Caddyfile.*`, `tests/rendered-html.test.mjs`, `README.md`, `YAPILACAKLAR.md`, `AGENTS.md`.
- Doğrulama: `npm test` içinde production build ve **24 test geçti; 0 başarısız, 0 atlandı**. Güvenli çerez, hatalı giriş, zorunlu parola değişimi, viewer yazma engeli, oturumsuz API erişimi ve admin yönlendirmesi test edildi. `npm run lint` **0 hata**, mevcut ham görseller için **21 performans uyarısı** ile tamamlandı. Giriş ekranı 1440, 768 ve 390 px genişliklerde yatay taşma olmadan; iki etiketli giriş alanı ve görünür klavye odağıyla doğrulandı, konsolda hata görülmedi.
- Kalan karar veya risk: İlk staging denemesinde eski sağlık kontrolünün artık korunan `/api/content` rotasında 200 beklediği görüldü; otomatik geri dönüş kesintiyi engelledi ve sağlık/smoke kontrolleri giriş sayfası, admin yönlendirmesi ve 401 oturum sözleşmesine geçirildi. MFA, IP tabanlı rate limit, hesap pasife alma/rol değiştirme, parola sıfırlama e-postası ve şüpheli giriş alarmı sonraki güvenlik adımlarıdır. Staging IP adresi HTTP çalıştığı için `Secure` çerezi alan adı HTTPS geçişinde etkinleşecektir.

### 2026-08-22 — Ziyaretçi sitesinin tıklanabilir hale getirilmesi ve eksik sayfalar

- İstek: Staging sitesinde menüdeki ve alt bölümdeki bağlantıların çalışmaması; "Son Dakika", "Ekonomi" gibi öğelere tıklanınca hiçbir şey olmaması; kategori oluşturma akışının netleştirilmesi; siteyi kullanılabilir hale getirip ayrıntılı test edilmesi ve yönetim panelinin gözden geçirilmesi.
- Yapılanlar: Ortak `SiteHeader`/`SiteFooter` bileşeni eklendi; ana sayfa, kategori, haber detayı, canlı yayın, yazarlar ve yeni sayfalar aynı çalışan menüyü kullanıyor. Boş çapa bağlantıları (`#gundem`, `#sondakika`, `#video`) ve tıklanamayan `span`/`button` öğeleri gerçek sayfalara bağlandı. Yeni rotalar: `/son-dakika`, `/arama`, `/videolar`, `/yazar/[slug]`, `/kurumsal/[slug]` (hakkımızda, künye, yayın ilkeleri, iletişim, KVKK, gizlilik, çerez) ve özel 404 ekranı. Menüye tarayıcının kendi GET gönderimiyle çalışan arama alanı eklendi; JavaScript yüklenmese de çalışır. Yazarlar sayfası sabit listeden veritabanı imzalarına geçirildi; her imza için haber arşivi ve `Person` yapılandırılmış verisi üretiliyor. Haber detayına gerçek paylaşım bağlantıları, bağlantı kopyalama, yazar bağlantısı, etiketler, video bloğu ve `BreadcrumbList` eklendi. Canlı yayın sayfası yayın akışı, kanal bilgileri ve gerçek oynatıcıya hazır bileşenle yenilendi; yayın kaynağı tanımsızken sahte yayın gösterilmiyor. Bulunamayan haber/kategori/yazar/kurumsal adresler artık 404 döndürüyor ve indekslenmiyor. Sitemap yeni rotaları, robots ise arama sayfasını kapsıyor. Yönetim panelinde kullanıcı rolü değiştirme ve pasife alma tamamlandı; son aktif yöneticinin düşürülmesi ve kişinin kendi hesabını kapatması engellendi, işlemler denetim kaydına yazılıyor. Kullanılmayan eski `.subpage`/`.live-page` stilleri kaldırıldı (canlı yayın sayfasındaki hizalama bozukluğunun kaynağıydı).
- Kök neden (tıklanamama): Bağlantıların `href` değerleri doğruydu; sorun `next/link` bileşeninin tıklamayı yakalayıp vinext beta istemci yönlendiricisinin gezinmeyi tamamlayamamasıydı (tarayıcı konsolunda `RSC prefetch setup error: f is not a function`). Bu nedenle menü, haber kartları ve yönetim paneli bağlantıları hiçbir şey yapmıyordu. Ziyaretçi sitesi ve yönetim panelindeki 74 bağlantı, tarayıcının kendi gezinmesini kullanan sade `<a href>` etiketlerine çevrildi; `@next/next/no-html-link-for-pages` kuralı gerekçesi yazılarak kapatıldı ve `next/link` kullanımını yasaklayan regresyon testi eklendi.
- Değişen ana dosyalar: `app/site-chrome.tsx`, `app/site-config.ts`, `eslint.config.mjs`, `app/site-client.tsx`, `app/page.tsx`, `app/son-dakika/page.tsx`, `app/arama/page.tsx`, `app/videolar/page.tsx`, `app/yazarlar/page.tsx`, `app/yazar/[slug]/page.tsx`, `app/kurumsal/[slug]/page.tsx`, `app/not-found.tsx`, `app/canli/page.tsx`, `app/canli/live-player.tsx`, `app/haber/[slug]/page.tsx`, `app/haber/[slug]/share-buttons.tsx`, `app/kategori/[slug]/page.tsx`, `app/admin/panel.tsx`, `app/api/users/route.ts`, `app/sitemap.ts`, `app/robots.ts`, `app/globals.css`, `db/index.ts`, `scripts/test-onizleme.sh`, `tests/rendered-html.test.mjs`.
- Doğrulama: `npm test` içinde üretim derlemesi ve **35 test geçti; 0 başarısız, 0 atlandı** (önceki 26 testin tamamı korundu, 9 yeni senaryo eklendi). Yeni testler: sekiz sayfadaki bütün iç bağlantıların gerçek sayfaya gitmesi (33 ayrı adres, 0 kırık), son dakika akışı, arama sonucu/boş sonuç/kısa terim/HTML enjeksiyonu, video merkezi, yedi kurumsal sayfa ve noindex davranışı, yazar arşivi ve bilinmeyen imza, özel 404 + robots + sitemap, yeni yayınlanan haberin arama/yazar/son dakika/video yüzeylerinde görünmesi, kullanıcı rolü değiştirme ve pasife alma korumaları, gezinme bağlantılarının `next/link`'e bağlı olmaması. `npx tsc --noEmit` başarılı; `npm run lint` **0 hata**, ham `<img>` yüzeyleri için **28 performans uyarısı** ile tamamlandı. Tarayıcı doğrulaması: 1440 px masaüstünde ana sayfa, son dakika, canlı yayın, yazarlar, arama ve kurumsal sayfa; 390 px mobilde yatay taşma yok (`scrollWidth == innerWidth`), mobil menü 16 bağlantıyla açıldı, menü araması açılıp `/arama?q=` adresine gitti. Gerçek fare tıklamasıyla menüdeki **Gündem**, **Son Dakika** ve haber kartı bağlantıları doğru sayfaları açtı; konsoldaki `RSC prefetch setup error` kayboldu.
- Kalan karar veya risk: Sosyal medya hesap adresleri, web canlı yayın HLS adresi ve künye/iletişim kurumsal bilgileri müşteri tarafından verilmedi; `app/site-config.ts` içinde boş bırakıldı ve boş oldukları sürece bağlantı olarak gösterilmiyor. Video merkezi haber editöründeki **Video URL** alanına bağlıdır; alan boşken sayfa yönlendirici boş durum gösterir. Depoda `vinext@1.0.0-beta.2` kullanılıyor; `1.0.0-beta.8` mevcut. Sürüm yükseltildiğinde `next/link` davranışı yeniden ölçülüp istemci tarafı geçişlere dönülüp dönülmeyeceğine karar verilmelidir. Üst bilgi bandındaki döviz kurları `api.frankfurter.app` isteği başarısız olduğunda kod içindeki sabit değerleri (41,12 / 48,06 / 55,74) gösteriyor; haber sitesinde yanlış veri riski taşıdığı için sunucu tarafı önbellekli bir kaynağa taşınmalı veya istek başarısızsa gösterim kaldırılmalıdır. Değişiklikler kullanıcı isteğiyle commit edilmedi; `main` push'u staging dağıtımını tetiklediği için yayın onayı bekliyor.

### 2026-08-22 — Panelden yönetilen site ayarları, adres eşlemesi ve gerçek piyasa verisi

- İstek: Yayınlanan işin staging'e dağıtılması; kalan işlerin mümkün olduğunca yönetim panelinden yönetilebilir hale getirilmesi (canlı yayın adresi, sosyal hesaplar, künye/iletişim bilgileri, eski site adres envanteri); yapılandırma ve güvenlik yerine ürün tarafına odaklanılması; döviz göstergesinin çözülmesi; AI haber masasının beklemesi; operasyon için kurgu hazırlanması.
- Yapılanlar: Önceki iş `main`'e alınıp staging'e dağıtıldı ve dış IP üzerinden bütün yeni rotalar doğrulandı. `site_settings` ve `redirects` tabloları eklendi; alan tanımı, doğrulama ve varsayılanlar `db/settings-model.mjs` içinde toplandı. Yönetim paneline **Site Ayarları** sekmesi eklendi: canlı yayın HLS adresi, yedek yayın adresi, uydu/platform bilgisi, dört sosyal hesap, ticari unvan, sorumlu müdür, haber koordinatörü, adres, telefon, üç e-posta ve düzenlenebilir yayın akışı. Ziyaretçi sitesi bu değerleri okur; sosyal hesap boşsa bağlantı üretilmez, canlı yayın adresi boşsa oynatıcı yerine kesinti ekranı gösterilir. Ana kaynak açılmazsa oynatıcı tanımlı yedek kaynağa geçer. Yönetim paneline **Adres Yönetimi** sekmesi eklendi: tek tek veya toplu envanter yapıştırarak eski adres → yeni adres eşlemesi, kalıcı/geçici tür, kullanım sayacı, durdurma/silme ve hedef adreslerin gerçekten açıldığını doğrulayan kontrol. Eşleme bulunan adres kalıcı yönlendirme ile yeni adrese gider; büyük/küçük harf, sondaki eğik çizgi, sorgu ve tam URL farkları tek biçime indirilir. Var olan `/haber`, `/kategori`, `/yazar` ve `/kurumsal` rotaları da kendi 404'lerinden önce eşleme tablosuna bakar. Üst bilgi bandındaki döviz kuru artık koda gömülü sabit değer değil: TCMB günlük bülteninden sunucu tarafında okunuyor, 15 dakika önbelleğe alınıyor, kaynak ve bülten tarihi birlikte gösteriliyor; veri alınamazsa gösterge hiç çizilmiyor. Hava durumu da aynı uçtan sunucu tarafında geliyor, böylece tarayıcıdan üçüncü tarafa istek gitmiyor. Operasyon kurgusu `deployment/OPERASYON.md` içinde tanımlandı: izlenecek sinyaller ve eşikler, P1/P2/P3 alarm seviyeleri, nöbet sorumlulukları, olay müdahale akışı, RPO/RTO hedefleri, bakım penceresi ve canlıya geçiş kontrol listesi.
- Değişen ana dosyalar: `db/settings-model.mjs`, `db/index.ts`, `app/api/settings/route.ts`, `app/api/redirects/route.ts`, `app/api/piyasa/route.ts`, `app/admin/site-settings.tsx`, `app/admin/redirects.tsx`, `app/admin/panel.tsx`, `app/site-config.ts`, `app/site-chrome.tsx`, `app/site-client.tsx`, `app/legacy-redirect.ts`, `app/[...eskiAdres]/page.tsx`, `app/canli/page.tsx`, `app/canli/live-player.tsx`, `app/kurumsal/[slug]/page.tsx`, `app/haber/[slug]/page.tsx`, `app/kategori/[slug]/page.tsx`, `app/yazar/[slug]/page.tsx`, `app/sitemap.ts`, `app/globals.css`, `deployment/OPERASYON.md`, `tests/rendered-html.test.mjs`.
- Doğrulama: `npm test` içinde üretim derlemesi ve **41 test geçti; 0 başarısız, 0 atlandı** (önceki 35 senaryo korundu, 6 yeni senaryo eklendi). Yeni testler: ayar modelinin adres/e-posta/yayın akışı doğrulaması, yönlendirme modelinin yol normalleştirmesi ve korumalı yol reddi, panelden kaydedilen ayarların canlı yayın/künye/iletişim/ana sayfa yüzeylerine yansıması, oturumsuz ayar değişikliğinin reddi, eski adreslerin kalıcı yönlendirmesi (haber yolu dahil), toplu envanter aktarımı, hedef kontrolü, kullanım sayacı, durdurma sonrası 404, piyasa ucunun Türkçe biçimli TCMB verisi döndürmesi ve koddan sabit kur değerlerinin kalkması, operasyon kurgusunun kritik bölümleri. `npx tsc --noEmit` başarılı; `npm run lint` **0 hata**, mevcut ham `<img>` yüzeyleri için 28 uyarı. Tarayıcı doğrulaması: yönetim panelinde 10 bölüm listelendi, Site Ayarları formu dört grup ve 35 alanla açıldı, panelden girilen yayın adresi ve X hesabı kaydedildikten sonra `/canli` ve ana sayfada göründü; Adres Yönetimi ekranından toplu envanter aktarıldı ve tablo güncellendi. `/api/piyasa` gerçek TCMB verisi döndürdü (21.08.2026 bülteni; USD 47,97 · EUR 56,12 · GBP 65,60).
- Toplu envanter aktarımında bulunan hata: boşluk içeren serbest metin satırı (örn. "bozuk satır") iki parçaya bölünüp geçerli bir eşleme gibi kaydediliyordu. Artık her iki parçanın da yol veya tam adres görünmesi zorunlu; sessiz yanlış kayıt regresyon testiyle kapatıldı.
- Kalan karar veya risk: Canlı yayın HLS adresi, sosyal hesaplar ve künye/iletişim bilgileri artık kod değişikliği gerektirmiyor; yöneticinin panelden girmesi yeterli. Eski site adres envanteri müşteri tarafından verildiğinde Adres Yönetimi ekranından aktarılacak. Operasyon kurgusu doküman düzeyindedir: hata izleme aracı, dış uptime kontrolü ve alarm kanalı hesap/karar bekliyor; yedekler hâlâ tek sunucuda olduğu için ikinci lokasyon canlıya geçişten önce zorunludur. AI haber masası istek üzerine bekletildi. `vinext` beta.2 → beta.8 yükseltmesi ayrı iş olarak duruyor.

### 2026-08-24 — Eski kozatv.com.tr arşivinin yeni siteye aktarımı

- İstek: `https://www.kozatv.com.tr/` üzerindeki tüm içeriğin yeni Koza TV'ye taşınması; yeni site canlıya alınana kadar içeriği web sitesinden alacak bir köprü kurulması.
- Keşif: Eski site WordPress değil; nginx üzerinde özel bir yapı. Sitemap dizini aylık dosyalara ayrılıyor (`haberler-YYYY-M.xml`) ve toplam **7.718 haber adresi** içeriyor (2020–2026). Her haber sayfasında tam `NewsArticle` JSON-LD var: başlık, açıklama, kategori (`articleSection`), yazar, görsel, yayın/güncelleme tarihi. **Kritik bulgu:** JSON-LD içindeki `articleBody` yaklaşık 500 karakterde kesiliyor (wordCount 114–1488 diyor), bu yüzden tam metin sayfadaki `class="detay"` konteynerinden okunuyor; paragraflar ve ara başlıklar korunuyor. Görseller `/images/haber/` altında ve erişilebilir. Aktarım, sitenin kendi yayımladığı sitemap envanteri üzerinden yürüyor; `robots.txt` içinde tarayıcılara kapalı olan `/arsiv` ve `/ara` yolları kullanılmıyor.
- Yapılanlar: `db/import-model.mjs` içinde ağ erişimi olmadan test edilebilen ayrıştırma katmanı kuruldu: sitemap okuma, JSON-LD çıkarma, tam gövde ve ara başlık ayrıştırma, eski adresten sabit slug üretme, alan adı izin listesi. `import_items` tablosu ile keşfedilen adresler, durum ve hata mesajları veritabanında tutuluyor; keşif ve aktarım tekrar çalıştırılabilir, mükerrer kayıt üretmiyor. `app/api/import/route.ts` üç işlem sunuyor: `discover` (sitemap taraması), `run` (parti parti aktarım), `retry` (hatalıları kuyruğa alma). Aktarımda kategori sitede yoksa menüde gizli olarak açılıyor, kapak görseli medya kütüphanesine indiriliyor (eski sitenin ayakta kalmasına bağımlılık kalmıyor), özgün yayın tarihi korunuyor ve eski adres yeni habere kalıcı olarak yönlendiriliyor. Yönetim paneline **İçerik Aktarımı** sekmesi eklendi: sayaçlar, ilerleme çubuğu, tek parti veya kuyruk boşalana kadar sürekli aktarım, durdurma, parti büyüklüğü, taslak/yayında seçeneği ve son işlem kaydı. Sunucu tarafında serbest adres çekmeyi (SSRF) engellemek için yalnızca `kozatv.com.tr` ve `www.kozatv.com.tr` adreslerinden veri alınıyor; istekler 250 ms aralıkla ve zaman aşımıyla yapılıyor.
- Değişen ana dosyalar: `db/import-model.mjs`, `db/index.ts`, `app/api/import/route.ts`, `app/admin/content-import.tsx`, `app/admin/panel.tsx`, `app/globals.css`, `tests/fixtures/eski-haber.html`, `tests/rendered-html.test.mjs`.
- Doğrulama: `npm test` içinde üretim derlemesi ve **45 test geçti; 0 başarısız, 0 atlandı** (önceki 41 senaryo korundu, 4 yeni senaryo eklendi). Yeni testler: alan adı izin listesi (dış alan adı, iç ağ adresi, `file://` ve benzer görünen alan adı reddi), sitemap ayrıştırma, eski adresten slug/yol üretimi, sabit örnek sayfadan tam gövde ve ara başlık çıkarımı, yorum bölümünün ve arayüz etiketlerinin gövdeye girmemesi, gövde konteyneri yoksa haberin atlanması, aktarım ucunda oturumsuz erişimin ve geçersiz işlemin reddi. `npx tsc --noEmit` başarılı; `npm run lint` **0 hata**. Gerçek aktarım denemesi: 33 sitemap tarandı, 7.718 adres kuyruğa alındı; 18 haber gerçek veriyle aktarıldı. Aktarılan haber sayfasında başlık, kategori (Siyaset), yazar, 6 paragraf, iki ara başlık (`KOLTUĞUNA GERİ DÖNDÜ`, `NE OLMUŞTU?`), özgün tarih (24 Ağustos 2026 12:29) ve indirilen WebP kapak görseli doğrulandı. Eski adres `haber-...-9715.html` yeni habere kalıcı yönlendirme verdi. Gövde uzunlukları 1.023–7.803 karakter (ortalama 3.139); 8/8 görsel indi; hiçbir gövdeye arayüz metni sızmadı. Keşif ikinci kez çalıştırıldığında 0 yeni kayıt eklendi ve mükerrer haber oluşmadı. Ölçülen hız haber başına ~0,5 saniye; 7.718 haber için tahmini süre ~64 dakika.
- Yolda bulunan hata: Arayüz etiketlerini gövdeden ayıklayan filtre `\b` sözcük sınırı kullandığı için Türkçe `ş` harfiyle biten "Paylaş" satırı elenmiyordu. Filtre satırın tamamını karşılaştıracak biçimde düzeltildi ve regresyon testiyle kapatıldı.
- Kalan karar veya risk: Aktarım varsayılan olarak **taslak** üretir; arşivin sitede hemen görünmesi için panelde "Doğrudan yayında aktar" açılmalıdır. 7.718 haberin tamamı aktarıldığında medya kütüphanesi ölçülen ortalamayla yaklaşık 6–7 GB yer tutacaktır; mevcut 10 GB kota ve tek sunucu diski göz önüne alındığında nesne depolamaya geçiş bu aktarımdan önce değerlendirilmelidir. Eski sitede tarayıcılara kapalı `/arsiv` bölümünde sitemap dışında içerik varsa ayrıca ele alınmalıdır. Eski sitenin künyesinde bulunan resmî X hesabı `twitter.com/kozatv01` Site Ayarları ekranına girilebilir. Eski site ayrıca `googlenews.xml` ve `/rss` yayınlıyor; Google News sitemap işinde referans alınabilir.

### 2026-08-24 — YouTube canlı yayın desteği, tam arşiv aktarımı ve sunucu taşıma hazırlığı

- İstek: Canlı yayın işinin bitirilmesi ve YouTube'un birinci sıraya alınması; eski sitedeki tüm içeriğin aktarılmasının gerçek ölçekte görülmesi; başka bir sunucuya taşınma durumunda ne yapılacağının düşünülmesi.
- Kök tespit: Eski sitenin canlı yayın sayfası (`/canli-tv`) yalnızca bir YouTube gömme etiketi içeriyor; kanal web yayınını YouTube üzerinden veriyor. Ancak sayfaya gömülü video kimliği (`fJpho27H7Mk`) YouTube'da artık bulunmuyor (oEmbed 404), yani eski sitedeki canlı yayın sayfası ölü bir video gösteriyor. Kanal kimliği `UC4Ohyy56H4EZAy0Pagsv3iA` olarak tespit edildi; kanal kimliğiyle gömme yapıldığında yayın yeniden başlasa ve video kimliği değişse bile adres güncellemek gerekmiyor.
- Yapılanlar: Canlı yayın kaynağı alanı yalnızca HLS kabul etmekten çıkarılıp `parseLiveSource` ile YouTube bağlantısı (`watch?v=`, `youtu.be`, `embed`, `live`, `channel/UC…`) ve HLS (`.m3u8`) adresini ayırt eden bir kaynak alanına dönüştürüldü. Oynatıcı kaynağa göre gömülü YouTube oynatıcısı veya video etiketi kuruyor; kaynak tanımsız ya da geçersizken sahte yayın göstermeyip kesinti ekranı veriyor. Kullanıcı adı biçimi (`@KozaTv`) gömülemediği için açık gerekçeli hata mesajı gösteriliyor. Yedek kaynak da aynı kuralı kullanıyor. Ayar doğrulaması geçersiz kaynağı kaydetmiyor. Sunucu taşımayı kolaylaştırmak için dağıtım hattındaki 10 yerde koda gömülü sunucu adresi `vars.KOZA_HOST` deposu değişkenine bağlandı; `deployment/TASIMA.md` içinde taşıma rehberi yazıldı.
- Değişen ana dosyalar: `db/settings-model.mjs`, `app/canli/live-player.tsx`, `app/canli/page.tsx`, `app/globals.css`, `.github/workflows/staging.yml`, `deployment/TASIMA.md`, `tests/rendered-html.test.mjs`.
- Doğrulama: `npm test` içinde üretim derlemesi ve **48 test geçti; 0 başarısız, 0 atlandı** (önceki 45 senaryo korundu, 3 yeni senaryo eklendi). Yeni testler: dört YouTube adres biçiminin ve kanal kimliğinin doğru gömme adresine çevrilmesi, HLS adresinin ayırt edilmesi, `javascript:` ve `.mp4` gibi geçersiz kaynakların reddi, kullanıcı adı biçiminin gerekçeli reddi, ayar kaydında aynı kuralın uygulanması, `/canli` sayfasının YouTube kaynağıyla gömülü oynatıcı kurması ve kaynak boşaltıldığında kesinti ekranına dönmesi, taşıma rehberinin ve dağıtım değişkeninin varlığı. `npx tsc --noEmit` başarılı; `npm run lint` **0 hata**. Tarayıcıda `/canli` sayfasında YouTube oynatıcısı yüklendi; eski sitedeki video kimliğiyle "Video unavailable" görüldüğü için kimliğin geçersizliği oEmbed ile doğrulandı ve kanal kimliği yaklaşımına geçildi. Tam arşiv aktarımı gerçek veriyle çalıştırıldı: 50'lik partilerle 21 saniyede 50 haber (haber başına ~0,42 sn), ilk 500 haberde 0 hata ve 0 atlanan kayıt.
- Kalan karar veya risk: Eski sitedeki ölü YouTube gömmesi eski site kapatılana kadar orada kalacak; yeni sitede kanal kimliği kullanıldığı için bu sorun tekrarlamaz. Sunucu taşıma artık bir dosya (SQLite) ve bir klasör (medya) kopyalamaya indi; asıl risk sunucu değil DNS geçişi ve kopyalama anındaki yazma boşluğu. Yedekler halen aynı sunucuda; ikinci lokasyon ayrı iş olarak duruyor. Cloudflare önüne alma işi müşterinin hesabı ve Krihost'taki nameserver değişikliğini gerektirdiği için uygulanmadı; mevcut MX (Yandex) ve diğer DNS kayıtlarının önce eksiksiz taşınması zorunludur.

### 2026-08-24 — Aktarım görsel hatası, başlık okunabilirliği ve ana sayfa/bölüm tasarımı

- İstek: Aktarılan haberlerde hep aynı görselin çıkması; ana sayfanın ve kategori/son dakika sayfalarının tasarımının beğenilmemesi, tasarım açısından örnek alınacak bir site istenmesi.
- Kök neden (aynı görsel): Aktarımda kapak görseli indirilemeyen haberlere yedek olarak `/news/gundem.jpg` yazılıyordu; bu dosya tohum verisindeki gerçek bir stüdyo karesi ("HÜSEYİN CAN GÜNER ADLİYEYE SEVK EDİLDİ"). Görseli olmayan her habere gerçek bir haber fotoğrafı gibi görünen aynı kare konuyordu. Veri incelendiğinde 7.712 haberde 7.380 farklı görsel bulunduğu, sorunun 216 haberlik yedek kümede olduğu görüldü. Bu haberlerin eski sitede de kapak görseli yok: `og:image`, `twitter:image` ve JSON-LD alanlarının üçü de dosya adı içermeyen `".../images/haber/"` klasör yolunu veriyor.
- Yapılanlar (görsel): Tarafsız `public/news/gorsel-yok.svg` yer tutucusu eklendi; "Bu haberde görsel bulunmuyor" yazısıyla eksikliği açıkça belirtiyor. Aktarımın yedeği bu dosyaya çevrildi. Görsel adayı seçimi sıkılaştırıldı: adres dosya adı ve görsel uzantısı içermiyorsa aday sayılmıyor, ayrıca `twitter:image` de yedek kaynak olarak deneniyor. Mevcut 216 kayıt tek seferlik migration ile yer tutucuya çevrildi. Aktarım paneline "kapak görseli yok" sayacı eklendi.
- Yapılanlar (tasarım): Arşiv başlıklarının %72'si (5.523/7.712) tamamen büyük harf olduğu için `db/title-model.mjs` eklendi: yalnızca tamamı büyük harf olan başlıklar okunur biçime çevriliyor, kısaltmalar (CHP, TBMM, PKK, UEFA) korunuyor, ekler küçültülüyor ("CHP'NİN" → "CHP'nin"), ilk harf noktalama atlanarak büyütülüyor ("(MAÇ" → "(Maç"). Veri değişmiyor, dönüşüm gösterim anında yapılıyor. Eski sitede `og:description` çoğu haberde başlığın kopyası olduğu için spot başlıkla aynıysa kartta gösterilmiyor; aktarımda da böyle durumlarda gövdenin ilk paragrafı spot olarak alınıyor. Ana sayfa yeniden kuruldu: masthead'deki boş gri reklam kutusu kaldırıldı, akışlar `listLatestArticles` ile tarihe göre sıralandı (arşiv aktarımından sonra demo haberleri öne çıkıyordu), bölümler havuz tükendiğinde boş kalmayacak biçimde sırayla dolduruluyor, kart görselleri 16:9 orana oturtuldu ve tembel yükleniyor. Kategori ve son dakika sayfaları aynı tasarım diline geçirildi: koyu bölüm başlığı, manşet haber, üç sütunlu ızgara, yan sütunda diğer kategoriler ve son dakika listesi. Kategori sayfalarına sayfalama eklendi (`?sayfa=`), böylece yüzlerce haberlik kategorilerde ilk 30 kayıttan sonrası erişilebilir oldu. Son dakika sayfası gün gruplarıyla "dakika dakika" akışına dönüştürüldü.
- Değişen ana dosyalar: `db/title-model.mjs`, `db/import-model.mjs`, `db/index.ts`, `app/api/import/route.ts`, `app/admin/content-import.tsx`, `app/page.tsx`, `app/kategori/[slug]/page.tsx`, `app/son-dakika/page.tsx`, `app/site-chrome.tsx`, `app/globals.css`, `public/news/gorsel-yok.svg`, ve başlık dönüşümünün uygulandığı haber/arama/video/yazar sayfaları.
- Doğrulama: `npm test` içinde üretim derlemesi ve **51 test geçti; 0 başarısız, 0 atlandı** (önceki 48 senaryo korundu, 3 yeni senaryo eklendi). Yeni testler: büyük harf başlık dönüşümü ve kısaltma korunması, spot tekrarının gizlenmesi, ana sayfanın az haberli kurulumda bile bütün bölümleri doldurması, boş reklam yer tutucusunun kalmaması, tembel yükleme, ve aktarımda gerçek haber karesinin yedek olarak kullanılmadığı. `npx tsc --noEmit` başarılı; `npm run lint` **0 hata**. Tam arşiv aktarımı gerçek veriyle tamamlandı: **7.712 haber, 0 hata, 0 atlanan, 3.372 saniye**; medya 155 MB/1.453 dosya ölçümünden 0,79 GB'a projekte edildi. Tarayıcıda ana sayfa, `/kategori/gundem`, `/son-dakika` ve `/kategori/kultur-sanat?sayfa=2` kontrol edildi: dört sayfada da **kalan büyük harf başlık sayısı 0**, sayfalama çalışıyor, son dakika akışında 36 satır ve 4 gün grubu render oldu. 375 px mobilde yatay taşma yok.
- Kalan karar veya risk: Başlık dönüşümü sezgiseldir; listede olmayan ve sesli harf içeren bir kısaltma (örn. "AKOM") kelime gibi çevrilebilir. Böyle bir durumda çözüm ya `knownAcronyms` listesine eklemek ya da haberi panelden düzeltmektir; veri bozulmadığı için geri dönüş kaybı yoktur. Kapak görseli olmayan 216 haber için kaynakta da görsel bulunmadığından tekrar denemek sonucu değiştirmez; editör panelden görsel ekleyebilir. Ana sayfada manşet hâlâ `is_featured` işaretine bağlı; arşivde işaretli haber olmadığı için en güncel haberler manşete geçiyor.

### 2026-08-24 — Aktarımda görsel hatasının görünür kılınması

- İstek: Staging'de aktarılan haberlerin görsellerinin gelmemesi.
- Tespit: Staging'de hiçbir `/media/` görseli yok; yerel aktarımda 7.712 haberin 7.380'i görselle indi. Aynı kod, aynı kaynak. Fark sunucu tarafında ve dışarıdan görülemiyor, çünkü `importImage` bütün hataları `catch { return ""; }` ile yutuyordu. Asıl kusur bu: hata sessizce yutulduğu için sebep teşhis edilemiyor.
- Yapılanlar: `importImage` artık başarısızlık sebebini döndürüyor (HTTP kodu, boş yanıt, kaydetme hatası) ve sebep aktarım kaydına yazılıyor. Yönetim paneline **"Eksik görselleri indir"** işlemi eklendi; yalnız kapak görseli inememiş haberlerde görseli yeniden dener, haber metnini yeniden çekmez ve sebepleri sayarak raporlar. `storeImage` artık türü dosya imzasından da tanıyor; kaynak sunucu yanlış veya eksik `content-type` gönderse bile geçerli görsel kaydedilebiliyor.
- Değişen ana dosyalar: `app/api/import/route.ts`, `app/admin/content-import.tsx`, `db/media-storage.ts`, `db/index.ts`, `tests/rendered-html.test.mjs`.
- Doğrulama: `npm test` içinde üretim derlemesi ve **52 test geçti; 0 başarısız, 0 atlandı**. Yeni test: imza tabanlı tür tanıma kuralı, görsel hatasının sessizce yutulmaması, yeniden deneme işleminin yetkisiz erişimi reddetmesi ve sayaç döndürmesi. `npx tsc --noEmit` başarılı; `npm run lint` **0 hata**.
- Kalan karar veya risk: Staging'deki görsel hatasının kök nedeni henüz bilinmiyor; panelden "Eksik görselleri indir" çalıştırıldığında sebep ekranda görünecek. En olası adaylar sunucudaki boş disk alanı koruması (1 GB altına düşünce kaydetme reddediliyor) ve medya dizini yazma izni. Sunucu kimlik bilgileri sohbette paylaşıldığı için parola ve API anahtarının değiştirilmesi önerildi.

### 2026-08-24 — Sunucudaki medya izni ve kapak görsellerinin onarımı

- İstek: Staging'de haber görsellerinin görünmemesi; sunucuya bağlanıp sorunun çözülmesi.
- Kök neden: `/srv/kozatv/data/media` dizini `root:root` sahipliğinde ve `700` izniyle oluşturulmuştu; uygulama `kozatv` kullanıcısıyla çalıştığı için dizine hiç yazamıyordu. Aktarımda veritabanı kaydı oluşuyor, dosya yazma işlemi izin hatası alıyor ve haber yer tutucu görsele düşüyordu. Disk sorunu yoktu (38 GB'ın 23 GB'ı boş). Hata `importImage` içinde sessizce yutulduğu için dışarıdan görülemiyordu.
- Yapılanlar: Dizin sahipliği `kozatv:kozatv` ve izni `750` yapıldı; yazma ve alt klasör oluşturma doğrulandı. Dağıtım betiğinin veri dizinine dokunmadığı, dolayısıyla düzeltmenin kalıcı olduğu kontrol edildi. Yalnız kapak görseli eksik haberleri yeniden indiren `scripts/kapak-gorsellerini-onar.mjs` yazıldı; haber metnini yeniden çekmez, var olan görselleri ellemez, tekrar çalıştırmak güvenlidir ve başarısızlık sebeplerini sayarak raporlar.
- Değişen ana dosyalar: `scripts/kapak-gorsellerini-onar.mjs`, `tests/rendered-html.test.mjs`, `AGENTS.md`; sunucuda `/srv/kozatv/data/media` sahiplik ve izni.
- Doğrulama: `npm test` içinde üretim derlemesi ve **53 test geçti; 0 başarısız, 0 atlandı**. Yeni test, onarım aracının depolama kurallarının (SHA-256 tabanlı ad, yıl/ay klasörü, `wx` bayrağı, kabul edilen dört tür) `db/media-storage.ts` ile aynı kalmasını ve aracın veri silmemesini denetler. Araç yerel veritabanında denendi; oradaki 216 haberin kaynağında gerçekten görsel bulunmadığı için "kaynakta kapak görseli yok" sebebiyle raporladı.
- Kalan karar veya risk: Sunucu kimlik bilgileri sohbette paylaşıldı; parolanın ve API anahtarının değiştirilmesi gerekiyor. Medya dizini izni ilk kurulumda yanlış oluşmuştu; benzer kurulumlarda `deployment/hetzner/README.md` adımlarına dizin sahipliği kontrolü eklenmelidir.

### Yeni günlük kaydı şablonu

### 2026-08-20 — Güvenli yayın stüdyosu, yedekleme ve editoryal onay

- İstek: Stage ortamının canlı alan adı gibi ele alınmaması; SQLite/medya yedeği ve güvenlik; profesyonel blok editörü; muhabirden yayın yönetmenine tam onay akışı.
- Yapılanlar: Blok tabanlı haber editörü, dört saniyelik otomatik taslak kaydı, masaüstü/mobil ön izleme, çoklu görsel, video ve sosyal bağlantı blokları, revizyon geri yükleme, ekip yorumları, görev atama ve eşzamanlı düzenleme sürüm kontrolü eklendi. Muhabir → editör → yayın yönetmeni akışı; değişiklik talebi, ret gerekçesi, yayın onayı, planlama, yayın, yayından kaldırma, düzeltme notu, manşet sırası, son dakika ve 30 günlük operasyon özeti veritabanı/API/UI düzeyinde kuruldu. Yayın öncesi başlık, spot, gövde, görsel alt metni ve kaynak kontrolü sunucuda zorunlu hale getirildi. SQLite ve medya için doğrulamalı günlük/haftalık/aylık yedek, tek komut geri yükleme, aylık geri yükleme testi, disk/servis/veritabanı sağlık timer'ı ve opsiyonel webhook alarmı hazırlandı. Stage sunucusunda anahtarlı `koza-admin` hesabı doğrulandı; root/parola SSH girişi kapatıldı ve UFW yalnız 22/80/443 portlarına indirildi.
- Değişen ana dosyalar: `db/index.ts`, `app/api/editorial/route.ts`, `app/api/articles/route.ts`, `app/admin/panel.tsx`, `app/admin/workflow-studio.tsx`, `app/haber/[slug]/*`, `app/globals.css`, `deployment/hetzner/kozatv-*`, `deployment/hetzner/README.md`, `tests/rendered-html.test.mjs`, `AGENTS.md`.
- Doğrulama: `npm test` production build'i tamamladı; editoryal onay, onaysız yayın engeli, revizyon/yorum/işlem geçmişi, yetkisiz erişim ve gerçek yedek doğrulaması dahil **26 test geçti, 0 başarısız, 0 atlandı**. `npx tsc --noEmit` başarılı; `npm run lint` **0 hata**, mevcut ham `<img>` yüzeyleri için 23 performans uyarısıyla tamamlandı. Sunucuda backup, health ve restore-test servisleri elle çalıştırılıp başarılı sonuç verdi; üç timer etkin. Yeni `koza-admin` oturumu/sudo doğrulandı, root SSH bağlantısı reddedildi ve UFW kuralları 22/80/443 olarak kontrol edildi.
- Kalan karar veya risk: Stage IP adresi HTTP olduğu için yalnız test hesapları kullanılmalıdır; alan adı canlıya alınırken HTTPS zorunlu kapı olacaktır. Harici yedek hedefi ve alarm webhook'u için müşteri hesabı/URL'si henüz verilmedi. Yedekler şu an aynı sunucuda tutulur; gerçek felaket kurtarma için ikinci lokasyon zorunludur.

Her yeni işte aşağıdaki biçimi kullan:

```md
### YYYY-AA-GG — Kısa iş adı

- İstek:
- Yapılanlar:
- Değişen ana dosyalar:
- Doğrulama:
- Kalan karar veya risk:
```

### 2026-08-25 — Premium ana sayfa ve otomatik manşet vitrini

- İstek: Görsel aktarımı düzeltildikten sonra ana sayfanın, özellikle manşet/slider alanının yeni kurulmuş güçlü bir haber markasına yakışacak kadar modern, alımlı ve otomatik geçişli hale getirilmesi.
- Yapılanlar: Ana sayfa sıcak kâğıt zeminli premium editoryal düzene geçirildi; yeni bölüm girişi, 640 px sinematik manşet, katmanlı kontrast, çağdaş tipografi, cam görünümlü kontrol şeridi, numaralı slaytlar, ilerleme çizgisi, önceki/sonraki ve duraklat/sürdür kontrolleri eklendi. Manşet beş haber arasında 6 saniyede yumuşak geçiyor; fare/klavye etkileşiminde bekliyor ve azaltılmış hareket tercihinde otomatik geçişi kapatıyor. Günün Akışı, öne çıkan kartlar, son haberler, son dakika yan sütunu, yazarlar ve video yüzeyleri aynı tasarım dilinde yenilendi. Eski demo manşetlerinin gerçek arşivin önüne geçmesini engelleyen güncellik ve gerçek görsel öncelikli seçim modeli kuruldu. Mobil QA'da bulunan dört sütuna geri dönen yazar şeridi taşması tek sütunla kapatıldı; manşet metni arka plan sekmesinde dahi tam kontrastla görünür tutuldu.
- Değişen ana dosyalar: `app/page.tsx`, `app/site-client.tsx`, `app/globals.css`, `db/homepage-model.mjs`, `tests/rendered-html.test.mjs`, `scripts/kapak-gorsellerini-onar.mjs`, `AGENTS.md`.
- Doğrulama: Staging ziyaretçi yüzeylerinden örneklenen **127 benzersiz görselin 127'si HTTP 200 ve doğru görsel türüyle** açıldı; çalışma görsel aktarımı yerine tasarıma odaklandı. `npm test` üretim derlemesiyle birlikte **54 test geçti; 0 başarısız, 0 atlandı**. `npx tsc --noEmit` başarılı; `npm run lint` **0 hata**, mevcut ham `<img>` yüzeyleri için **33 performans uyarısı** ile tamamlandı; bağımsız `npm run build` standalone çıktıyı başarıyla üretti. Gerçek tarayıcı motorunda 1440, 768 ve 390 px ölçümlerinde `scrollWidth == innerWidth`; mobil menü görünür. Manşet 6 saniyede 1'den 2'ye otomatik geçti, duraklatıldığında aynı slaytta kaldı; mobil metin opaklığı 1 ve rengi beyaz doğrulandı.
- Kalan karar veya risk: Değişiklikler kullanıcı isteğiyle commit kapsamına alındı; push edilmedi ve staging'e dağıtılmadı. Gerçek 7.712 haberlik staging verisiyle son görsel kabul kontrolü dağıtım onayından sonra yapılmalıdır. Ham `<img>` performans uyarılarının Next Image/CDN kararıyla ayrıca kapatılması gerekir.

### 2026-08-25 — Canlı eski vitrin eşitlemesi ve demo haber temizliği

- İstek: Yeni ana sayfada eski `kozatv.com.tr` ana sayfasında gösterilen gerçek haberlerin aynı sırayla yer alması, tasarımın daha şık kalması ve eski test haberlerinin tamamen kaldırılması.
- Yapılanlar: Eski ana sayfadaki `haber-...html` bağlantılarını görünür sırayla ve tekrarsız çıkaran güvenli ayrıştırıcı eklendi. Admin İçerik Aktarımı ekranına **Canlı ana sayfayı eşitle** işlemi kondu; ilk 25 görünür haber aynı doğrulanmış metin/görsel hattından doğrudan yayına alınıyor, eski adres yönlendirmesi kuruluyor, ilk beş haber manşet sırasına ve ilk haber son dakika bandına atanıyor. İşlem tekrar çalıştırıldığında mükerrer haber üretmiyor. Manşet altı, eski vitrindeki dört güncel haber ritmini koruyan fakat yeni sitenin premium tasarım dilindeki geniş görselli kartlara dönüştürüldü. Bilinen yedi prototip haber slug'ı üretim başlangıcında yalnız boş kaynak + Koza TV eşleşmesiyle siliniyor; demo seed yalnız `KOZA_ENABLE_DEMO_CONTENT=1` açık test ortamında çalışıyor.
- Değişen ana dosyalar: `db/demo-content-model.mjs`, `db/import-model.mjs`, `db/index.ts`, `app/api/import/route.ts`, `app/admin/content-import.tsx`, `app/page.tsx`, `app/globals.css`, `tests/rendered-html.test.mjs`, `AGENTS.md`.
- Doğrulama: Canlı `https://www.kozatv.com.tr/` sayfası izole veritabanına karşı eşitlendi: **25 görünür, 25 yeni, 25 işlenen, 25 sıralanan, 0 hata**. Ana sayfada canlı ilk ve ikinci haber başlıkları göründü, yedi demo haber görünmedi, dört manşet altı kart üretildi ve **13/13 benzersiz medya** HTTP 200 görsel yanıtı verdi. İkinci eşitlemede **0 yeni, 0 işlenen, 25 sıralanan** sonucu alınarak tekrar çalıştırılabilirlik doğrulandı. 390 px mobilde `scrollWidth == innerWidth`, uzun manşet kontrol şeridinin üstünde kaldı. `npm test` içinde üretim derlemesiyle **56 test geçti; 0 başarısız, 0 atlandı**; `npx tsc --noEmit` başarılı; `npm run lint` **0 hata**, mevcut **33** ham görsel uyarısıyla tamamlandı; bağımsız `npm run build` başarılı.
- Kalan karar veya risk: Değişiklikler kullanıcı isteğiyle commit kapsamına alındı; push edilmedi ve staging'e dağıtılmadı. Canlı eski site değiştikçe yönetici eşitleme düğmesini yeniden çalıştırmalıdır; otomatik zamanlayıcı ayrıca istenirse kurulabilir. Ham `<img>` performans uyarıları nesne depolama/CDN kararıyla ele alınmalıdır.

### 2026-08-25 — Premium ana sayfanın Hetzner staging dağıtımı

- İstek: Commit'e alınan yeni ana sayfa tasarımının, otomatik manşet vitrininin, canlı eski site eşitlemesinin ve demo haber temizliğinin staging ortamına dağıtılması.
- Yapılanlar: `73cafbe` commit'i fast-forward olarak `main` branch'ine push edildi. GitHub Actions üzerindeki **Koza TV Staging** iş akışı test, dosya aktarımı ve atomik Hetzner dağıtımını tamamladı.
- Değişen ana dosyalar: Dağıtılan uygulama sürümü `73cafbe`; bu yayın kaydı için `AGENTS.md`.
- Doğrulama: Push öncesinde `npm test` üretim derlemesiyle **56 test geçti; 0 başarısız, 0 atlandı**. GitHub Actions çalışması `32811613369` içindeki **Test ve Hetzner staging dağıtımı** işi başarılı tamamlandı. Dış kontrolde `/`, `/canli`, `/yazarlar` ve `/admin/giris` HTTP 200; `/admin` HTTP 307 ve `/api/auth/me` HTTP 401 döndürdü. Staging ana sayfasında **Günün Öne Çıkanları** ve **Günün Akışı** bileşenleri görüldü.
- Kalan karar veya risk: Staging IP üzerinden HTTP çalışmaktadır; canlı alan adı/DNS geçişi bu dağıtımın kapsamında değildir. Eski sitenin vitrini değiştikçe yönetici **Canlı ana sayfayı eşitle** işlemini yeniden çalıştırmalıdır.

### 2026-08-25 — Sosyal ikonlar ve site geneli etkileşim dili

- İstek: Üst banttaki sosyal medya ikonlarının iç içe görünmesinin düzeltilmesi; kategori menüsüne ve sitenin tamamındaki tıklanabilir yüzeylere belirgin hover, aktif, basılı ve klavye odağı hissi kazandırılması.
- Yapılanlar: Adresi tanımlanmamış sosyal hesapların karakter yer tutucuları kaldırıldı; yalnız gerçek adresi bulunan hesaplar Facebook, X, YouTube ve Instagram için ayrı, erişilebilir SVG ikon düğmeleri olarak gösteriliyor. Ana menüye animasyonlu hover çizgisi, güçlü `aria-current` aktif durumu ve basma geri bildirimi eklendi. Mobil menü, kategori çipleri, sayfalama, haber/yan akış/video/yazar kartları ve metin bağlantıları ortak etkileşim diline geçirildi; dokunmatik cihazlarda yapışkan hover engellendi ve azaltılmış hareket tercihi korundu.
- Değişen ana dosyalar: `app/site-chrome.tsx`, `app/globals.css`, `tests/rendered-html.test.mjs`, `AGENTS.md`.
- Doğrulama: `npm test` Node 23 ortamında production build ile **56 test geçti; 0 başarısız, 0 atlandı**. `npx tsc --noEmit` başarılı; `npm run lint` **0 hata**, mevcut ham `<img>` kullanımları için **33 uyarı** ile tamamlandı; bağımsız `npm run build` başarılı. Geçici veritabanında dört sosyal hesapla 1440 px ana sayfa/kategori/canlı yayın, 768 px kategori ve mobil ana sayfa/haber görünümleri gerçek Chrome motorunda incelendi. Dört ikon birbirinden ayrı dairesel düğmeler olarak göründü; aktif kategori kalın kırmızı çizgiyle ayrıldı. Gerçek 390 px emülasyonda `scrollWidth == innerWidth == 390`, canlı yayın ve menü kontrolleri viewport içinde kaldı. Ana ziyaretçi rotalarının tamamı HTTP 200 döndürdü; Türkçe arama URL kodlamasıyla ayrıca doğrulandı.
- Kalan karar veya risk: Değişiklikler kullanıcı isteğiyle commit ve Hetzner staging dağıtımı kapsamına alındı. Yerel kabuğun varsayılan Node 16 sürümü build başlamadan `node:readline/promises` hatası verdi; proje testleri NVM ile Node 23'e geçirilince eksiksiz geçti. Ham `<img>` performans uyarıları ayrıca ele alınmalıdır.

### 2026-08-26 — Sosyal ikon ve etkileşim sürümünün staging dağıtımı

- İstek: Sosyal medya ikonları ve site geneli tıklama hissi iyileştirmelerinin commit edilip Hetzner staging ortamına dağıtılması.
- Yapılanlar: `17ccea7` uygulama commit'i fast-forward olarak `main` branch'ine push edildi. GitHub Actions üzerindeki **Koza TV Staging** iş akışı test, aktarım ve atomik dağıtım adımlarını başarıyla tamamladı.
- Değişen ana dosyalar: Dağıtılan uygulama sürümü `17ccea7`; dağıtım kaydı için `AGENTS.md`.
- Doğrulama: Push öncesinde `npm test` production build ile **56 test geçti; 0 başarısız, 0 atlandı**. `npm run lint` **0 hata** ve mevcut **33** ham görsel performans uyarısıyla, bağımsız `npm run build` başarıyla tamamlandı. GitHub Actions çalışması `32939104787` başarılı oldu. Dış kontrolde `/`, `/kategori/gundem`, `/canli`, `/yazarlar` ve `/admin/giris` HTTP 200; `/admin` HTTP 307 ve `/api/auth/me` HTTP 401 döndürdü. Gündem menüsünde tek aktif `aria-current` işareti görüldü ve eski `f𝕏▶◎` ikon dizisi bulunmadı.
- Kalan karar veya risk: Staging IP üzerinden HTTP çalışmaktadır; canlı alan adı ve DNS geçişi bu dağıtımın kapsamında değildir. Sosyal hesap adresleri panelde boşsa ikonların hiç gösterilmemesi beklenen davranıştır. Ham `<img>` performans uyarıları ayrıca ele alınmalıdır.


### 2026-08-26 — Kompakt son dakika, canlı bilgi bandı ve resmî sosyal hesaplar

- İstek: Ana sayfadaki aşağı doğru gereksiz uzayan son dakika kutusunun ve bozuk “Daha Fazla Haber” alanının yeniden tasarlanması; sıcaklık ve döviz bilgisinin daha güçlü sunulması; admin panelinden yönetilen son dakika ibaresinin haber görsellerinde kullanılması; Koza TV’nin resmî Facebook, X, Instagram ve YouTube hesaplarının eklenmesi.
- Yapılanlar: Son dakika kutusundaki bütün bağlantılara yanlışlıkla uygulanan iki sütunlu grid kuralı yalnız haber satırlarına daraltıldı. Akış beş gelişmeyle sınırlanıp masaüstünde kompakt, tablette iki sütunlu, mobilde tek sütunlu hale getirildi; çağrı bağlantısı tek satırlı güçlü bir butona dönüştürüldü. Üst bant koyu yayın şeridi olarak yeniden tasarlandı; İstanbul hava durumu, Dolar, Euro, Sterlin, TCMB kaynağı ve bülten tarihi birbirinden ayrılan bilgi kartlarıyla sunuldu. Son dakika işareti, admin panelindeki mevcut `isBreaking` alanına bağlı dikey kırmızı görsel şeride dönüştürüldü; normal haberlerde yanlış son dakika bandı ve etiketi kaldırıldı. Editör ekranındaki seçeneğin etkisi açıklığa kavuşturuldu. Resmî hesaplar kanonik adreslerle varsayılanlara eklendi; mevcut veritabanında yalnız boş sosyal ayarları bir kez dolduran ve sonraki yönetici tercihini ezmeyen geçiş kuruldu.
- Değişen ana dosyalar: `app/page.tsx`, `app/site-client.tsx`, `app/site-chrome.tsx` ile ortak çalışan `app/globals.css`, `app/kategori/[slug]/page.tsx`, `app/son-dakika/page.tsx`, `app/haber/[slug]/page.tsx`, `app/admin/panel.tsx`, `app/admin/workflow-studio.tsx`, `db/settings-model.mjs`, `db/index.ts`, `tests/rendered-html.test.mjs`, `AGENTS.md`.
- Doğrulama: `npm test` production build ile **57 test geçti; 0 başarısız, 0 atlandı**. `npx tsc --noEmit` başarılı; `npm run lint` **0 hata**, mevcut ham `<img>` yüzeyleri için **33 performans uyarısı** ile tamamlandı; bağımsız `npm run build` başarılı. Gerçek Chrome motorunda 1440, 900 ve 390 px kontrol edildi: her görünümde `scrollWidth == clientWidth`; son dakika alanı sırasıyla 561/409/517 px yüksekliğinde ve beş satır; çağrı butonu `white-space: nowrap`. Mobilde İstanbul sıcaklığı ve iki temel kur görünür, canlı yayın metni korunur. Dört sosyal hesabın dördü doğru dış adrese gider; son dakika şeridi mobilde 33×105 px, haber detayında 33×92 px ölçüldü. Normal haberin son dakika bandı taşımadığı ayrıca doğrulandı.
- Kalan karar veya risk: Değişiklikler kullanıcı isteğiyle commit ve Hetzner staging dağıtımı kapsamına alındı. TCMB ve hava durumu dış kaynakları erişilemezse mevcut güvenli davranış gereği bilgi kartları gizlenir, uydurma değer gösterilmez. Ham `<img>` performans uyarıları ayrıca ele alınmalıdır.

### 2026-08-26 — Son dakika ve canlı bilgi bandının staging dağıtımı

- İstek: Kompakt son dakika alanının, canlı bilgi bandının, resmî sosyal hesapların ve yönetilebilir son dakika şeridinin commit edilip Hetzner staging ortamına doğrudan dağıtılması.
- Yapılanlar: `9ed185c` uygulama commit'i fast-forward olarak `main` branch'ine push edildi. GitHub Actions üzerindeki **Koza TV Staging** iş akışı test, aktarım ve atomik dağıtım adımlarını başarıyla tamamladı.
- Değişen ana dosyalar: Dağıtılan uygulama sürümü `9ed185c`; dağıtım kaydı için `AGENTS.md`.
- Doğrulama: Push öncesinde `npm test` production build ile **57 test geçti; 0 başarısız, 0 atlandı**. GitHub Actions çalışması `32943349518` başarıyla tamamlandı. Dış kontrolde `/`, `/kategori/gundem`, `/son-dakika`, `/canli`, `/yazarlar` ve `/admin/giris` HTTP 200; `/admin` HTTP 307 ve `/api/auth/me` HTTP 401 döndürdü. Staging ana sayfasında kompakt çağrı bağlantısı, son dakika görsel şeridi ve dört resmî sosyal hesap doğrulandı.
- Kalan karar veya risk: Staging IP üzerinden HTTP çalışmaktadır; canlı alan adı ve DNS geçişi bu dağıtımın kapsamında değildir. TCMB ve hava durumu verisi alınamazsa bilgi kartlarının gizlenmesi beklenen güvenli davranıştır. Ham `<img>` performans uyarıları ayrıca ele alınmalıdır.

### 2026-08-26 — Kaynak oranlı manşet ve hareketli sağ son dakika şeridi

- İstek: Son dakika görsel şeridinin Koza TV örneğindeki gibi sağ tarafa alınması ve hareketli, belirgin hale getirilmesi; eski siteden aktarılan görsellerin ana sayfa sliderında kötü kırpılmasının düzeltilmesi.
- Yapılanlar: Canlı Koza TV ana sayfasının güncel kaynakları incelendi; manşet görsellerinin `735×410` oranında sunulduğu ve sliderın otomatik ilerlediği doğrulandı. Yeni ana manşet masaüstü ve tablette doğrudan bu orana bağlandı; mobilde görsel aynı oranla üstte korunup metin koyu okuma alanında bırakıldı. Slider otomatik geçişi 5 saniyeye çekildi. Sağdaki Günün Akışı, manşet oranını bozmaması için dört kompakt gelişmeye indirildi ve grid taşması kapatıldı. Son dakika şeridi bütün haber görsellerinde sağa taşındı; 1,15 saniyelik dikkat animasyonu eklendi, azaltılmış hareket tercihinde animasyon kapatıldı.
- Değişen ana dosyalar: `app/globals.css`, `app/page.tsx`, `app/site-client.tsx`, `tests/rendered-html.test.mjs`, `AGENTS.md`.
- Doğrulama: `npm test` production build ile **57 test geçti; 0 başarısız, 0 atlandı**. `npx tsc --noEmit` başarılı; `npm run lint` **0 hata** ve mevcut **33** ham görsel performans uyarısıyla tamamlandı. Gerçek Chrome motorunda 1440 ve 900 px ekran görüntüleri incelendi; iki sütun çakışmadan hizalandı ve görsel kompozisyonu korundu. Gerçek cihaz emülasyonunda 390 px için `scrollWidth == innerWidth == 390`; sağ şerit 33×105 px, animasyon 1,15 saniye ve slider otomatik geçişi aktif ölçüldü.
- Kalan karar veya risk: Browser becerisi çalışma alanındaki bağlantılı klasör kısıtı nedeniyle açılamadı; canlı kaynak incelemesi ve görsel kabul gerçek yerel Chrome ile tamamlandı. Değişiklikler kullanıcı isteğiyle commit ve Hetzner staging dağıtımı kapsamına alındı. Ham `<img>` performans uyarıları ayrıca ele alınmalıdır.

### 2026-08-26 — Kaynak oranlı manşetin staging dağıtımı

- İstek: Sağdaki hareketli son dakika şeridinin ve `735×410` kaynak oranını koruyan yeni ana sliderın commit edilip Hetzner staging ortamına dağıtılması.
- Yapılanlar: `22a08e0` uygulama commit'i fast-forward olarak `main` branch'ine push edildi. GitHub Actions üzerindeki **Koza TV Staging** iş akışı test, aktarım ve atomik dağıtım adımlarını başarıyla tamamladı.
- Değişen ana dosyalar: Dağıtılan uygulama sürümü `22a08e0`; dağıtım kaydı için `AGENTS.md`.
- Doğrulama: Push öncesinde `npm test` production build ile **57 test geçti; 0 başarısız, 0 atlandı**. GitHub Actions çalışması `32946105334` başarılı oldu. Dış kontrolde `/`, `/kategori/gundem`, `/son-dakika`, `/canli`, `/yazarlar` ve `/admin/giris` HTTP 200; `/admin` HTTP 307 ve `/api/auth/me` HTTP 401 döndürdü. Staging HTML'inde dört Günün Akışı satırı ve son dakika şeridi; yayın CSS'inde `aspect-ratio:735/410`, sağ konum ve `koza-breaking-pulse` animasyonu doğrulandı.
- Kalan karar veya risk: Staging IP üzerinden HTTP çalışmaktadır; canlı alan adı ve DNS geçişi bu dağıtımın kapsamında değildir. Ham `<img>` performans uyarıları ayrıca ele alınmalıdır.

### 2026-08-26 — Premium köşe yazarları ve haber servisleri vitrini

- İstek: Ana sayfadaki sıkışık görünen köşe yazarları alanının, kırık görsel hissi vermeyen ve daha hoş bir tasarıma dönüştürülmesi.
- Yapılanlar: Eski dar yazar şeridi kaldırılıp koyu editoryal zeminde dört kartlı **Köşe Yazarları & Haber Servisleri** vitrini kuruldu. Veri modelinde portre alanı bulunmadığı için kırılabilir harici fotoğraf yerine her imza için renk tonlu monogram kullanıldı. Haber merkezi ve servis hesapları köşe yazarı gibi gösterilmek yerine **Haber Servisi** etiketiyle ayrıldı; teknik `Administrator Administrator` adı ziyaretçi yüzeyinde `Koza TV Editör Masası` olarak temizlendi. Her kartta içerik sayısı, son çalışma, kategori ve arşiv bağlantısı gösterildi; belirgin hover, klavye odağı ve azaltılmış hareket desteği eklendi. Düzen masaüstünde dört sütun, tablette iki sütun, mobilde sayfayı taşırmadan yatay snap akışı olarak kuruldu.
- Değişen ana dosyalar: `app/page.tsx`, `app/globals.css`, `tests/rendered-html.test.mjs`, `AGENTS.md`.
- Doğrulama: `npm test` production build ile **58 test geçti; 0 başarısız, 0 atlandı**. Yeni regresyon testi vitrinin dört kart, monogram, servis ayrımı, teknik ad temizliği, klavye odağı ve mobil kaydırma sözleşmesini doğruladı. `npx tsc --noEmit` başarılı; `npm run lint` **0 hata**, mevcut **33** ham görsel performans uyarısıyla tamamlandı. Gerçek Chrome motorunda 1440 px görünümde dört eşit kart, 900 px tablette 2×2 düzen ve 390 px mobilde yatay snap akışı incelendi; üç görünümde de `documentWidth == viewportWidth`. Mobil vitrinde `clientWidth 330`, `scrollWidth 1054`, kart genişliği 255 px ve bölüm içindeki görsel sayısı **0** ölçüldü. Masaüstü klavye odağı 3 px beyaz çerçeveyle görünür doğrulandı.
- Kalan karar veya risk: Yazar veri modelinde gerçek portre alanı bulunmadığı için monogram bilinçli olarak kullanılıyor; ileride admin paneline yazar portresi eklendiğinde aynı kart yapısı gerçek fotoğrafı güvenli yedekle gösterebilir. Browser becerisi bağlantılı çalışma klasörü kısıtı nedeniyle açılamadı; görsel kabul gerçek yerel Chrome motoruyla tamamlandı. Değişiklikler kullanıcı isteğiyle commit ve Hetzner staging dağıtımı kapsamına alındı.

### 2026-08-26 — Köşe yazarları vitrininin staging dağıtımı

- İstek: Yeni köşe yazarları ve haber servisleri vitrininin commit edilip Hetzner staging ortamına dağıtılması.
- Yapılanlar: `9386df6` uygulama commit'i fast-forward olarak `main` branch'ine push edildi. GitHub Actions üzerindeki **Koza TV Staging** iş akışı test, dosya aktarımı ve atomik Hetzner dağıtımını başarıyla tamamladı.
- Değişen ana dosyalar: Dağıtılan uygulama sürümü `9386df6`; dağıtım kaydı için `AGENTS.md`.
- Doğrulama: Push öncesinde `npm test` production build ile **58 test geçti; 0 başarısız, 0 atlandı**. GitHub Actions çalışması `32953498758` başarılı oldu. Dış kontrolde `/`, `/kategori/gundem`, `/son-dakika`, `/canli`, `/yazarlar` ve `/admin/giris` HTTP 200; `/admin` HTTP 307 ve `/api/auth/me` HTTP 401 döndürdü. Staging ana sayfasında **Köşe Yazarları & Haber Servisleri**, dört imza kartı, dört monogram, servis etiketleri ve temizlenen `Koza TV Editör Masası` adı doğrulandı.
- Kalan karar veya risk: Staging IP üzerinden HTTP çalışmaktadır; canlı alan adı ve DNS geçişi bu dağıtımın kapsamında değildir. Gerçek yazar portreleri için veri modeline ve admin paneline ayrıca portre alanı eklenmelidir; mevcut monogramlar kırık görsel riskini ortadan kaldıran güvenli davranıştır.

### 2026-08-26 — Panelden yönetilen Koza TV mottosu

- İstek: Ana sayfa üst bölümündeki **Doğru haber. Güçlü yorum.** metninin **Şimdi konuşma zamanı** olarak değiştirilmesi ve yönetim panelinden düzenlenebilir hale getirilmesi.
- Yapılanlar: Site ayarları modeline varsayılanı `Şimdi konuşma zamanı` olan zorunlu `siteMotto` alanı eklendi. Motto baştaki/sondaki boşluklardan arındırılıyor, 3–80 karakterle sınırlandırılıyor ve boş değer reddediliyor. Admin **Site Ayarları → Yayın ve canlı akış** bölümünde açıklaması, karakter sınırı ve tarayıcı tarafı zorunluluk işaretiyle düzenlenebiliyor; kaydedilen değer tüm ziyaretçi sayfalarının ortak üst bilgisinde logo yanında gösteriliyor. Eski sabit metin kaynak koddan kaldırıldı.
- Değişen ana dosyalar: `db/settings-model.mjs`, `app/admin/site-settings.tsx`, `app/site-chrome.tsx`, `tests/rendered-html.test.mjs`, `AGENTS.md`.
- Doğrulama: `npm test` production build ile **58 test geçti; 0 başarısız, 0 atlandı**. Regresyon senaryoları varsayılan mottoyu, boşluk temizlemeyi, panelden başarılı kaydı, boş ve 80 karakteri aşan değerlerin reddini, yetkisiz isteğin 401 almasını ve eski sabit metnin kalkmasını doğruladı. `npx tsc --noEmit` başarılı; `npm run lint` **0 hata**, mevcut **33** ham görsel performans uyarısıyla tamamlandı. Gerçek Chrome motorunda 1440 px görünümde motto `220×43` px alanda logo yanında görüldü; 900 ve 390 px kırılımlarında mevcut kompakt header davranışıyla gizlendi. Üç görünümde de `documentWidth == viewportWidth` ve eski motto metni yoktu.
- Kalan karar veya risk: Tablet ve mobilde motto, mevcut 1050 px altı kompakt header kuralı gereği gizlidir; bu değişiklik var olan mobil düzeni korur. Browser becerisi bağlantılı çalışma klasörü kısıtı nedeniyle açılamadı; görsel kabul gerçek yerel Chrome motoruyla tamamlandı. Değişiklikler kullanıcı isteğiyle commit ve Hetzner staging dağıtımı kapsamına alındı.

### 2026-08-26 — Yönetilebilir motto sürümünün staging dağıtımı

- İstek: **Şimdi konuşma zamanı** mottosunun ve admin panelinden yönetim alanının commit edilip Hetzner staging ortamına dağıtılması.
- Yapılanlar: `c8a05c1` uygulama commit'i fast-forward olarak `main` branch'ine push edildi. GitHub Actions üzerindeki **Koza TV Staging** iş akışı test, dosya aktarımı ve atomik Hetzner dağıtımını başarıyla tamamladı.
- Değişen ana dosyalar: Dağıtılan uygulama sürümü `c8a05c1`; dağıtım kaydı için `AGENTS.md`.
- Doğrulama: Push öncesinde `npm test` production build ile **58 test geçti; 0 başarısız, 0 atlandı**. GitHub Actions çalışması `32955365880` başarılı oldu. Dış kontrolde `/`, `/kategori/gundem`, `/son-dakika`, `/canli`, `/yazarlar` ve `/admin/giris` HTTP 200; `/admin` HTTP 307 ve yetkisiz `/api/settings` HTTP 401 döndürdü. Staging ana sayfasında **Şimdi konuşma zamanı** mottosu bulundu ve eski **Doğru haber. Güçlü yorum.** metni bulunmadı.
- Kalan karar veya risk: Staging IP üzerinden HTTP çalışmaktadır; canlı alan adı ve DNS geçişi bu dağıtımın kapsamında değildir. Tablet ve mobilde motto mevcut kompakt header kuralı gereği gizlidir; yöneticinin değiştirdiği metin masaüstü üst bilgisinde gösterilir.

### 2026-08-26 — Mobil manşette parmakla kaydırma

- İstek: Mobil ana sayfa manşet haberlerinin yalnız sağ/sol düğmeleriyle değil, parmakla sağa ve sola kaydırılarak değiştirilebilmesi.
- Yapılanlar: Manşete modern Pointer Events tabanlı dokunma desteği eklendi. En az 42 px ve yatay yönü baskın hareket sola kaydırmada sonraki, sağa kaydırmada önceki haberi açıyor; kısa dokunuşlar ve dikey sayfa kaydırmaları manşet geçişi sayılmıyor. Kaydırma boyunca otomatik geçiş bekliyor, işlem sonrasında yeniden başlıyor; kaydırma bitişindeki sentetik tıklamanın haber bağlantısını yanlışlıkla açması engellendi. `touch-action: pan-y` ile mobil sayfanın doğal dikey hareketi korundu.
- Değişen ana dosyalar: `app/site-client.tsx`, `app/slider-gesture.mjs`, `app/globals.css`, `tests/rendered-html.test.mjs`, `AGENTS.md`.
- Doğrulama: `npm test` production build ile **58 test geçti; 0 başarısız, 0 atlandı**. `npx tsc --noEmit` ve bağımsız `npm run build` başarılı; `npm run lint` **0 hata**, mevcut **33** ham görsel performans uyarısıyla tamamlandı. Gerçek Chrome motorunda 390 px dokunmatik emülasyonda beş manşetli geçici veriyle sola kaydırma `01 → 02`, sağa kaydırma `02 → 01` çalıştı; kısa dokunuş ve dikey hareket manşeti değiştirmedi, dikey sayfa kaydırması korundu, yanlış haber navigasyonu ve yatay taşma oluşmadı.
- Kalan karar veya risk: Browser becerisi çalışma alanındaki sembolik klasör kısıtı nedeniyle açılamadı; gerçek cihaz davranışı yerel Chrome 151 dokunmatik emülasyonuyla doğrulandı. Değişiklikler kullanıcı isteğiyle commit ve Hetzner staging dağıtımı kapsamına alındı.

### 2026-08-26 — Mobil manşet kaydırmasının staging dağıtımı

- İstek: Mobil manşette parmakla sağa/sola kaydırma desteğinin commit edilip Hetzner staging ortamına dağıtılması.
- Yapılanlar: `b98c88a` uygulama commit'i fast-forward olarak `main` branch'ine push edildi. GitHub Actions üzerindeki **Koza TV Staging** iş akışı test, dosya aktarımı ve atomik Hetzner dağıtımını başarıyla tamamladı.
- Değişen ana dosyalar: Dağıtılan uygulama sürümü `b98c88a`; dağıtım kaydı için `AGENTS.md`.
- Doğrulama: Push öncesinde `npm test` production build ile **58 test geçti; 0 başarısız, 0 atlandı**; `npx tsc --noEmit`, `npm run lint` ve bağımsız `npm run build` başarılı oldu. GitHub Actions çalışması `32973736863` başarıyla tamamlandı. Dış kontrolde `/`, `/kategori/gundem`, `/son-dakika`, `/canli`, `/yazarlar` ve `/admin/giris` HTTP 200; `/admin` HTTP 307 ve `/api/auth/me` HTTP 401 döndürdü. Yayın CSS'inde `touch-action:pan-y`, istemci paketinde Pointer Events kodu doğrulandı. Staging'in gerçek beş manşetli verisinde 390 px Chrome dokunmatik emülasyonuyla sola kaydırma `01 → 02`, sağa kaydırma `02 → 01` çalıştı; yanlış navigasyon ve yatay taşma oluşmadı.
- Kalan karar veya risk: Staging IP üzerinden HTTP çalışmaktadır; canlı alan adı ve DNS geçişi bu dağıtımın kapsamında değildir. Mobil kaydırma düğmelere ek bir kullanım yoludur; mevcut sağ/sol ve erişilebilir klavye kontrolleri korunmuştur.

### 2026-08-29 — Yetkili ajans akış merkezi ve kaynağa özel bilgilendirme

- İstek: Eski Koza TV haber detayında görülen AA, İHA, DHA ve diğer ajanslara ilişkin genel bilgilendirmenin yeni sitede yönetilip yönetilemediğinin incelenmesi; yoksa ajans haberlerini metin, görsel ve diğer alanlarıyla otomatik alabilecek gerçek bir yönetim ve aktarım yapısının kurulması.
- Yapılanlar: Mevcut **Kaynak Merkezi**nin yalnız ad/URL kaydı tuttuğu, hiçbir veri çekme veya zamanlama işlemi yapmadığı doğrulandı ve ekran gerçek **Ajans Akış Merkezi**ne dönüştürüldü. AA, İHA, DHA ve diğer sözleşmeli sağlayıcılar için HTTPS uç noktası, RSS/Atom, NewsML-G2 veya JSON biçimi, Bearer/Basic/IP izinli erişim, yalnız ortam değişkeni adıyla gizli anahtar yönetimi, 5–1440 dakikalık periyot, kategori eşlemesi, editör kuyruğu veya açıkça seçilen doğrudan yayın, aktif/pasif durum, bağlantı testi, elle çekme, son başarı/hata ve kayıt sayaçları eklendi. Ajans verisi ortak modele dönüştürülürken tam ham kayıt, ajans kimliği, başlık, spot, gövde, kategori, imza, tarih, güncelleme, konum, öncelik, hak bilgisi, etiket, fotoğraf ve video alanları korunuyor. `(source_id, external_id)` ile mükerrer kayıt engelleniyor; değişiklikler revizyona, geri çekmeler yayından kaldırmaya ve bütün işlemler denetim kaydına yazılıyor. Görseller dosya imzasıyla doğrulanıp yerel medya kütüphanesine alınıyor; ilk fotoğraf kapak, diğer medya içerik bloğu oluyor. Feed ve medya yanıtlarında boyut sınırı, HTTPS, DNS/IP özel ağ engeli, sınırlı yönlendirme ve kimlik bilgisini başka origin'e taşımama kontrolleri uygulandı. Yalnız ajans bağlantılı haberlerde kaynak adı, ajans kayıt numarası, alınma zamanı ve panelden ajans bazında düzenlenen bilgilendirme gösteriliyor; Koza TV'nin kendi haberlerine blanket metin eklenmiyor. Beş dakikalık systemd zamanlayıcısı ve gizli `/etc/kozatv/agency.env` operasyonu hazırlandı.
- Değişen ana dosyalar: `db/agency-model.mjs`, `db/index.ts`, `app/api/agency/route.ts`, `app/api/sources/route.ts`, `app/admin/agency-sources.tsx`, `app/admin/panel.tsx`, `app/haber/[slug]/page.tsx`, `app/globals.css`, `deployment/hetzner/kozatv.service`, `deployment/hetzner/kozatv-agency-pull.service`, `deployment/hetzner/kozatv-agency-pull.timer`, `deployment/hetzner/README.md`, `tests/rendered-html.test.mjs`, `AGENTS.md`.
- Doğrulama: Canlı eski haber sayfasındaki genel ajans metni doğrulandı; ancak sayfanın bu özel haber için ajans kimliği göstermediği tespit edildi. AA'nın resmî Haber Akış Sistemi/API/XML/NewsML ve abonelik sayfaları, İHA'nın resmî XML/RSS otomatik entegrasyon/abonelik açıklaması ve DHA'nın resmî abonelik-bülten dağıtım sayfası incelendi. `npm test` production build ile **61 test geçti; 0 başarısız, 0 atlandı**. Yeni senaryolar güvenli kaynak doğrulamasını, gizli değerin API/veritabanına sızmamasını, RSS/Atom, NewsML-G2 ve JSON tam alan eşlemesini, kategori eşlemesini, kaynak oluşturma/güncelleme yetkisini, oturumsuz çekim engelini, kaynağa özel haber bilgilendirmesini ve otomatik systemd sözleşmesini kapsıyor. `npx tsc --noEmit` başarılı; `npm run lint` **0 hata**, mevcut ham `<img>` yüzeyleri için **33 uyarı** ile tamamlandı. Gerçek tarayıcı motorunda 1280 px masaüstü, 900 px tablet ve 390 px mobil Ajans Akış Merkezi kontrol edildi; üç görünümde yatay taşma olmadı, form ve kontroller kırılımlara uygun yerleşti.
- Kalan karar veya risk: Hiçbir AA/İHA/DHA canlı akışı henüz bağlanmadı; gerçek otomasyon için ilgili ajansla kurumsal abonelik, ajansın verdiği endpoint/NewsML veya API örneği, erişim anahtarı ve varsa IP izin listesi gerekir. Her sağlayıcının gerçek örnek bülteni **Bağlantıyı test et** sonucuyla karşılaştırılmadan doğrudan yayın açılmamalıdır. Paneldeki bilgilendirme metni kaynak bazında yönetilir fakat sorumluluğu kendiliğinden ajansa devreden bir hukuki garanti değildir; canlıya alınmadan önce yayın kuruluşunun hukuk danışmanı tarafından kesinleştirilmelidir. Systemd dosyaları repoda hazırdır; canlı sunucuda timer kurulumu ve `/etc/kozatv/agency.env` tanımı dağıtım sırasında yapılmalıdır. Değişiklikler commit edilmedi ve staging'e dağıtılmadı.

### 2026-08-29 — Otomatik TCMB kur yenilemesi

- İstek: Üst bilgi bandındaki Dolar, Euro ve Sterlin değerlerinin statik kalmaması; gerçek bir veri kaynağından otomatik güncellenmesi.
- Yapılanlar: Kur değerlerinin zaten resmî TCMB `today.xml` bülteninden sunucu tarafında okunduğu doğrulandı; kodda sabit kur değeri bulunmadı. Eksik olan açık sayfa yenilemesi tamamlandı: tarayıcı `/api/piyasa` verisini beş dakikada bir önbelleksiz yeniden istiyor, sekme yeniden görünür olduğunda ve bağlantı geri geldiğinde anında kontrol ediyor. Sunucu TCMB ve hava durumu kaynaklarını 15 dakikalık kontrollü önbellekle okuyor. Kaynak alanına yeşil durum noktası, **Otomatik** ibaresi, son bülten tarihi ve araç ipucunda son kontrol saati eklendi; dinamik değişiklikler ekran okuyuculara nazikçe duyuruluyor.
- Değişen ana dosyalar: `app/site-client.tsx`, `app/api/piyasa/route.ts`, `app/globals.css`, `app/admin/site-settings.tsx`, `tests/rendered-html.test.mjs`, `AGENTS.md`.
- Doğrulama: `npm test` production build ile **58 test geçti; 0 başarısız, 0 atlandı**. Yeni regresyon kontrolleri beş dakikalık tarayıcı yenilemesini, önbelleksiz isteği, sekmeye dönüş/bağlantı geri gelişi tetiklerini, resmî TCMB veri adresini, 15 dakikalık sunucu önbelleğini, kaynak ibaresini ve erişilebilir canlı duyuruyu doğruladı. `npx tsc --noEmit` başarılı; `npm run lint` **0 hata**, mevcut **33** ham görsel performans uyarısıyla tamamlandı. Yerel `/api/piyasa` gerçek TCMB yanıtıyla USD 48,16, EUR 56,09, GBP 65,55 ve bülten tarihi 28.08.2026 döndürdü. Gerçek uygulama tarayıcısında 1440, 900 ve 390 px görünümler kontrol edildi; yatay taşma ve konsol hatası yoktu, masaüstünde üç kur ile **TCMB · 28.08.2026 · Otomatik** birlikte göründü.
- Kalan karar veya risk: TCMB verisi gerçek zamanlı serbest piyasa fiyatı değil, her iş günü yayımlanan resmî gösterge satış kurudur; hafta sonu ve resmî tatillerde son iş gününün tarihi görünür. Canlı/anlık piyasa kuru istenirse ayrıca lisanslı bir gerçek zamanlı veri sağlayıcısı seçilmelidir. Değişiklikler henüz commit edilmedi ve staging'e dağıtılmadı.

### 2026-08-29 — BIST, altın ve yön göstergeli piyasa bandı

- İstek: Eski Koza TV ana sayfasındaki gibi BIST, altın, dolar ve euro değerlerinin değişim yüzdesiyle; yükselişte yeşil yukarı, düşüşte kırmızı aşağı işaretiyle otomatik gösterilmesi.
- Yapılanlar: Eski `kozatv.com.tr` üst bandındaki dört değerin Mynet Finans genel piyasa akışıyla birebir eşleştiği doğrulandı. Yeni sunucu tarafı ayrıştırıcı BIST 100, gram altın, dolar ve euro değerlerini, değişim yüzdelerini, yönlerini ve veri zamanlarını sıkı biçimde doğrulayıp Türkçe biçimlendiriyor. Birincil akış erişilemezse yalnız resmî TCMB dolar/euro değerleri nötr ve yüzdesiz güvenli yedek olarak gösteriliyor; BIST veya altın değeri uydurulmuyor. Üst bant yeşil/kırmızı yön okları, yüzde değişimi, kaynak ve otomatik güncelleme durumu ile yenilendi. Masaüstü ve tablette dört veri aynı satırda; mobilde hiçbir kalem gizlenmeden bandın kendi içinde yatay kaydırılabiliyor. Her veri için yön, değer ve kaynak zamanı erişilebilir etikette açıklanıyor.
- Değişen ana dosyalar: `app/api/piyasa/market-model.mjs`, `app/api/piyasa/route.ts`, `app/site-client.tsx`, `app/globals.css`, `app/admin/site-settings.tsx`, `tests/rendered-html.test.mjs`, `AGENTS.md`.
- Doğrulama: `npm test` production build ile **59 test geçti; 0 başarısız, 0 atlandı**. Yeni testler dört piyasa kaleminin Türkçe sayı/yüzde biçimini, yükseliş-düşüş-nötr yönlerini, eksik veya bozuk kaynak verisinin reddini, birincil Mynet adresini, TCMB yedeğini, beş dakikalık önbellek/yenileme sözleşmesini ve erişilebilir yön metinlerini doğruladı. `npx tsc --noEmit` başarılı; `npm run lint` **0 hata**, mevcut **33** ham görsel performans uyarısıyla tamamlandı; bağımsız `npm run build` başarılı oldu. Yerel `/api/piyasa` isteği BIST 14.642 (%0,45), altın 6.909 (%-3,01), dolar 48,24 (%0,18) ve euro 55,92 (%-0,49) döndürdü. Gerçek uygulama tarayıcısında 1440, 900 ve 390 px kontrol edildi: dört kalem görünür, renk ve yönler doğru, sayfada yatay taşma ve konsol hatası yok; mobil piyasa şeridi `375/684 px` kendi içinde kaydırılabilir ölçüldü.
- Kalan karar veya risk: Mynet Finans üçüncü taraf ve gecikmeli bir piyasa kaynağıdır; okura her kalemin kaynak zamanı gösterilir. Lisanslı gerçek zamanlı Borsa İstanbul yayını istenirse sözleşmeli bir veri sağlayıcıya geçilmelidir. Değişiklikler henüz commit edilmedi ve staging'e dağıtılmadı.

### 2026-08-29 — Yön göstergeli piyasa bandının staging dağıtımı

- İstek: BIST 100, altın, dolar ve euro değişimlerini gösteren yeni piyasa bandının commit edilip Hetzner staging ortamına dağıtılması.
- Yapılanlar: `e6ec993` uygulama commit'i fast-forward olarak `main` branch'ine push edildi. GitHub Actions üzerindeki **Koza TV Staging** iş akışı test, dosya aktarımı ve atomik Hetzner dağıtımını başarıyla tamamladı.
- Değişen ana dosyalar: Dağıtılan uygulama sürümü `e6ec993`; dağıtım kaydı için `AGENTS.md`.
- Doğrulama: Push öncesinde `npm test` production build ile **59 test geçti; 0 başarısız, 0 atlandı**. GitHub Actions çalışması `33265340143` başarıyla tamamlandı. Dış kontrolde `/`, `/kategori/gundem`, `/son-dakika`, `/canli`, `/yazarlar` ve `/admin/giris` HTTP 200; `/admin` HTTP 307 ve `/api/auth/me` HTTP 401 döndürdü. Staging `/api/piyasa` yanıtında Mynet Finans kaynaklı BIST 100, altın, dolar ve euro; değişim yüzdeleri, `up/down` yönleri ve kaynak zamanları doğrulandı. Gerçek uygulama tarayıcısında 1440 px görünümde dört kalem doğru yeşil/kırmızı renklerle gösterildi; 390 px mobilde dört kalem korunup piyasa şeridi kendi içinde kaydırıldı, sayfada yatay taşma ve konsol hatası oluşmadı.
- Kalan karar veya risk: Staging IP üzerinden HTTP çalışmaktadır; canlı alan adı ve DNS geçişi bu dağıtımın kapsamında değildir. Mynet Finans gecikmeli üçüncü taraf kaynaktır; lisanslı gerçek zamanlı Borsa İstanbul verisi ayrıca sağlayıcı sözleşmesi gerektirir.

### 2026-08-29 — Piyasa bandından kaynak markasının kaldırılması

- İstek: Üst piyasa bandında görünen **Mynet Finans** kaynak kutusunun tamamen kaldırılması.
- Yapılanlar: Ziyaretçi üst bandındaki kaynak adı, tarih ve **Otomatik** metnini içeren ayrı kutu ile bu kutuya ait bütün CSS kuralları kaldırıldı. BIST 100, altın, dolar ve euro değerleri; yüzdeler, yeşil/kırmızı yön okları, beş dakikalık otomatik yenileme ve her kalemin erişilebilir veri zamanı korunuyor. Kaynak kutusunun ziyaretçi bileşenine yeniden eklenmesini engelleyen regresyon kontrolü eklendi.
- Değişen ana dosyalar: `app/site-client.tsx`, `app/globals.css`, `tests/rendered-html.test.mjs`, `AGENTS.md`.
- Doğrulama: `npm test` production build ile **59 test geçti; 0 başarısız, 0 atlandı**. `npx tsc --noEmit` başarılı; `npm run lint` **0 hata**, mevcut **33** ham görsel performans uyarısıyla tamamlandı. Gerçek uygulama tarayıcısında 1440, 900 ve 390 px görünümler kontrol edildi: görünür **Mynet Finans** metni ve kaynak kutusu sayısı sıfır, dört piyasa kalemi görünür, sayfada yatay taşma ve konsol hatası yok; mobil bant kendi içinde kaydırılabilir durumda.
- Kalan karar veya risk: Değişiklikler henüz commit edilmedi ve staging'e dağıtılmadı. Piyasa verisinin sunucu tarafındaki sağlayıcısı değişmedi; yalnız üçüncü taraf marka sunumu ziyaretçi arayüzünden kaldırıldı.

### 2026-08-29 — Kaynak markası kaldırılmış piyasa bandının staging dağıtımı

- İstek: Üst banttan kaldırılan **Mynet Finans** kaynak kutusunun commit edilip Hetzner staging ortamına dağıtılması.
- Yapılanlar: `573160c` uygulama commit'i fast-forward olarak `main` branch'ine push edildi. GitHub Actions üzerindeki **Koza TV Staging** iş akışı test, dosya aktarımı ve atomik Hetzner dağıtımını başarıyla tamamladı.
- Değişen ana dosyalar: Dağıtılan uygulama sürümü `573160c`; dağıtım kaydı için `AGENTS.md`.
- Doğrulama: Push öncesinde `npm test` production build ile **59 test geçti; 0 başarısız, 0 atlandı**. GitHub Actions çalışması `33265762508` başarıyla tamamlandı. Dış kontrolde `/`, `/kategori/gundem`, `/son-dakika`, `/canli`, `/yazarlar` ve `/admin/giris` HTTP 200; `/admin` HTTP 307 ve `/api/auth/me` HTTP 401 döndürdü. Staging ana sayfasının gerçek tarayıcı kontrolünde 1440 ve 390 px görünümlerde görünür **Mynet Finans** metni ve `.live-data-source` kutusu sayısı sıfır; BIST 100, altın, dolar ve euro kartlarının dördü de görünür, sayfada yatay taşma ve konsol hatası yok.
- Kalan karar veya risk: Staging IP üzerinden HTTP çalışmaktadır; canlı alan adı ve DNS geçişi bu dağıtımın kapsamında değildir. Piyasa verisinin sunucu tarafındaki sağlayıcısı değişmedi; üçüncü taraf marka yalnız ziyaretçi arayüzünden kaldırıldı.

### 2026-08-29 — Opsiyonel ajans kimliği ve rol bazlı haber yetkileri

- İstek: Ajanslardan alınan haberlerin tek tek düzenlenebilmesi; ajans kimliği, kaynak kaydı ve fotoğraf kredisinin değiştirilebilir ve zorunlu olmayan alanlara dönüştürülmesi; bu işlemleri yapabilecek kullanıcı rollerinin netleştirilmesi.
- Yapılanlar: Haber editörüne isteğe bağlı ajans bağlantısı, ajans kayıt numarası, fotoğraf/telif kredisi ve kaynak alanları eklendi. Yönetici, yayın yönetmeni ve editör bu bilgileri değiştirebilir, boşaltabilir veya haberi ajanstan tamamen ayırabilir; boş alanlar ziyaretçi haber detayında gösterilmez. Ajans bazındaki bilgilendirme metni de boş bırakılabilir hale getirildi. Editörün ajans haberini düzenlemesi otomatik kaynak güncellemesine karşı koruma oluşturuyor; yeni ajans sürümü geldiğinde içerik ezilmek yerine bekleyen güncelleme uyarısı veriliyor. Yönetici ve yayın yönetmeni bütün haberleri düzenleyip yayınlayabilir; editör taslak/inceleme haberlerini ve ajans alanlarını düzenleyebilir fakat doğrudan yayınlayamaz; muhabir yalnız kendisine atanmış taslakları düzenleyebilir ve ajans alanlarını değiştiremez; görüntüleyici salt okunurdur. Atama, durum geçişi ve API kontrolleri aynı rol kurallarıyla sunucu tarafında zorunlu hale getirildi.
- Değişen ana dosyalar: `db/editorial-permissions.mjs`, `db/agency-model.mjs`, `db/index.ts`, `app/api/articles/route.ts`, `app/api/editorial/route.ts`, `app/api/agency/route.ts`, `app/admin/panel.tsx`, `app/admin/workflow-studio.tsx`, `app/admin/agency-sources.tsx`, `app/haber/[slug]/page.tsx`, `app/globals.css`, `tests/rendered-html.test.mjs`, `AGENTS.md`.
- Doğrulama: `npm test` production build ile **64 test geçti; 0 başarısız, 0 atlandı**. Yeni regresyon senaryoları boş ajans bilgilendirmesini, ajans bağlantısını/kimliğini/krediyi kaldırmayı, haber detayında boş alanların gizlenmesini, korunan ajans güncellemesinin beklemeye alınmasını ve yönetici-yayın yönetmeni-editör-muhabir-görüntüleyici ayrımını kapsıyor. `npx tsc --noEmit` başarılı; `npm run lint` **0 hata**, mevcut ham `<img>` yüzeyleri için **33 uyarı** ile tamamlandı; bağımsız `npm run build` başarılı oldu. Gerçek uygulama tarayıcısında 1280 px masaüstü, 900 px tablet ve 390 px mobil haber editörü kontrol edildi; üç görünümde yatay taşma olmadı. Gerçek editör oturumunda Ajans Akış Merkezi gizli, yalnız **Editör incelemesine gönder** ve **Taslak kaydet** işlemleri görünür, isteğe bağlı ajans alanları düzenlenebilir olarak doğrulandı.
- Kalan karar veya risk: AA, İHA veya DHA'nın gerçek canlı akışı henüz bağlı değildir; üretim otomasyonu için sözleşmeli endpoint ve erişim bilgileri gerekir. Editör korumasında bekleyen ajans güncellemesi mevcut arayüzde koruma kaldırılıp yeniden çekilerek uygulanır; yan yana sürüm karşılaştırması sonraki editoryal iyileştirme olarak ele alınabilir. Ajans bilgilendirme metninin boş bırakılabilmesi teknik olarak mümkündür; hukuki metnin kullanımı yayın kuruluşunun hukuk danışmanıyla belirlenmelidir.

### 2026-08-29 — Ajans akışı ve rol güvenliğinin staging dağıtımı

- İstek: Yetkili ajans akış merkezi, isteğe bağlı haber bazlı ajans alanları ve rol sınırlarının commit edilip staging ortamına dağıtılması.
- Yapılanlar: `a5371fa` uygulama commit'i fast-forward olarak `main` branch'ine push edildi. GitHub Actions üzerindeki **Koza TV Staging** iş akışı temiz kurulum, production bağımlılık denetimi, 64 test, lint, sunucu tarafı yeniden test, atomik sürüm değişimi ve dış smoke test adımlarını başarıyla tamamladı.
- Değişen ana dosyalar: Dağıtılan uygulama sürümü `a5371fa`; dağıtım kaydı için `AGENTS.md`.
- Doğrulama: GitHub Actions çalışması `33269202887` başarılı tamamlandı. Dış kontrolde `/`, `/kategori/gundem`, `/son-dakika`, `/canli`, `/yazarlar` ve `/admin/giris` HTTP 200; `/admin` HTTP 307 ve `/api/auth/me` HTTP 401 döndürdü. Oturumsuz ajans çekme isteği HTTP 401 ile reddedildi. Push öncesinde `npm test` production build ile **64 test geçti; 0 başarısız, 0 atlandı**; `npx tsc --noEmit`, `npm run lint` ve bağımsız `npm run build` başarılı oldu.
- Kalan karar veya risk: Staging IP üzerinden HTTP çalışmaktadır; canlı alan adı/DNS geçişi bu dağıtım kapsamında değildir. Zamanlayıcı ve servis dosyaları sürümle sunucuya ulaştı ancak gerçek ajans abonelik bilgileri ve `KOZA_AGENCY_CRON_TOKEN` tanımlanmadan etkinleştirilmemelidir. AA/İHA/DHA akışlarından hiçbiri henüz bağlı değildir.

### 2026-08-31 — Geniş masaüstü yan reklamları ve görünür Koza TV dummy kreatifi

- İstek: Sözcü örneğindeki gibi ana içeriğin iki yanında da açıkça görünen reklam alanları oluşturulması; dummy kreatiflerin içinde doğrudan **KOZA TV REKLAM** yazması; reklam yoğunluğunun artırılması ve mevcut Reklam Merkezi yönetiminin bu alanları kapsaması.
- Yapılanlar: Site geneline ayrı ayrı yönetilen **Masaüstü sol duvar** ve **Masaüstü sağ duvar** konumları eklendi; reklam envanteri dörtten altıya çıktı. İki Koza TV kurum içi kampanyası ve içinde büyük **KOZA TV · REKLAM ALANI · MARKANIZ BURADA** yazan kurumsal SVG kreatifi hazırlandı. Yan duvarlar 1360–1799 px masaüstünde 120 px ince sürüm, 1800 px ve üzerinde tam 300×600 sürüm olarak üst reklamla aynı hizada görünür; tablet ve mobilde tamamen gizlenir. Tüm Koza TV dummy reklamlarının kreatif içine ayrıca **KOZA TV REKLAM** damgası eklendi. Mevcut dört alanlı SQLite tablosunu kayıt kimliklerini, öncelikleri ve tarihleri koruyarak altı alana taşıyan, ikinci çalıştırmada veri kopyalamayan transaction migration'ı kuruldu. Reklam Merkezi altı alanı ve iki yan duvarı gösterir hale getirildi. Tablet QA sırasında bulunan üst bağlantı/menü taşması kapatıldı; menü 1100 px altında sayfayı büyütmek yerine kendi içinde kayıyor. Sekiz sayfalık yönetim kılavuzu yeni alanlar ve güncel ekran görüntüleriyle Sürüm 1.1'e yükseltildi.
- Değişen ana dosyalar: `db/ad-model.mjs`, `db/ad-schema.mjs`, `db/index.ts`, `app/ad-slot.tsx`, `app/site-chrome.tsx`, `app/admin/advertising-center.tsx`, `app/globals.css`, `public/ads/koza-tv-reklam-dikey.svg`, `tests/rendered-html.test.mjs`, `docs/yonetim/Koza-TV-Reklam-Merkezi-Yonetim-Kilavuzu.docx`, `AGENTS.md`.
- Doğrulama: `npm test` production build ile **70 test geçti; 0 başarısız, 0 atlandı**. Yeni regresyon senaryosu eski dört alanlı tabloyu veri kaybı olmadan altı alana taşıdı, iki yeni konumu kabul etti, bilinmeyen konumu reddetti ve ikinci migration çalışmasında kayıt çoğaltmadı. `npx tsc --noEmit` başarılı; bağımsız `npm run build` başarılı; `npm run lint` **0 hata**, mevcut ham `<img>` yüzeyleri için **35 performans uyarısı** ile tamamlandı. Gerçek tarayıcıda 1905 ve 1800 px'te iki yan duvar 300 px; 1799, 1440 ve 1360 px'te kırpılmayan, metni 17 px okunur 120 px dikey kreatif; 1359, 1100, 1024, 768 ve 390 px'te gizli ve bütün ölçümlerde `scrollWidth == clientWidth`. Mobilde gizli iki yan kreatifin tembel yükleme sayesinde indirilmediği de doğrulandı. Ana sayfa, Gündem, Son Dakika ve haber detayında iki yan duvar, iki **REKLAM** etiketi ve iki **KOZA TV REKLAM** damgası doğrulandı; kırık görsel sayısı sıfır. Yerel HTTP kontrolde ana rota, kategori, Son Dakika, haber ve SVG kreatif 200; `/admin` 307 ile girişe, oturumsuz `/api/ads` 401'e döndü. Word kılavuzu sekiz sayfa olarak render edilip bütün sayfaları görsel incelendi; erişilebilirlik denetimi **0 yüksek, 0 orta, 0 düşük bulgu** verdi.
- Kalan karar veya risk: Yan duvar kampanyaları içerik sıkışmasını önlemek için 1360 px altındaki dizüstü/tablet/mobil ekranlarda gösterilmez; orta masaüstünde ince, geniş masaüstünde tam boy çalışır. Bunlar şimdilik Koza TV dummy kampanyalarıdır; yönetici veya yayın yönetmeni Reklam Merkezi'nden gerçek reklam görseli, bağlantısı ve yayın takvimiyle değiştirebilir. Gerçek reklam ağı/script entegrasyonu yapılmadı. Değişiklikler henüz commit edilmedi ve staging ortamına dağıtılmadı.

### 2026-08-31 — Panelden yönetilen reklam envanteri ve Koza TV tanıtımları

- İstek: Patronların sitedeki reklam alanlarını gerçek sayfa bağlamında görebilmesi; alanların açıkça reklam olarak belirtilmesi; şimdilik Koza TV kurum içi tanıtımlarının dummy kreatif olarak kullanılması; profesyonel haber sitelerindeki reklam yönetiminin incelenip yönetim paneline taşınması.
- Yapılanlar: Reklam konumu ile kampanya/kreatif birbirinden ayrıldı. Kodda sabit kalan dört envanter konumu (`site_top`, `home_billboard`, `section_inline`, `article_sidebar`) ve yönetim panelinden değiştirilen reklamveren, kampanya adı, başlık, açıklama, görsel, hedef, buton, tema, yayın türü, öncelik, başlangıç/bitiş ve aktiflik alanları oluşturuldu. İlk kurulum her eksik konuma bir Koza TV kurum içi tanıtımı ekliyor, sonraki yönetici tercihlerini ezmiyor. Bütün kreatiflerde kod tarafından üretilen ve panelden kaldırılamayan **REKLAM** etiketi bulunuyor; ücretli/dış hedefler `rel="sponsored nofollow"`, kurum içi Koza TV bağlantıları normal iç bağlantı olarak işaretleniyor. Ana sayfa, kategori, son dakika ve haber detay sayfaları duyarlı reklam bileşenine geçirildi; bölüm içi reklam ilk altı haberden sonra gösteriliyor, eski boş `300 × 250` kutusu kaldırıldı. Görselsiz veya kırık görselli kreatifler ölçüyü bozmayan kontrollü metin yedeğine düşüyor; ziyaretçiye teknik konum/ölçü kodları gösterilmiyor.
- Yapılanlar (admin ve güvenlik): **Reklam Merkezi** dört numaralı envanter kartı, gerçek “yayında/yedekte/planlandı/durduruldu” ayrımı, canlı ön izleme, medya kütüphanesinden seçim, doğrulanmış görsel yükleme, tek tıkla durdurma/yeniden başlatma ve Türkiye saatli yayın planıyla sadeleştirildi. Form üç kısa adıma ayrıldı; gelişmiş alanlar isteğe bağlı bölümde tutuldu, henüz bağlı olmayan programatik seçenek kullanılamaz hale getirildi. Alan bazlı hata ve klavye odağı, yükleniyor/başarı/ağ hatası durumları, kaydedilmemiş değişiklik uyarısı ve mobilde seçilen kampanyaya otomatik kaydırma eklendi. Yalnız yönetici ve yayın yönetmeni envanteri görebiliyor ve değiştirebiliyor. API'de POST oluşturma/PATCH güncelleme ayrıldı; bulunamayan kayıt 404, eski sürümle eşzamanlı güncelleme 409 dönüyor ve başarısız çakışma denetim kaydı üretmiyor. Denetim kaydı önce/sonra verisini ve işlemi yapanın kimlik/rolünü tutuyor. Güvenli bağlantı kontrolü ters eğik çizgi, origin kaçışı, kimlik bilgili URL, HTTP dış hedef, `javascript:`/`file:` ve kontrol karakterlerini reddediyor; buton metni hedef adres olmadan kabul edilmiyor. Tarih girişleri sunucudan bağımsız olarak Europe/Istanbul saatine çevriliyor, başlangıç/bitiş ve öncelik sınırları hem model hem SQLite kısıtlarıyla korunuyor.
- Değişen ana dosyalar: `db/ad-model.mjs`, `db/index.ts`, `app/api/ads/route.ts`, `app/ad-slot.tsx`, `app/ad-image.tsx`, `app/admin/ad-form-model.mjs`, `app/admin/advertising-center.tsx`, `app/admin/panel.tsx`, `app/site-chrome.tsx`, `app/page.tsx`, `app/kategori/[slug]/page.tsx`, `app/son-dakika/page.tsx`, `app/haber/[slug]/page.tsx`, `app/globals.css`, `tests/rendered-html.test.mjs`, `AGENTS.md`.
- Doğrulama: `npm test` production build ile **68 test geçti; 0 başarısız, 0 atlandı**. Regresyon paketi dört sabit konumu, eksik seed tamamlama, tek yayındaki kampanya seçimi, programlı/aktif/durdurulmuş/süresi dolmuş durumları, öncelik, tarih sınırı, güvenli/güvensiz URL'ler, zorunlu alan ve uzunluklar, görselsiz/kırık görsel, iç/dış bağlantı işaretleri, medya yükleme, optimistic concurrency, denetim kaydı, POST/PATCH/400/401/403/404/409 sözleşmeleri, yönetici-yayın yönetmeni-editör-muhabir-görüntüleyici ayrımı ve dört ziyaretçi yüzeyini kapsıyor. `npx tsc --noEmit` ve bağımsız `npm run build` başarılı; `npm run lint` **0 hata**, mevcut ham `<img>` yüzeyleri için **35 performans uyarısıyla** tamamlandı. Gerçek uygulama tarayıcısında 1280 px masaüstü, 900 px tablet ve 390/360 px mobil Reklam Merkezi ile ziyaretçi reklamları kontrol edildi; tüm görünümlerde sayfa yatay taşmadı. Mobil form 351 px içinde, giriş metinleri 14 px ve ana işlem 44 px ölçüldü. Boş alan, hedefsiz buton, görsel yükleme/ön izleme, kırık görsel yedeği, kaydedilmemiş formdan çıkış uyarısı ve sunucu kesintisi ayrı ayrı denendi; kesintide açıklayıcı hata gösterildi, form kaybolmadı ve işlem düğmesi yeniden etkinleşti. Rota kontrolünde `/`, `/kategori/gundem`, `/son-dakika`, `/admin/giris` HTTP 200; oturumsuz `/admin` HTTP 307 ve `/api/ads` HTTP 401 döndürdü.
- Kalan karar veya risk: Şimdilik yalnız Koza TV kurum içi tanıtımları vardır; ticari satış başladığında sözleşme/fatura/vergi akışı, gösterim-tıklama raporlaması, kampanya rezervasyonu ve gerekiyorsa Google Ad Manager entegrasyonu ayrıca kurulmalıdır. Programatik veya kişiselleştirilmiş reklamdan önce KVKK/çerez rızası ve hukuk değerlendirmesi gerekir. Pop-up, otomatik ses, tam ekran engelleme ve yüksek mobil reklam yoğunluğu bilinçli olarak desteklenmiyor. Commit ve staging sonucu aşağıdaki yayın kaydında yer alır.

### 2026-08-31 — Reklam Merkezi yönetim ekibi kullanım kılavuzu

- İstek: Yönetim ekibinin Reklam Merkezi'ni teknik bilgi gerektirmeden kullanabilmesi için basit, vurucu ve gerçek ekran görüntüleriyle desteklenen bir kullanıcı dokümanı hazırlanması.
- Yapılanlar: Koza TV kurumsal diliyle sekiz sayfalık Word kılavuzu oluşturuldu. Kılavuz; yetkili roller, beş adımlı güvenli yayın akışı, dört sabit reklam alanı, yeni reklam oluşturma, görsel ve bağlantı kuralları, Türkiye saatine göre yayın planlama, Yayında/Yedekte/Planlandı/Durduruldu/Süresi Doldu durumları, durdurma-başlatma işlemleri, masaüstü ve mobil canlı sayfa kontrolü, hata yardımı ve yayın öncesi kontrol listesini kapsıyor. Görseller ayrı test veritabanı ve yerel yönetici hesabıyla çalışan gerçek uygulamadan alındı; staging veya canlı veri değiştirilmedi. Belgedeki logo ve beş farklı gerçek ekran görüntüsü erişilebilir alternatif açıklamalarla işlendi; ana sayfa görünümü iki bölümde bağlama göre tekrar kullanıldı.
- Değişen ana dosyalar: `docs/yonetim/Koza-TV-Reklam-Merkezi-Yonetim-Kilavuzu.docx`, `tests/rendered-html.test.mjs`, `AGENTS.md`.
- Doğrulama: Word dosyası bozulmaya karşı ZIP/OOXML testiyle açıldı; gerçek panel ifadeleri, dört alan adı, Türkiye saati, canlı kontrol adımı, en az altı gömülü görsel ve tüm görsel açıklamaları regresyon testiyle doğrulandı. Üretim derlemesiyle birlikte `npm test` **69 test geçti; 0 başarısız, 0 atlandı**. `npx tsc --noEmit` başarılı; `npm run lint` **0 hata**, mevcut ham `<img>` yüzeyleri için **35 performans uyarısıyla** tamamlandı; bağımsız `npm run build` başarılı oldu. Belge LibreOffice motoruyla PDF/PNG'ye çevrilip sekiz sayfanın tamamı tek tek görsel kontrolden geçirildi; boş sayfa, taşma ve kırpılma bulunmadı. Otomatik erişilebilirlik denetimi **0 yüksek, 0 orta, 0 düşük** bulgu verdi; görsel denetimi yedi erişilebilir satır içi görsel doğruladı.
- Kalan karar veya risk: Kılavuz mevcut Reklam Merkezi sürümünü anlatır; alan adları, butonlar veya yetki modeli değişirse aynı sürümde güncellenmelidir. Commit ve staging sonucu aşağıdaki yayın kaydında yer alır.

### 2026-08-31 — Kılavuz testinin dağıtım ortamından bağımsızlaştırılması

- İstek: Reklam Merkezi ve yönetim kılavuzunun bütün doğrulama kapıları korunarak staging ortamına yayınlanması.
- Yapılanlar: İlk staging çalışmasında GitHub tarafındaki temiz kurulum, production bağımlılık denetimi, build, 69 test ve lint başarılı oldu; sunucudaki atomik dağıtımın yeniden çalıştırdığı test paketi ise yalnız işletim sisteminde `unzip` komutu bulunmadığı için kılavuz testinde durdu. Otomatik süreç mevcut çalışan sürümü değiştirmedi. DOCX doğrulaması sistem paketine bağlı olmaktan çıkarılıp projede doğrudan tanımlanan `fflate` ile Word arşivini açacak, `document.xml` içeriğini ve gömülü medya envanterini aynı kapsamda kontrol edecek biçime getirildi.
- Değişen ana dosyalar: `package.json`, `package-lock.json`, `tests/rendered-html.test.mjs`, `AGENTS.md`.
- Doğrulama: Düzeltmeden sonra `npm test` production build ile **69 test geçti; 0 başarısız, 0 atlandı**. `npx tsc --noEmit` ve bağımsız `npm run build` başarılı; `npm run lint` **0 hata**, mevcut ham `<img>` yüzeyleri için **35 performans uyarısıyla** tamamlandı. `npm audit --omit=dev` **0 production açığı** bildirdi.
- Kalan karar veya risk: Düzeltmenin staging sonucu aşağıdaki yayın kaydında yer alır.

### 2026-08-31 — Reklam Merkezi ve yönetim kılavuzunun staging dağıtımı

- İstek: Reklam Merkezi, dört Koza TV kurum içi tanıtımı, ziyaretçi reklam yerleşimleri ve görselli yönetim kılavuzunun tamamının commit edilip yayınlanması.
- Yapılanlar: Yayın öncesinde SQLite ile medya arşivinin yeni günlük yedeği alındı; SHA-256 değerleri, SQLite `quick_check` ve gerçek geri-yükleme doğrulaması başarılı oldu. `c3eec37` uygulama commit'i fast-forward olarak `main` branch'ine gönderildi. İlk GitHub Actions çalışmasında GitHub üzerindeki bütün kapılar geçti, fakat sunucu yeniden testinde eksik `unzip` komutu nedeniyle yalnız DOCX testi durdu ve atomik süreç çalışan sürüme dokunmadı. Platform bağımsız test düzeltmesi `b27e868` commit'iyle gönderildi; ikinci çalışma temiz kurulum, production bağımlılık denetimi, test/build, lint, aktarım, sunucu tarafı yeniden test, atomik sürüm değişimi ve dış smoke test adımlarının tamamını başarıyla bitirdi. Aktif sürüm ve kalıcı veri ayrı ayrı doğrulandıktan sonra yalnız başarısız ilk hazırlık dizini kaldırılarak yaklaşık 575 MB alan geri kazanıldı; çalışan ve önceki geri dönüş sürümleri korundu.
- Değişen ana dosyalar: Dağıtılan uygulama sürümleri `c3eec37` ve düzeltme `b27e868`; yayın kaydı için `AGENTS.md`.
- Doğrulama: GitHub Actions çalışması `33394713204` başarıyla tamamlandı ve sunucunun aktif bağlantısı `b27e8687f442e4705cfbd1939aede6e8726d7376` sürümünü gösterdi. `/`, `/kategori/gundem`, `/son-dakika`, `/canli`, `/yazarlar` ve `/admin/giris` HTTP 200; `/admin` HTTP 307; `/api/auth/me` ve `/api/ads` HTTP 401; `/koza-logo.png` HTTP 200 döndürdü. Staging HTML'inde ana sayfada `site_top` ile `home_billboard`, kategori ve son dakika sayfalarında `section_inline`, güncel haber detayında `article_sidebar` yerleşimleri ve hepsinde görünür **REKLAM** etiketi doğrulandı. Kalıcı veritabanında dört konumun dördünde de aktif Koza TV tanıtımı bulundu. Gerçek Chrome ile 1440 ve 390 px'te ana sayfa, Gündem, Son Dakika ve ilk haber detayı kontrol edildi; kırık görsel, yatay taşma, konsol hatası veya kritik görsel bozulma görülmedi. Yayın düzeltmesi sonrasında `npm test` production build ile **69 test geçti; 0 başarısız, 0 atlandı**; typecheck/build başarılı, lint **0 hata**, production bağımlılık açığı **0**.
- Kalan karar veya risk: Bu yayın Hetzner staging IP'sindedir ve HTTP kullanır; canlı marka alan adı/DNS geçişi bu kapsamda değildir. Ticari reklam satışı başladığında sözleşme, fatura/vergi, gösterim-tıklama raporu ve KVKK/çerez gereksinimleri ayrıca tamamlanmalıdır.

### 2026-08-31 — Yan reklamlar ve premium footerın staging dağıtımı

- İstek: Masaüstü yan reklam alanlarının, görünür Koza TV dummy kreatifinin, yenilenen footerın ve sıkılaştırılmış mobil footer aralıklarının commit edilip staging ortamına dağıtılması.
- Yapılanlar: Migration öncesinde staging SQLite verisi ve medya arşivi için yeni günlük yedek alındı; uygulama paketi `f5a9af7` commit'iyle fast-forward olarak `main` branch'ine gönderildi. GitHub Actions temiz kurulum, production bağımlılık denetimi, test/build, lint, SSH aktarımı, sunucuda ikinci test, atomik sürüm değişimi ve dış smoke adımlarının tamamını başarıyla bitirdi. Dört reklam alanlı mevcut tablo altı alanı destekleyecek biçimde veri kaybı ve mükerrer kayıt olmadan taşındı.
- Değişen ana dosyalar: Dağıtılan uygulama sürümü `f5a9af7`; yayın kaydı için `AGENTS.md`.
- Doğrulama: Yeni yedekte 56 MB SQLite ve 687 MB medya arşivinin SHA-256 kontrolleri geçti; yedek veritabanı `quick_check` sonucu `ok`. GitHub Actions çalışması `33411374321` başarıyla tamamlandı. Aktif sunucu bağlantısı `f5a9af7c27218ab3aa77bdfbac658466d36193ac`; `kozatv` ve Caddy servisleri aktif. Kalıcı veritabanında altı farklı reklam konumu ve altı kampanya var, geçici eski tablo yok, `quick_check` sonucu `ok`. `/`, `/kategori/gundem`, `/son-dakika`, `/canli`, `/yazarlar` ve `/admin/giris` HTTP 200; `/admin` HTTP 307; `/api/auth/me` ve `/api/ads` HTTP 401; yeni dikey reklam SVG'si HTTP 200 ve doğru görsel türüyle açıldı. Ana sayfa, kategori, Son Dakika ve güncel haber detayında iki yan reklam ile tek footer işareti doğrulandı. Gerçek staging tarayıcısında 360 px mobil footer 2×2 sosyal düzen, görünür iki navigasyon grubu, 38 px bağlantı yüksekliği, 14,16 px satır yüksekliği ve sıfır yatay taşmayla çalıştı; görünür kırık görsel yoktu. Yerel yayın kapıları `npm test` ile **71 geçti, 0 başarısız, 0 atlandı**; typecheck başarılı, lint **0 hata/35 bilinen uyarı**, production bağımlılık açığı **0**.
- Kalan karar veya risk: Yayın Hetzner staging IP'sinde HTTP çalışmaktadır; canlı marka alan adı ve DNS geçişi bu kapsamda değildir. Yan reklamlar içerik sıkışmasını önlemek için 1360 px altında bilinçli olarak gizlenir. Ham `<img>` performans uyarıları ayrı optimizasyon işidir.

### 2026-08-31 — Premium ve kompakt Koza TV footerı

- İstek: Eski footerın daha şık bir yayın kapanışına dönüştürülmesi; dikey dizilen sosyal medya ikonlarının düzeltilmesi; mobil kategori ve kurumsal bağlantıların gereksiz satır yüksekliğinin azaltılması.
- Yapılanlar: Footer koyu katmanlı kurumsal zemin, panelden yönetilen motto, canlı yayın ve RSS çağrıları, marka özeti, semantik kategori/kurumsal navigasyonları, yayın bilgisi kartı ve düzenli yasal bağlantı satırıyla baştan kuruldu. Facebook, X, YouTube ve Instagram bağlantıları ikon ile adını birlikte taşıyan yatay düğmelere dönüştürüldü; mobilde dört bağlantı alt alta uzamak yerine 2×2 yerleşiyor. Eski mobil kuralın kategori ve kurumsal grupları gizlemesi kesin olarak geçersiz kılındı. Mobil bağlantı yüksekliği 44 pikselden 38 piksele, satır yüksekliği 16,2 pikselden 14,16 piksele indirildi; grup iç boşlukları sıkılaştırıldı. Klavye odağı, dokunmatik hover davranışı ve azaltılmış hareket tercihi korundu.
- Değişen ana dosyalar: `app/site-chrome.tsx`, `app/globals.css`, `tests/rendered-html.test.mjs`, `AGENTS.md`.
- Doğrulama: `npm test` production build ile **71 test geçti; 0 başarısız, 0 atlandı**. `npx tsc --noEmit` başarılı; bağımsız `npm run build` başarılı; `npm run lint` **0 hata**, mevcut ham `<img>` yüzeyleri için **35 performans uyarısıyla** tamamlandı. Gerçek uygulama tarayıcısında geniş masaüstü footer görsel olarak incelendi; dört sosyal düğme aynı satırda ölçüldü. 360 px mobilde dört sosyal düğme 2×2, kategori ve Koza TV grupları görünür, bağlantılar 38 px yüksekliğinde ve 14,16 px satır aralığında ölçüldü; `scrollWidth == innerWidth` ve yatay taşma sıfırdı. 1100 px altındaki iki sütunlu tablet sözleşmesi ile 680 px altındaki tek sütunlu mobil sözleşme regresyon testinde korunuyor.
- Kalan karar veya risk: Footer değişiklikleri henüz commit edilmedi ve staging ortamına dağıtılmadı. Çalışma ağacında footer işi öncesinden bulunan, devam eden reklam alanı değişiklikleriyle aynı dosyalara dokunan kullanıcı değişiklikleri korunmuştur; sonraki committe kapsam dikkatle seçilmelidir.

### 2026-09-08 — Disk doluluğu giderimi, kapak onarımı ve sürüm saklama sınırı

- İstek: Haber aktarımı sırasında görsellerin yüklenmemesinin giderilmesi; sunucuda yalnız son üç-dört uygulama sürümünün tutulması.
- Yapılanlar: Hetzner diskinin 42 ayrı uygulama sürümü nedeniyle 24 GB, yerel yedeklerle birlikte toplam 38 GB dolduğu; son 33 aktarım görselinin `ENOSPC` hatası aldığı doğrulandı. Aktif sürüm ve en yeni üç geri dönüş sürümü korunarak açıkça doğrulanan 38 eski sürüm kaldırıldı; 6 Eylül'de yarım kalmış geçersiz yedek silindi. Kapak onarım aracı 280 yer tutuculu haberi yeniden taradı ve 65 gerçek görseli medya kütüphanesine geri kazandırdı. Dağıtım sonrası aktif sürüm dahil en yeni dört sürümü koruyan güvenli temizleme aracı kuruldu. Yedekleme süresi dolan kopyaları yeni arşivden önce temizleyecek ve hata halinde yarım günlük arşivi kaldıracak biçimde sağlamlaştırıldı. Sunucudaki önceki dağıtım/yedek komutları geri dönüş kopyalarıyla korunup yeni komutlar `/usr/local/sbin` altına kuruldu.
- Değişen ana dosyalar: `deployment/hetzner/prune-releases.sh`, `deployment/hetzner/deploy.sh`, `deployment/hetzner/kozatv-backup.sh`, `deployment/hetzner/README.md`, `tests/rendered-html.test.mjs`, `AGENTS.md`; sunucuda sürüm/yedek envanteri, 65 haberin kapak yolu ve üç operasyon komutu.
- Doğrulama: Disk kullanımı `%100` ve 0 boş alandan `%35` ve 24 GB boş alana indi; sürüm sayısı 42'den 4'e düştü. Canlı SQLite `PRAGMA quick_check` sonucu `ok`; yeni `20260908T192005Z` yedeğinin SQLite ve medya SHA-256 değerleri ile yedek veritabanı `quick_check` sonucu başarılı. Uygulama, Caddy, backup ve health timer'ları aktif; ana sayfa, Gündem, Son Dakika, canlı yayın, yazarlar ve admin giriş rotaları HTTP 200 döndürdü. Onarılan en yeni 10 görselin 10'u HTTP 200 ve doğru `image/webp` türüyle açıldı. `npm test` production build ile **72 test geçti; 0 başarısız, 0 atlandı**; yeni regresyonlar geçersiz saklama sınırında hiçbir sürümün silinmemesini, aktif sürüm eski olsa bile korunmasını, tam dört sürüm kalmasını, süresi dolan yedeğin önceden temizlenmesini ve yarım yedek hata temizliğini kapsıyor. `npx tsc --noEmit` ve bağımsız `npm run build` başarılı; `npm run lint` **0 hata**, mevcut **35** ham görsel uyarısıyla tamamlandı. İlk test denemesi yerel Node 16'nın `node:readline/promises` desteği olmaması nedeniyle başlamadı; Node 23 ile tekrarlandı. Mutlak operasyon komutu adına göre güncellenmemiş ilk test beklentisi bir kez başarısız oldu, beklenti düzeltilip tam paket yeniden çalıştırıldı.
- Kalan karar veya risk: Kaynakta gerçek kapak bulunmayan 164 ve boş dosya dönen 51 eski haber tarafsız yer tutucuda kaldı; bunlar disk hatası değildir ve editör görsel ekleyebilir. Altyapı değişiklikleri çalışma ağacındadır, commit veya push yapılmadı. Sunucudaki önceki komut kopyaları `/usr/local/sbin/*.before-retention-20260908` adlarıyla geri alınabilir durumdadır.

### 2026-09-09 — Manşet seçimlerinin görünür sıraya alınması

- İstek: **PISA'da Basamak Atladık**, **CHP 103 Yaşında** ve **İstanbul'da Üç Plaja Kapatma Geldi** haberleri panelde Manşet olarak seçildiği halde ana sayfa sliderında görünmediği için sorunun düzeltilip staging ortamına yayınlanması.
- Yapılanlar: Kök neden, panelden Manşet seçilen haberlere varsayılan `homepage_order=100` yazılması ve ana sayfanın yalnız `1–5` sıralı ilk beş manşeti göstermesiydi. Yeni yayınlanan, planlanan veya iş akışından yayına alınan bir haber slider/manşet yanı/manşet altı konumlarından birine ilk kez girdiğinde artık otomatik olarak o alanın birinci sırasına alınıyor; önceki kayıtlar birer sıra geriye kayıyor. Önceki sürümde slider için sıra 100'de kalmış mevcut kayıtları, en son seçilen en önde olacak biçimde yalnız bir kez onaran tekrar çalıştırılabilir veri geçişi eklendi. Dağıtım sonrasındaki gerçek veri kontrolünde PISA ve İstanbul kayıtları sliderda görünürken CHP 103 kaydının panelde **Manşet-sol** konumunda kaldığı görüldü; kullanıcı isteğine uygun olarak yönetici ekranından **Manşet · Slider** seçilip yeniden yayına alındı.
- Değişen ana dosyalar: `db/homepage-order.mjs`, `db/index.ts`, `tests/rendered-html.test.mjs`, `AGENTS.md`.
- Doğrulama: `npm test` production build ile **76 test geçti; 0 başarısız, 0 atlandı**. Yeni regresyonlar eski `1–5` kayıtlarının arkasında sıra 100'de kalan üç manşetin `1–2–3` görünür sırasına alınmasını, onarımın ikinci çalışmada sırayı değiştirmemesini, yeni seçilen üç manşetin ana sayfadaki ilk beş slaytta görünmesini ve editoryal iş akışından yayınlanan ana sayfa haberinin birinci sıraya taşınmasını doğruladı. `npx tsc --noEmit` ve bağımsız `npm run build` başarılı; `npm run lint` **0 hata**, mevcut ham `<img>` yüzeyleri için **36 bilinen performans uyarısı** ile tamamlandı. `8018ca3` commit'i `main` branch'ine gönderildi; GitHub Actions çalışması `34379274963` temiz kurulum, production bağımlılık denetimi, test/build, lint, sunucu testi, atomik dağıtım ve dış smoke adımlarını **1 dakika 59 saniyede başarıyla** tamamladı. Gerçek staging sliderının beş konumu tek tek açıldı; **CHP 103 Yaşında**, **PISA'da Basamak Atladık** ve **İstanbul'da Üç Plaja Kapatma Geldi** birlikte görünür ilk beş içinde doğrulandı. 1440, 900 ve 390 px gerçek Chrome görüntülerinde slider ve haber görselleri kontrol edildi; dış HTTP kontrollerinde `/` ve `/admin/giris` 200, `/admin` 307 döndürdü.
- Kalan karar veya risk: Yayın Hetzner staging IP'sinde HTTP çalışmaktadır; canlı marka alan adı/DNS/HTTPS geçişi bu kapsamda değildir. Bundan sonra yeni bir haber Manşet seçildiğinde otomatik olarak birinci slayda gelir; gerekirse Yayın Stüdyosu'ndaki sıra alanından sonradan farklı bir konuma taşınabilir.

### 2026-09-09 — Haber arşivinden yayındaki habere doğrudan geçiş

- İstek: Yönetim panelindeki **Tüm Haberler / Haber Arşivi** listesinde her yayındaki haberin tam karşısına, ilgili ziyaretçi haber sayfasını doğrudan açan açık bir **Habere git** işlemi eklenmesi ve staging ortamına yayınlanması.
- Yapılanlar: Arşiv tablosunun son sütunu **İşlem** olarak adlandırıldı. Yalnız `published` durumundaki kayıtlar için `/haber/{slug}` adresini yeni sekmede açan **Habere git ↗** bağlantısı eklendi; taslak, inceleme ve planlı içeriklerde herkese kapalı ziyaretçi bağlantısı üretilmiyor. Eski belirsiz ok, yetkili kullanıcılar için açık **Düzenle →** düğmesine dönüştürüldü. Bağlantı ve düzenleme işlemleri geçerli HTML için ayrı etkileşim öğeleri olarak kuruldu; klavye odağı, hover durumu ve mobilde 44 px dokunma alanı eklendi.
- Değişen ana dosyalar: `app/admin/panel.tsx`, `app/globals.css`, `tests/rendered-html.test.mjs`, `AGENTS.md`.
- Doğrulama: Değişiklikten önce yeni regresyon testi **İşlem** sütunu bulunmadığı için başarısız oldu; uygulamadan sonra geçti. `npm test` production build ile **74 test geçti; 0 başarısız, 0 atlandı**. `npx tsc --noEmit` ve bağımsız `npm run build` başarılı; `npm run lint` **0 hata**, mevcut ham `<img>` yüzeyleri için **36 performans uyarısıyla** tamamlandı. Gerçek tarayıcıda 1280 px masaüstü, 900 px tablet ve 390 px mobil Haber Arşivi kontrol edildi; tablet ve mobilde `documentWidth == viewportWidth`, mobil işlem yüksekliği 44 px. Test verisindeki altı yayındaki kaydın altısında bağlantı, incelemedeki kayıtta yalnız düzenleme işlemi görüldü; ilk **Habere git** tıklaması doğru haber adresini yeni sekmede açtı.
- Kalan karar veya risk: Değişiklikler kullanıcı isteğiyle commit ve Hetzner staging dağıtımı kapsamına alındı. Ham `<img>` performans uyarıları ayrı optimizasyon işidir.

### 2026-09-09 — Hızlı fotoğraf araması ve yönetici yayın akışının staging dağıtımı

- İstek: Daha önce tamamlanan medya araması, yalnız yöneticinin doğrudan yayınlaması ve disk/sürüm korumalarının önce sunucuya yayınlanması.
- Yapılanlar: Fotoğraf arşivinde dosya adı, açıklama ve kredi/ajans üzerinden arama; habere tek tıkla kapak seçimi; yalnız yönetici için doğrudan **Şimdi yayınla** işlemi; sürüm saklama ve yedek sağlamlaştırmaları `b7fd563` commit'iyle `main` branch'ine gönderildi. GitHub Actions temiz kurulum, test, lint, sunucu aktarımı, atomik sürüm değişimi ve dış smoke kontrollerini tamamladı.
- Değişen ana dosyalar: Dağıtılan uygulama sürümü `b7fd563ce2776772bacc42a8f67aa18aa81f1d4e`; yayın kaydı için `AGENTS.md`.
- Doğrulama: GitHub Actions çalışması `34359833009` başarıyla tamamlandı. Dış kontrolde `/` ve `/admin/giris` HTTP 200, `/admin` HTTP 307, `/api/auth/me` HTTP 401 döndürdü. Sunucuda `kozatv` ve Caddy servisleri aktif, `/srv/kozatv/current` doğru commit'e bağlı ve aktif sürüm dahil yalnız dört sürüm korunuyor.
- Kalan karar veya risk: Yayın Hetzner staging IP'sinde HTTP çalışmaktadır; `www.kozatv.com.tr` DNS/HTTPS canlı geçişi bu dağıtımın kapsamında değildir.

### 2026-09-09 — Aranabilir video kütüphanesi ve haber içi oynatıcı

- İstek: Editörün “Özgür Özel Meclis” gibi bir adla fotoğrafı hızlı arayıp seçebilmesi; videoların da kütüphaneye yüklenmesi, aranması ve haberlerde doğru yerde oynatılması.
- Yapılanlar: Medya kütüphanesi fotoğraf/video türlerine ayrıldı; dosya adı, açıklama ve kredi/ajans araması iki türde de çalışıyor. Haber editörüne ayrı **Bilgisayardan video yükle** ve **Kütüphanede video ara** işlemleri eklendi. MP4/WebM dosyaları imzayla doğrulanıyor, video başına 150 MB sınırı, mevcut 10 GB toplam kota ve 1 GB boş disk güvenlik payı korunuyor. Video sunumu HTTP byte-range destekli akışa geçirildi; ileri/geri sarma çalışıyor. Ana video kapak görselinin altında ve haber metninden önce 16:9 oynatılıyor; YouTube, Vimeo ve güvenli HTTPS/HLS bağlantıları ile Yayın Stüdyosu'ndaki paragraf arası ek video blokları korundu. Fotoğraf seçicisinde video, video seçicisinde fotoğraf gösterilmiyor; geçersiz video adresleri ve sahte dosyalar reddediliyor.
- Değişen ana dosyalar: `db/media-storage.ts`, `db/index.ts`, `db/article-model.mjs`, `app/api/media/route.ts`, `app/media/[...path]/route.ts`, `app/admin/panel.tsx`, `app/admin/workflow-studio.tsx`, `app/haber/[slug]/article-blocks.tsx`, `app/haber/[slug]/page.tsx`, `app/videolar/page.tsx`, `app/globals.css`, `tests/rendered-html.test.mjs`, `AGENTS.md`.
- Doğrulama: `npm test` production build ile **73 test geçti; 0 başarısız, 0 atlandı**. Testler geçerli/sahte MP4, tür filtreleme, açıklama/kredi araması, toplam kota, yetkisiz yükleme, geçerli/geçersiz byte-range yanıtı, güvenli video adresi ve kapak → video → metin sırasını kapsıyor. `npx tsc --noEmit` başarılı; `npm run lint` **0 hata**, mevcut ham `<img>` yüzeyleri için **35 uyarı** ile tamamlandı. Gerçek 1 saniyelik MP4 yerel panelden “Özgür Özel Meclis konuşması” adı ve “Koza TV Arşiv” kredisiyle yüklendi, “Meclis” aramasında tek sonuç bulundu, tek tıkla habere bağlandı ve oynatıldı. Masaüstünde oynatıcı 857×482; 900 px tablette 817×460; 390 px mobilde 348×196 ölçüldü. Mobilde `documentWidth == viewportWidth`, dört hızlı medya işlemi en az 44 px yüksekliğinde ve tarayıcı konsolunda hata yoktu. `48a552e` uygulama commit'i `main` branch'ine gönderildi; GitHub Actions çalışması `34364237139` test, aktarım, atomik dağıtım ve dış smoke adımlarının tamamını 1 dakika 58 saniyede geçti. Sunucunun aktif sürümü `48a552ecee46e25527749da7d6e4b317250e3b0c`; `kozatv` ve Caddy aktif, yalnız dört sürüm tutuluyor. Dış kontrolde `/`, `/videolar` ve `/admin/giris` HTTP 200; `/admin` HTTP 307; yetkisiz `/api/auth/me` ve `/api/media` HTTP 401 döndürdü. Yayın paketinde **Bilgisayardan video yükle** arayüzü ayrıca doğrulandı.
- Kalan karar veya risk: Doğrudan yükleme kısa ve orta uzunluktaki MP4/WebM videolar içindir; uzun/yüksek çözünürlüklü yayınlarda sunucu diskini hızla doldurmamak için YouTube/Vimeo bağlantısı tercih edilmelidir. Yayın Hetzner staging IP'sinde HTTP çalışmaktadır; canlı marka alan adı/DNS/HTTPS geçişi bu kapsamda değildir.

### 2026-09-09 — Yalnız yönetici için tek tıkla haber yayını

- İstek: Haber editöründeki **Şimdi yayınla** düğmesinin yönetici için ek editör/yayın onayı adımı istemeden çalışması ve haberleri yalnız yönetici rolünün yayınlayabilmesi.
- Yapılanlar: `published` ve `scheduled` durumlarına geçiş yalnız `admin` rolüne indirildi; yayın yönetmeninin içerik inceleme, onaylama ve diğer operasyon yetkileri korunurken doğrudan yayınlama ve planlama yetkisi kaldırıldı. Yönetici basit haber editöründe veya Yayın Stüdyosu'nda **Şimdi yayınla** düğmesine bastığında haber hangi taslak aşamasında olursa olsun doğrudan `published` durumuna ve `published` iş akışına geçiriliyor. Basit editörden incelemeye gönderilen haberin iş akışı artık `editor_review` ile eşzamanlı tutuluyor. Doğrudan yayın ve planlama işlemleri revizyon, denetim ve iş akışı geçmişine yönetici adıyla kaydediliyor. Yayın düğmeleri arayüzde yalnız yöneticiye gösteriliyor; API ve editoryal işlem uçları da aynı rol sınırını sunucu tarafında uyguluyor.
- Değişen ana dosyalar: `db/editorial-permissions.mjs`, `db/index.ts`, `app/api/articles/route.ts`, `app/api/editorial/route.ts`, `app/admin/panel.tsx`, `app/admin/workflow-studio.tsx`, `tests/rendered-html.test.mjs`, `AGENTS.md`.
- Doğrulama: Hata önce regresyon testiyle yeniden üretildi; mevcut kod yönetici doğrudan yayınında HTTP 409 döndürdü ve yayın yönetmenine yayın yetkisi verdi. Düzeltmeden sonra `npm test` production build ile **73 test geçti; 0 başarısız, 0 atlandı**. Yeni senaryo yöneticinin inceleme durumundaki haberi tek PATCH ile yayınlamasını, durum ve iş akışının birlikte `published` olmasını, haberin ziyaretçi sayfasında açılmasını, yayın yönetmeninin hem haber API'sinde hem Yayın Stüdyosu işleminde HTTP 403 almasını ve iki arayüzde yayın düğmesinin yalnız admin koşuluna bağlı olmasını doğruladı. `npx tsc --noEmit` ve bağımsız `npm run build` başarılı; `npm run lint` **0 hata**, mevcut ham `<img>` yüzeyleri için **35 bilinen performans uyarısı** ile tamamlandı. Ayrı yerel veritabanı ve gerçek Chrome ile yönetici oturumunda muhabir taslağındaki **Şimdi yayınla** düğmesine basıldı; haber `v1 / Muhabir taslağı` durumundan `v2 / Yayında` durumuna geçti ve işlem geçmişinde **Yönetici doğrudan yayına aldı.** kaydı görüldü.
- Kalan karar veya risk: Değişiklikler henüz commit edilmedi, GitHub'a gönderilmedi ve Hetzner staging ortamına dağıtılmadı; bu nedenle mevcut staging panelinde eski davranış sürer. İlk yerel test Node 24'e göre derlenmemiş mevcut `better-sqlite3` native bağımlılığıyla sunucuyu başlatamadı; projenin kurulu ve daha önce doğrulanmış Node 23 ortamında bütün kapılar eksiksiz geçti.

### 2026-09-09 — Haber editöründe hızlı medya arşivi araması

- İstek: Editörün haber hazırlarken kalabalık medya kütüphanesini elle taramak zorunda kalmaması; arşivdeki fotoğrafı hızlıca arayıp tek tıkla kapak görseli olarak seçebilmesi ve bilgisayardan hızlı yükleme yolunun korunması.
- Yapılanlar: Haber editöründeki görsel alanına **Kütüphanede ara** işlemi ve sonuçları aynı ekranda gösteren arama paneli eklendi. Medya Kütüphanesi'nin tamamında dosya adı, alternatif metin/açıklama ve fotoğraf-ajans kredisi üzerinden arama yapılabiliyor; sorgu 250 ms gecikmeyle çalışıyor, yükleniyor/hata/sonuç bulunamadı durumlarını gösteriyor ve sonuç tek tıkla habere atanıyor. API araması yalnız ekranda önceden yüklenmiş ilk kayıtları değil bütün `media_assets` tablosunu tarıyor; özel SQL joker karakterleri kaçırılıyor, sorgu ve sonuç sınırları uygulanıyor. Arşivden yeni fotoğraf seçildiğinde önceki fotoğrafın açıklamasının yanlışlıkla korunması tarayıcı kontrolünde yakalandı; seçilen görselin kendi alt metni artık kapak alanına birlikte aktarılıyor. Bilgisayardan hızlı yükleme korunarak arama düğmesinin yanında bırakıldı. Mobil arama alanı 44 px yüksekliğe ve 16 px yazıya çıkarıldı.
- Değişen ana dosyalar: `db/index.ts`, `app/api/media/route.ts`, `app/admin/panel.tsx`, `app/globals.css`, `tests/rendered-html.test.mjs`, `AGENTS.md`.
- Doğrulama: Arama testi önce mevcut davranışta iki noktada başarısız oldu: panelde arama arayüzü yoktu ve eşleşmeyen API sorgusu bütün medyayı döndürüyordu. Düzeltmeden sonra `npm test` production build ile **73 test geçti; 0 başarısız, 0 atlandı**. Yeni kontroller arayüz sözleşmesini, dosya adı/açıklama/kredi aramasını, sonuç bulunamamasını, önceki görsel açıklamasının taşınmamasını ve mobil arama ölçülerini kapsıyor. `npx tsc --noEmit` ve bağımsız `npm run build` başarılı; `npm run lint` **0 hata**, mevcut ham `<img>` yüzeyleri için **35 bilinen performans uyarısı** ile tamamlandı. Ayrı yerel veritabanına üç test görseli yüklendi; gerçek Chrome'da `Ankara` ve `stüdyo` aramaları birer doğru sonuç verdi, sonuç tek tıkla seçildi ve görsel yolu ile alt metin birlikte güncellendi. 1920 px masaüstü, 900 px tablet ve mobil kırılımda yatay taşma görülmedi; mobil arama alanı 44 px/16 px ölçüldü.
- Kalan karar veya risk: Tek sorguda en yeni 80 eşleşme gösteriliyor; mevcut medya hacmi için yeterli olmakla birlikte ileride tek kelimede 80'den fazla eşleşme oluşursa sayfalama eklenebilir. Değişiklikler henüz commit edilmedi, GitHub'a gönderilmedi ve Hetzner staging ortamına dağıtılmadı.

### 2026-09-09 — Sade manşet sliderı ve editörden yazı konumu

- İstek: Ana sayfa sliderında görseli kapatan Spotlight alanının, gereksiz manşet etiketinin ve spot/alt yazının kaldırılması; beyaz başlığın 2 px küçültülmesi; karartmanın hafifletilmesi ve başlığın editörden sol üst, sol alt, sağ üst veya sağ alt köşeye alınabilmesi.
- Yapılanlar: Slider altındaki dört kartlı Spotlight bölümü tamamen kaldırıldı. Slider içindeki **KOZA TV MANŞET** etiketi ve spot metni çizimden çıkarıldı; kategori, başlık, tarih ve haber bağlantısı korunarak bilgi yoğunluğu azaltıldı. Başlık masaüstü, tablet ve mobilde önceki değerden tam 2 px küçültüldü. Yüzde 96'ya çıkan sabit karartma daha hafif, metnin seçildiği sağ/sol tarafa göre yön değiştiren degrade ile değiştirildi. `headline_position` alanı güvenli migration, doğrulama, SQLite kayıt/okuma ve revizyon desteğiyle haber modeline eklendi; eski haberler veri kaybı olmadan **sol alt** varsayımını kullanıyor. Basit haber editörü ve Yayın Stüdyosu dört köşeyi Türkçe adlarıyla seçebiliyor; masaüstünde kenar boşlukları ve sağ üstte Son Dakika şeridi için ek güvenli alan var, mobilde okunabilirlik için metin görselin altındaki koyu alanda tutuluyor.
- Değişen ana dosyalar: `db/article-model.mjs`, `db/index.ts`, `app/page.tsx`, `app/site-client.tsx`, `app/admin/panel.tsx`, `app/admin/workflow-studio.tsx`, `app/globals.css`, `tests/rendered-html.test.mjs`, `AGENTS.md`.
- Doğrulama: Regresyonlar önce mevcut Spotlight, alt yazı ve eksik konum alanı nedeniyle başarısız oldu. Düzeltmeden sonra `npm test` production build ile **73 test geçti; 0 başarısız, 0 atlandı**. `npx tsc --noEmit` başarılı; `npm run lint` **0 hata**, mevcut ham `<img>` yüzeyleri için **34 bilinen performans uyarısı** ile tamamlandı. Ayrı geçici SQLite verisi ve gerçek Chrome ile 1440, 900 ve 390 px görünümler incelendi: Spotlight, slider spotu ve ek manşet etiketi sayısı sıfır; seçilen **sağ üst** konumu masaüstü/tablette doğru hizalandı, 1440 px başlık 44,8 px ölçülerek önceki değerden tam 2 px küçük olduğu doğrulandı. Mobilde `documentWidth == viewportWidth == 390`, başlık 25 px ve güvenli alt okuma alanında; editörde dört seçenek de görünür ve varsayılan **Sol alt**.
- Kalan karar veya risk: Değişiklikler henüz commit edilmedi, GitHub'a gönderilmedi ve Hetzner staging ortamına dağıtılmadı. Sağ/sol köşe seçimi masaüstü ve tablette uygulanır; mobilde küçük ekranda fotoğrafı ve başlığı birlikte korumak için dikey köşe seçimi bilinçli olarak güvenli alt yerleşime düşer.

### 2026-09-09 — Yayın sırasında ana sayfa konumu ve aşağı taşınan Günün Akışı

- İstek: Hero alanındaki **Günün Akışı** yerine gerçek haber gösterilmesi; akışın ana sayfanın altına taşınması; haber yayınlanırken **Manşet / slider**, **Manşet-sol / eski Günün Akışı alanı**, **Manşet altı** veya varsayılan **Son Haberler** konumunun seçilebilmesi.
- Yapılanlar: Haber modeline `latest`, `slider`, `side` ve `below` değerlerini kabul eden `homepage_placement` alanı eklendi. Konum seçilmeyen yeni haberler güvenli biçimde **Son Haberler**e gider; yalnız `slider` seçimi eski `is_featured` işaretiyle eşzamanlı tutulur. Mevcut manşet haberleri veri kaybı olmadan slider olarak korunur ve tek seferlik geçiş başlangıç vitrini için en güncel normal haberi manşet yanına, bir sonrakini manşet altına yerleştirir; yönetici tercihleri sonraki açılışlarda ezilmez. Hero yanındaki metin listesi kaldırılıp tam görselli haber kartına dönüştürüldü, manşet altına ayrı görselli alan eklendi ve altı maddelik **Günün Akışı** Son Haberler bölümünün altına taşındı. Basit haber editörü ile Yayın Stüdyosu aynı dört Türkçe konum seçeneğini sunuyor; slider seçilmedikçe fotoğraf üzerindeki başlık köşesi alanı kapalı tutuluyor.
- Değişen ana dosyalar: `db/article-model.mjs`, `db/index.ts`, `app/page.tsx`, `app/admin/panel.tsx`, `app/admin/workflow-studio.tsx`, `app/globals.css`, `tests/rendered-html.test.mjs`, `AGENTS.md`.
- Doğrulama: Yeni regresyonlar önce eksik doğrulama/SQLite alanı, eski hero akışı ve eksik yerleşim yüzeyleri nedeniyle **4 beklenen başarısızlık** verdi. Uygulamadan sonra `npm test` production build ile **73 test geçti; 0 başarısız, 0 atlandı**. Testler geçersiz konumun reddini, konumun SQLite'ta korunmasını, `side` seçiminin slider işaretine dönüşmemesini, seçim yapılmayan haberin **Son Haberler**e gitmesini, üç ayrı ana sayfa yerleşimini ve iki yönetim ekranındaki seçenekleri kapsıyor. `npx tsc --noEmit` başarılı; `npm run lint` **0 hata**, mevcut ham `<img>` yüzeyleri ve iki yeni haber görseli için toplam **36 performans uyarısı** ile tamamlandı. Ayrı geçici SQLite verisi ve gerçek Chrome ile 1440, 900 ve 390 px ölçüldü: üç görünümde `scrollWidth == clientWidth`, kırık görsel sayısı sıfır; manşet yanı, manşet altı ve aşağı taşınan altı akış satırı görünür. Yönetici formunda varsayılan **Son Haberler (varsayılan)** göründü; **Manşet · Slider** seçilince manşet yazısı konumu etkinleşti ve ziyaretçi/yönetim konsollarında hata yoktu.
- Kalan karar veya risk: Değişiklikler henüz commit edilmedi, GitHub'a gönderilmedi ve Hetzner staging ortamına dağıtılmadı. İlk geçiş yalnız başlangıç düzenini kurar; sonraki bütün haber konumları yayın sırasında editörün seçimine bağlıdır. Ham `<img>` performans uyarıları ayrı optimizasyon işidir.

### 2026-09-09 — Ana sayfa konumlarının staging dağıtımı

- İstek: Sade manşet sliderı, editörden başlık köşesi, yayın sırasında ana sayfa konumu ve aşağı taşınan Günün Akışı çalışmalarının önce staging ortamına yayınlanması.
- Yapılanlar: Dokuz ilgili dosya `70ef47c` commit'iyle fast-forward olarak `main` branch'ine gönderildi. GitHub Actions temiz kurulum, test/build, lint, sunucu aktarımı, atomik sürüm değişimi ve dış smoke kontrollerini başarıyla tamamladı.
- Değişen ana dosyalar: Dağıtılan uygulama sürümü `70ef47cef905ce92e1b0bbd2f530d03be16e6025`; yayın kaydı için `AGENTS.md`.
- Doğrulama: Push öncesinde production build ile **73 test geçti; 0 başarısız, 0 atlandı**; typecheck başarılı, lint **0 hata/36 bilinen ham görsel uyarısı** verdi. GitHub Actions çalışması `34371354364` başarılı tamamlandı. Dış kontrolde `/` ve `/admin/giris` HTTP 200 döndürdü; staging HTML'inde `hero-side-news`, `headline-below` ve `home-flow-lower` yüzeyleri doğrulandı.
- Kalan karar veya risk: Yayın Hetzner staging IP'sinde HTTP çalışmaktadır; canlı marka alan adı/DNS/HTTPS geçişi bu kapsamda değildir.

### 2026-09-09 — Haber sonunda kesintisiz kategori akışı ve ince reklam

- İstek: Okur bir haberi bitirdiğinde sayfanın doğrudan sona ermemesi; aynı kategoride ondan önce yayınlanmış son beş haberin sırayla tam metin olarak gelmesi; her haber bitiminde ince reklam gösterilmesi ve tek habere birden fazla görsel eklenebilmesinin doğrulanması.
- Yapılanlar: Verilen MedyaTava örneğinin beş sayfalı fotoğraf yapısı ve haber sunumu incelendi; Koza TV için sayfa tıklatmadan çalışan özgün bir **Kesintisiz Okuma** yüzeyi kuruldu. Geçerli haberden yalnız daha eski, aynı kategorideki, yayındaki beş kayıt bileşik yayın tarihi/kayıt kimliği sırasıyla seçiliyor; başka kategori, taslak ve altıncı eski kayıt akışa girmiyor. Her takip haberi başlık, spot, yazar, tarih, kapak, video, tam blok metni, ajans/kaynak bilgisi ve ayrı sayfa bağlantısıyla okunuyor. Ana haberin ve takip eden her haberin sonunda mevcut `section_inline` envanterinden 82 px kreatifli ince reklam gösteriliyor. Devam haberlerinin görselleri tembel, doğrudan video dosyaları `preload=none` yüklenerek uzun sayfanın ağ maliyeti sınırlandı. Yayın Stüdyosu'ndaki sınırsız **+ Görsel** blokları korunuyor; tek haberde iki ek içerik görseli gerçek sayfada birlikte doğrulandı.
- Değişen ana dosyalar: `db/index.ts`, `db/ad-model.mjs`, `app/haber/[slug]/page.tsx`, `app/haber/[slug]/article-blocks.tsx`, `app/globals.css`, `tests/rendered-html.test.mjs`, `AGENTS.md`.
- Doğrulama: Regresyon testi uygulama öncesinde yeni akış bulunmadığı için beklenen biçimde **1 başarısızlık** verdi. Tam paket ilk tekrarında eski ajans testinin artık devam haberlerindeki meşru kaynak kutularını da taradığı görüldü; kontrol yalnız açık ana haberin alanına daraltıldı ve hiçbir ürün kuralı gevşetilmedi. Sonrasında `npm test` production build ile **74 test geçti; 0 başarısız, 0 atlandı**; `npx tsc --noEmit` ve bağımsız build başarılı, lint **0 hata/36 bilinen ham görsel uyarısı** ile tamamlandı. Gerçek Chrome'da 1440, 900 ve 390 px görünümlerde beş devam haberi, altı ince reklam, iki ek görsel ve kategori sonu bağlantısı doğrulandı; üç görünümde `scrollWidth == clientWidth`, kırık görsel ve konsol hatası sıfır. Devam kartları sırasıyla 820, 760 ve 366 px genişliğinde; ince reklam masaüstünde 102, mobilde 134 px toplam yüksekliğinde ölçüldü.
- Kalan karar veya risk: Kesintisiz akış bilinçli olarak en fazla beş haberle sınırlıdır; içerik JavaScript olmadan da okunur. Çoklu görseller MedyaTava'daki gibi her fotoğraf için yeni sayfa açmak yerine aynı haber içinde ardışık ve açıklamalı gösterilir. Yeni kesintisiz okuma değişiklikleri henüz commit edilmedi ve staging ortamına dağıtılmadı.

### 2026-09-09 — Yalnız başlıklı sade manşet sliderı

- İstek: Ana sayfa sliderında görselin üzerindeki kategori/etiket, spot, tarih, **Haberi oku** düğmesi ve Son Dakika şeridinin kaldırılması; yalnız beyaz haber başlığının kalması; başlığın görseli kapatmayacak ölçüde küçültülüp harf aralığının iyileştirilmesi ve editördeki sağ/sol köşe yerleşiminin korunması; ardından staging ortamına yayınlanması.
- Yapılanlar: Slider çizimi yalnız tıklanabilir beyaz başlık ve mevcut erişilebilir slider kontrollerini bırakacak biçimde sadeleştirildi. Kategori rozeti, tarih, okuma düğmesi ve slidera özel Son Dakika şeridi bileşenden tamamen çıkarıldı; daha önce kaldırılan spotun geri gelmesini de engelleyen regresyon korundu. Başlığın masaüstü üst sınırı 50 px'ten 39 px'e, mobil boyutu 22 px'e indirildi; satır aralığı `1.08`, harf aralığı `-0.75px` oldu. Hover alt çizgisi kaldırıldı, metin alanı 620 px ile sınırlandı ve genel karartma hafifletildi. Sol üst, sol alt, sağ üst ve sağ alt yönetici konumları korunarak güvenli kenar boşlukları eşitlendi.
- Değişen ana dosyalar: `app/site-client.tsx`, `app/globals.css`, `tests/rendered-html.test.mjs`, `AGENTS.md`.
- Doğrulama: Yeni regresyon mevcut kategori, tarih/düğme ve Son Dakika şeridi nedeniyle önce beklenen biçimde başarısız oldu. Uygulamadan sonra `npm test` production build ile **74 test geçti; 0 başarısız, 0 atlandı**. `npx tsc --noEmit` başarılı; `npm run lint` **0 hata**, mevcut ham `<img>` yüzeyleri için **36 bilinen performans uyarısı** ile tamamlandı. Ayrı geçici SQLite verisi ve gerçek Chrome ile 1440, 900 ve 390 px görünümler incelendi: sliderda istenmeyen öğe sayısı sıfır, kırık görsel ve konsol hatası sıfır. 1280 px gerçek tarayıcı ölçümünde başlık 32,64 px, iki satır ve toplam 71 px yüksekliğinde; 390 px mobilde 22 px ve fotoğrafın altındaki güvenli koyu alanda göründü. Dört köşe sınıfı ve yönetici seçenekleri otomatik testle korundu.
- Kalan karar veya risk: Başlık mobilde okunabilirliği ve fotoğraf kompozisyonunu birlikte korumak için güvenli alt alana düşer; masaüstü ve tablette editörün seçtiği dört köşe uygulanır. Staging dağıtım sonucu bir sonraki yayın kaydında yer alacaktır.

### 2026-09-09 — Sade manşet ve kesintisiz okumanın staging dağıtımı

- İstek: İşaretlenen slider öğelerinin kaldırıldığı sade manşetin ve daha önce tamamlanan haber sonu kesintisiz kategori akışının yayınlanması.
- Yapılanlar: Slider sadeleştirmesi ile aynı kategorideki önceki beş haberi ve aralarındaki ince reklamları sunan kesintisiz okuma paketi `96ee74f` commit'iyle fast-forward olarak `main` branch'ine gönderildi. GitHub Actions temiz kurulum, production bağımlılık denetimi, test/build, lint, sunucu aktarımı, atomik sürüm değişimi ve dış smoke test adımlarının tamamını başarıyla bitirdi.
- Değişen ana dosyalar: Dağıtılan uygulama sürümü `96ee74f14b1fdb74d29297890da5a5aaa99b1cc9`; yayın kaydı için `AGENTS.md`.
- Doğrulama: GitHub Actions çalışması `34375336012` **2 dakika 6 saniyede başarılı** tamamlandı. Dış kontrolde `/` ve `/admin/giris` HTTP 200, `/admin` HTTP 307, `/api/auth/me` HTTP 401 döndürdü. Staging ana sayfasındaki gerçek sliderda kategori, tarih/düğme ve Son Dakika şeridi sayısı sıfır; yayın CSS'inde 39 px masaüstü üst sınırı ile sıkı harf/satır aralığı doğrulandı. İlk slider haberinin detayında **5 kesintisiz devam haberi** ve `kesintisiz-okuma` bölümü bulundu. Gerçek staging tarayıcısında 1280 px görünümde başlık 32,64 px ve 71 px toplam yükseklikte; kırık görsel ve konsol hatası sıfır.
- Kalan karar veya risk: Yayın Hetzner staging IP'sinde HTTP çalışmaktadır; canlı marka alan adı/DNS/HTTPS geçişi bu kapsamda değildir. Ham `<img>` performans uyarıları ayrı optimizasyon işidir.

### 2026-09-09 — Haber kapak altındaki otomatik dosya ve kaynak yazısının kaldırılması

- İstek: `pisa-da-basamak-atladik` haberinde kapak görselinin altında görünen `egitim.jpg · Fotoğraf: Koza TV` satırının kaldırılması ve değişikliğin staging ortamına yayınlanması.
- Yapılanlar: Haber detayındaki ana kapak ile kesintisiz okuma akışındaki takip haberlerinin kapaklarından otomatik dosya adı/görsel alt metni ve `Fotoğraf:` kaynak satırı kaldırıldı. Görsellerin erişilebilir `alt` açıklamaları korunurken editörün haber gövdesine özellikle eklediği görsel açıklamalarına dokunulmadı. Kullanılmayan iki kapak altyazısı stili temizlendi.
- Değişen ana dosyalar: `app/haber/[slug]/page.tsx`, `app/globals.css`, `tests/rendered-html.test.mjs`, `AGENTS.md`.
- Doğrulama: Yeni regresyon testi düzeltme öncesinde otomatik `Fotoğraf:` satırını yakalayarak beklenen biçimde başarısız oldu. Uygulamadan sonra `npm test` production build ile **76 test geçti; 0 başarısız, 0 atlandı**. `npx tsc --noEmit` ve bağımsız `npm run build` başarılı; `npm run lint` **0 hata**, mevcut ham `<img>` yüzeyleri için **36 bilinen performans uyarısı** ile tamamlandı. Test ayrıca tek habere eklenen iki içerik görselinin editör açıklamalarının görünmeye devam ettiğini doğruladı.
- Kalan karar veya risk: Değişiklikler kullanıcı isteğiyle commit ve Hetzner staging dağıtımı kapsamına alındı. Fotoğraf kredisi veride ve yönetim panelinde korunur; yalnız kapak altında ziyaretçiye otomatik basılmaz. Haber gövdesine editörün eklediği özel açıklamalar görünmeye devam eder.

### 2026-09-09 — Kesintisiz okuma açıklamasının kaldırılması

- İstek: Kesintisiz Okuma bölümündeki **Bu haberden önce yayınlanan 5 haberi sayfadan ayrılmadan okumaya devam edin.** cümlesinin kaldırılması.
- Yapılanlar: Yalnız açıklama paragrafı ve artık kullanılmayan masaüstü/mobil stilleri kaldırıldı. **KESİNTİSİZ OKUMA**, kategori başlığı, beş devam haberi ve haber aralarındaki reklam akışı korundu.
- Değişen ana dosyalar: `app/haber/[slug]/page.tsx`, `app/globals.css`, `tests/rendered-html.test.mjs`, `AGENTS.md`.
- Doğrulama: Yeni regresyon testi düzeltme öncesinde istenmeyen açıklamayı yakalayarak beklenen biçimde başarısız oldu. Uygulamadan sonra `npm test` production build ile **76 test geçti; 0 başarısız, 0 atlandı**. `npx tsc --noEmit` ve bağımsız `npm run build` başarılı; `npm run lint` **0 hata**, mevcut ham `<img>` yüzeyleri için **36 bilinen performans uyarısı** ile tamamlandı. Aynı regresyon beş devam haberinin ve altı reklam yerleşiminin korunmasını doğruladı.
- Kalan karar veya risk: Bu sadeleştirme, aşağıdaki haber editörü iyileştirmeleriyle aynı staging yayın kapsamına alındı.

### 2026-09-10 — Manuel haber kaydı, zorunlu kapak ve kompakt video

- İstek: Spot alanının 500 karaktere çıkarılması; fotoğrafsız haberin hiçbir durumda kaydedilmemesi; daha küçük video gösterimi ve videonun seçilen paragraf arasına konabilmesi; acil durumlar için 1280×720 Son Dakika görseli; Yayın Stüdyosu otomatik kaydının kaldırılması; değişikliklerin test edilip yayınlanması.
- Yapılanlar: Spot sınırı hem Yeni Haber hem Yayın Stüdyosu hem de sunucu doğrulamasında 500 karaktere çıkarıldı. Yeni habere otomatik fotoğraf atayan eski varsayılan kaldırıldı; kapak görseli eksikse taslak, inceleme, planlama ve doğrudan yayın işlemleri arayüzde ve API'de engelleniyor. ImageGen ile Koza TV'nin kırmızı-siyah yayın diline uygun, tam 1280×720 Son Dakika kapağı üretildi ve iki editörde tek düğmeyle seçilebilir hale getirildi. Ana video masaüstünde en çok 720 px, metin içi video en çok 640 px genişliğe indirildi; her paragraf/ara başlık/alıntı/liste bloğuna “Bu bloğun altına video ekle” işlemi kondu. Dört saniyelik otomatik kayıt tamamen kaldırıldı; sürüm yalnız açıkça **Sürümü kaydet** işlemiyle saklanıyor, kaydedilmemiş değişiklikte sekmeden/sayfadan çıkış uyarısı veriliyor.
- Değişen ana dosyalar: `db/article-model.mjs`, `app/admin/panel.tsx`, `app/admin/workflow-studio.tsx`, `app/globals.css`, `public/news/son-dakika-1280x720.png`, `tests/rendered-html.test.mjs`, `AGENTS.md`.
- Doğrulama: Yeni regresyonlar uygulamadan önce spot, kapak zorunluluğu ve editör sözleşmelerinde beklenen biçimde başarısız oldu; uygulamadan sonra `npm test` production build ile **77 test geçti; 0 başarısız, 0 atlandı**. `npx tsc --noEmit` ve bağımsız `npm run build` başarılı; `npm run lint` **0 hata/36 bilinen ham görsel uyarısı**, `npm audit --omit=dev` **0 production açığı** verdi. Gerçek tarayıcıda fotoğrafsız kaydın iki ayrı uyarıyla engellendiği, hazır görselle taslağın kaydolduğu, 4,5 saniyede otomatik kayıt oluşmadığı, manuel kaydın yeni sürüm oluşturduğu ve videonun seçilen paragrafın hemen altına eklendiği doğrulandı. Chrome 1280/900/390 px ölçümlerinde editör ve haber sayfasında yatay taşma yoktu; metin içi video genişliği sırasıyla 640/640/364 px ölçüldü.
- Kalan karar veya risk: Ham `<img>` performans uyarıları ayrı optimizasyon işidir. Son Dakika kapağı acil kullanım için güvenli bir yedektir; gerçek olay fotoğrafı bulunduğunda editörün doğru fotoğraf ve alt metinle değiştirmesi beklenir. Staging yayın sonucu ayrı kayıtla eklenecektir.

### 2026-09-10 — Editör ve manşet yanı iyileştirmelerinin staging dağıtımı

- İstek: Spot, zorunlu fotoğraf, kompakt/yerleştirilebilir video, manuel kayıt ve Son Dakika kapağı geliştirmeleri ile bekleyen manşet yanı sadeleştirmesinin test edilip staging ortamına yayınlanması.
- Yapılanlar: `96faba2` uygulama commit'i fast-forward olarak `main` branch'ine gönderildi. GitHub Actions temiz kurulum, production bağımlılık denetimi, test/build, lint, SSH aktarımı, sunucuda ikinci test, atomik sürüm değişimi ve dış smoke adımlarının tamamını başarıyla bitirdi.
- Değişen ana dosyalar: Dağıtılan uygulama sürümü `96faba229e96b571203b489a00bb33d1eea8bb77`; yayın kaydı için `AGENTS.md`.
- Doğrulama: GitHub Actions çalışması `34507434182` **1 dakika 59 saniyede başarılı** tamamlandı. Dış kontrolde `/`, `/kategori/gundem`, `/son-dakika`, `/canli`, `/yazarlar` ve `/admin/giris` HTTP 200; `/admin` HTTP 307 ve `/api/auth/me` HTTP 401 döndürdü. Yeni Son Dakika görseli staging'de HTTP 200 `image/png` yanıtıyla açıldı ve gerçek dosya ölçüsü **1280×720** doğrulandı. Ana sayfa sunucu HTML'inde manşet yanı alanı ve haber başlığı bulunurken kategori `span`ı, tarih `time`ı ve Son Dakika şeridi bulunmadı.
- Kalan karar veya risk: Yayın Hetzner staging IP'sinde HTTP çalışmaktadır; canlı marka alan adı/DNS/HTTPS geçişi bu kapsamda değildir. Ham `<img>` performans uyarıları ayrı optimizasyon işidir.

### 2026-09-09 — Haber editöründe seçili metni gerçek ara başlığa dönüştürme

- İstek: Haber metninde büyük harflerle yazılan ara başlıklara üst/alt boşluk verilememesi ve bu metinlerin siyah, kalın gösterilememesi.
- Yapılanlar: Basit **Yeni Haber** editörüne metin seçildikten sonra çalışan **Seçili metni ara başlık yap** işlemi eklendi. Seçim güvenli biçimde ayrı bir `##` ara başlık parçasına çevriliyor; kayıtta normal paragraf yerine gerçek başlık bloğu oluşturuluyor. Yayın Stüdyosu'ndaki paragraf bloklarına da **Seçileni ara başlık yap** işlemi eklendi; seçimin önündeki ve arkasındaki paragraf metni korunarak araya ayrı başlık bloğu yerleştiriliyor. Boş, çok kısa, 180 karakteri aşan, birden fazla satıra taşan veya paragraf dışındaki seçimler açıklayıcı biçimde reddediliyor; düzenleme yetkisi olmayan kullanıcıda işlem kapalı. Ziyaretçi haberinde ara başlıklar koyu siyah, 900 kalınlıkta ve masaüstünde 42/18 px, mobilde 34/15 px üst/alt boşlukla gösteriliyor.
- Değişen ana dosyalar: `db/article-model.mjs`, `db/index.ts`, `app/admin/panel.tsx`, `app/admin/workflow-studio.tsx`, `app/globals.css`, `tests/rendered-html.test.mjs`, `AGENTS.md`. Önceki tamamlanmış fakat henüz yayınlanmamış kesintisiz okuma açıklaması değişiklikleri korundu.
- Doğrulama: Arayüz regresyonu uygulamadan önce seçim düğmesi bulunmadığı için beklenen biçimde başarısız oldu. Uygulamadan sonra `npm test` production build ile **77 test geçti; 0 başarısız, 0 atlandı**. Yeni testler seçim bölme, düz metinden başlık bloğu üretme, paragraf öncesi/sonrası metni koruma, boş/taşan/çok satırlı/yetkisiz kullanımı reddetme ve gerçek haber HTML'inde `<h2>` üretimini doğruladı. `npx tsc --noEmit` ve bağımsız `npm run build` başarılı; `npm run lint` **0 hata**, mevcut ham `<img>` yüzeyleri için **36 bilinen performans uyarısı** ile tamamlandı. Ayrı geçici veritabanı ve gerçek tarayıcıda yayınlanan test haberinde başlık rengi `rgb(11, 17, 23)`, kalınlığı `900`, boşlukları masaüstünde `42/18 px`, mobilde `34/15 px` ölçüldü. 1280 px masaüstü, 900 px tablet ve 390 px mobilde yatay taşma olmadı; iki editörde işlem düğmeleri ve açıklamaları görünür doğrulandı.
- Kalan karar veya risk: Daha önce normal paragrafın içine yazılmış mevcut büyük harfli metinler yanlış alıntıları etkilememek için otomatik dönüştürülmez; ilgili haber editörde açılıp metin seçilerek bir kez ara başlığa çevrilmelidir. Değişiklikler henüz commit edilmedi, GitHub'a gönderilmedi ve Hetzner staging ortamına dağıtılmadı.

### 2026-09-09 — Ara başlık ve sade kesintisiz okuma sürümünün staging dağıtımı

- İstek: Gün içinde tamamlanan fakat henüz yayınlanmamış değişikliklerin tamamının commit edilip staging ortamına çıkarılması ve yapılanların numaralı bir özetle bildirilmesi.
- Yapılanlar: Kesintisiz okuma alanındaki gereksiz açıklama cümlesinin kaldırılması ile iki haber editöründeki seçili metni gerçek ara başlığa dönüştürme özelliği `f56f738` commit'iyle fast-forward olarak `main` branch'ine gönderildi. GitHub Actions temiz kurulum, production bağımlılık denetimi, test/build, lint, sunucu aktarımı, atomik sürüm değişimi ve dış smoke test adımlarının tamamını başarıyla bitirdi.
- Değişen ana dosyalar: Dağıtılan uygulama sürümü `f56f7387b0fa6c618eff9154a5c1cb8edc617a56`; yayın kaydı için `AGENTS.md`.
- Doğrulama: Yerelde production build ile **77 test geçti; 0 başarısız, 0 atlandı**; `npx tsc --noEmit` ve bağımsız `npm run build` başarılı, lint **0 hata/36 bilinen ham görsel uyarısı** verdi. GitHub Actions çalışması `34388752291` **1 dakika 52 saniyede başarılı** tamamlandı. Dış kontrolde `/`, `/kategori/gundem`, `/son-dakika`, `/canli`, `/yazarlar` ve `/admin/giris` HTTP 200; `/admin` HTTP 307 ve `/api/auth/me` HTTP 401 döndürdü. Gerçek staging haberinde istenmeyen açıklama bulunmadı, **KESİNTİSİZ OKUMA** akışı korundu; yayın CSS'inde ara başlıkların siyah `#0b1117`, `900` kalınlık ve masaüstünde `42/18 px`, mobilde `34/15 px` boşluk kuralları doğrulandı.
- Kalan karar veya risk: Yayın Hetzner staging IP'sinde HTTP çalışmaktadır; canlı marka alan adı/DNS/HTTPS geçişi bu kapsamda değildir. Daha önce paragraf içine yazılmış büyük harfli mevcut metinlerin editörde seçilip bir kez ara başlığa çevrilmesi gerekir. Ham `<img>` performans uyarıları ayrı optimizasyon işidir.

### 2026-09-10 — Manşet yanı haber kartlarının sadeleştirilmesi

- İstek: Ana sayfadaki manşet yanı haberlerde kategori etiketi ve tarihin kaldırılması.
- Yapılanlar: Manşet yanı haber kartlarından kategori, yayın tarihi ve etiket sayılan Son Dakika şeridi çıkarıldı; bu alanda yalnız haber görseli ile başlığı bırakıldı. Manşet altı ve diğer haber kartlarının mevcut bilgilerine dokunulmadı.
- Değişen ana dosyalar: `app/page.tsx`, `app/globals.css`, `tests/rendered-html.test.mjs`, `AGENTS.md`.
- Doğrulama: Yeni regresyon testi değişiklikten önce kategori ve tarih öğelerini yakalayarak beklenen biçimde başarısız oldu. Uygulamadan sonra `npm test` production build ile **77 test geçti; 0 başarısız, 0 atlandı**. `npx tsc --noEmit` ve bağımsız `npm run build` başarılı; `npm run lint` **0 hata**, mevcut ham `<img>` yüzeyleri için **36 bilinen performans uyarısı** ile tamamlandı. Gerçek tarayıcıda 1440, 900 ve 390 px görünümlerde manşet yanı alanının görünür olduğu, kartlarda `span`, `time` ve Son Dakika şeridi bulunmadığı, yalnız görsel ile başlığın kaldığı ve sayfada yatay taşma oluşmadığı doğrulandı.
- Kalan karar veya risk: Değişiklikler henüz commit edilmedi, GitHub'a gönderilmedi ve Hetzner staging ortamına dağıtılmadı.

### 2026-09-10 — Sürükle-bırak ana sayfa düzeni ve manşetten düşen haber koruması

- İstek: Yönetim panelinde manşet/slider, manşet yanı ve manşet altı haberlerinin fareyle taşınıp sıralanabildiği bir ekran kurulması; beşli sliderdan çıkan haberlerin ana sayfadan kaybolmak yerine Son Haberler bölümüne aktarılması.
- Yapılanlar: Yalnız yöneticiye açık **Ana Sayfa Düzeni** ekranı eklendi. Ekran **1. Haberi tut → 2. Yerini seç → 3. Düzeni kaydet** biçiminde üç kısa adımla sadeleştirildi; alan adları ziyaretçi yüzeyiyle eşleştirilip **Manşet yanı · Günün Akışı** açıkça belirtildi. Manşet, manşet yanı, manşet altı ve Son Haberler aynı ekranda gösteriliyor; haberler fare veya parmakla tutulup taşınabiliyor, klavye için sıra düğmeleri ve her kartta açık konum seçimi bulunuyor. Yanlış işlem **Değişiklikleri geri al** ile tek tıkta son kayıtlı düzene dönüyor. Son Haberler tarih akışını kendi içinde elle sıralayan gereksiz oklar kaldırıldı; bu havuzun doğal tarih sırası artık yanlışlıkla kaydedilmemiş değişiklik üretmiyor. Boş havuz ile sonuçsuz arama ayrı mesajlarla anlatılıyor. Mobil sabit işlem çubuğu yalnız iki gerekli düğmeye indirildi ve içeriği daha az kaplıyor. İlk tarayıcı denemesinde yerleşik HTML sürükleme ile Pointer Events çakışması yakalandı; fare ve dokunma tek Pointer Events mekanizmasına geçirilerek düzeltildi. Slider 5, manşet yanı 2, manşet altı 4 haberle sınırlandırıldı; dolu alana yeni haber geldiğinde son sıradaki kayıt otomatik olarak `latest` konumuna taşınıp Son Haberler'de gösteriliyor. Aynı koruma doğrudan yayın, iş akışından yayın, planlı yayının zamanı gelmesi ve uygulama başlangıcındaki mevcut fazla kayıtlar için veritabanı düzeyinde zorunlu hale getirildi. Değişiklikler otomatik kaydedilmiyor; yalnız **Düzeni kaydet** ile uygulanıyor. Kaydedilmemiş değişiklik uyarısı, iki yöneticinin eski ekranla birbirinin sırasını ezmesini önleyen sürüm çakışması, sunucu tarafı kapasite/tekrar/geçerli haber doğrulaması ve denetim kaydı eklendi.
- Değişen ana dosyalar: `db/homepage-order.mjs`, `db/index.ts`, `app/api/homepage-layout/route.ts`, `app/admin/homepage-layout.tsx`, `app/admin/homepage-layout-model.mjs`, `app/admin/panel.tsx`, `app/globals.css`, `tests/rendered-html.test.mjs`, `AGENTS.md`.
- Doğrulama: `npm test` production build ile **80 test geçti; 0 başarısız, 0 atlandı**. Yeni testler altıncı/yedinci manşetin Son Haberler'e inmesini, sıra 1–5'in korunmasını, manşet işaretinin kaldırılmasını, dolu slidera yeni haber taşındığında son haberin kaybolmamasını, kart çoğalmamasını, Son Haberler sırasının sahte değişiklik üretmemesini, kapasite/tekrar/geçersiz kimlik reddini, yalnız yönetici erişimini, 401/403/400/409 durumlarını, açık kaydetmeyi ve test sonunda ilk düzenin geri yüklenmesini kapsıyor. `npx tsc --noEmit` ve production build başarılı; `npm run lint` **0 hata**, mevcut ham görsellerle birlikte **37 performans uyarısıyla** tamamlandı; `npm audit --omit=dev` **0 production açığı** bildirdi. İzole test verisi ve gerçek Chromium motoruyla 1440 px masaüstü, 900 px tablet ve 390 px mobil ekran kontrol edildi: yerleşim sırasıyla 3/2/1 sütun, mobilde `documentWidth == viewportWidth`, Son Haberler'de sıra oku sayısı sıfır ve sabit kaydetme işlemi görünür. Gerçek fareyle sliderdan manşet altına taşıma, parmak hareketiyle alan değiştirme, klavyede Enter ile sıra değiştirme ve geri alma çalıştı. Arayüzde konum değiştirildikten sonra API/veritabanı sırasının kaydetme öncesi değişmediği, **Düzeni kaydet** sonrasında değiştiği doğrulandı; konsol hatası oluşmadı.
- Kalan karar veya risk: Son Haberler havuzunda performans için en güncel 60 yayın gösterilir; daha eski kayıtların ana sayfa konumu haber editöründen değiştirilebilir. Ham `<img>` uyarıları ayrı görsel/CDN optimizasyon işidir. Staging dağıtım sonucu aşağıdaki yayın kaydında yer alır.

### 2026-09-10 — Basit ana sayfa yerleşiminin staging dağıtımı

- İstek: Fare, parmak ve klavyeyle kolay kullanılan Ana Sayfa Düzeni ekranının; manşetten çıkan haberleri Son Haberler'e aktaran kapasite korumasıyla birlikte canlı sunucuya gönderilmesi.
- Yapılanlar: `580ddc0` uygulama commit'i fast-forward olarak `main` branch'ine gönderildi. GitHub Actions temiz kurulum, production bağımlılık denetimi, test/build, lint, SSH aktarımı, sunucuda ikinci test, atomik sürüm değişimi ve dış smoke test adımlarının tamamını başarıyla bitirdi.
- Değişen ana dosyalar: Dağıtılan uygulama sürümü `580ddc028a947689ad96861c9d3d41f3b28a32b6`; yayın kaydı için `AGENTS.md`.
- Doğrulama: GitHub Actions çalışması `34517513826` **2 dakika 7 saniyede başarılı** tamamlandı. Yerel ve dağıtım hattında production build ile **80 test geçti; 0 başarısız, 0 atlandı**; typecheck başarılı, lint **0 hata/37 bilinen ham görsel uyarısı**, production bağımlılık açığı **0**. Dış kontrolde `/`, `/kategori/gundem`, `/son-dakika`, `/canli`, `/yazarlar` ve `/admin/giris` HTTP 200; `/admin` HTTP 307 ve oturumsuz `/api/homepage-layout` HTTP 401 döndürdü.
- Kalan karar veya risk: Yayın Hetzner staging IP'sinde HTTP çalışmaktadır; canlı marka alan adı/DNS/HTTPS geçişi bu kapsamda değildir. Ana Sayfa Düzeni yalnız yönetici rolüne açıktır. Ham `<img>` performans uyarıları ayrı optimizasyon işidir.

### 2026-09-10 — Son Haberler boş sıra düzeltmesi

- İstek: Ana sayfa Son Haberler alanında PISA haberinin yanındaki iki hücrenin, yeni haber eklenmesine rağmen boş kalmasının nedeninin bulunması ve bu alanın daha fazla haber gösterebilmesi.
- Yapılanlar: Kök nedenin Son Haberler görünür listesindeki sabit **8 haber** sınırı olduğu doğrulandı. İlk kart üç sütunu kapladığından kalan yedi kart `3 + 3 + 1` diziliyor ve son satırda iki hücre boş kalıyordu. Görünür sınır **10 habere** çıkarıldı; liste doğrudan `latest` ana sayfa konumundan, yayın tarihine göre okunuyor. Böylece geniş ilk karttan sonra dokuz kart üç tam sıra oluşturuyor. Ana Sayfa Düzeni ekranına ziyaretçi ana sayfasında en güncel 10 haberin göründüğü, daha eski yayınların **Tümünü Gör** sayfasında kaldığı açıkça yazıldı.
- Değişen ana dosyalar: `app/page.tsx`, `app/admin/homepage-layout.tsx`, `tests/rendered-html.test.mjs`, `AGENTS.md`.
- Doğrulama: Yeni regresyon testi düzeltme öncesinde eski sekiz haber sınırını yakalayarak beklenen biçimde başarısız oldu. Uygulamadan sonra `npm test` production build ile **81 test geçti; 0 başarısız, 0 atlandı**. `npx tsc --noEmit` başarılı; `npm run lint` **0 hata**, mevcut ham `<img>` yüzeyleri için **37 bilinen performans uyarısı** ile tamamlandı. Gerçek tarayıcıda 1440 px masaüstünde 10 kart ve son üç kartın aynı sırada yan yana olduğu; 900 px tablette ve 390 px mobilde 10 kartın korunduğu, yatay taşma ve kırık görsel bulunmadığı doğrulandı.
- Kalan karar veya risk: Ziyaretçi ana sayfasında performans ve sayfa uzunluğu için en güncel 10 Son Haberler kaydı gösterilir; daha eski tüm haberler kategori ve **Tümünü Gör** akışından erişilebilir. Değişiklikler henüz commit edilmedi, GitHub'a gönderilmedi ve Hetzner staging ortamına dağıtılmadı.

### 2026-09-10 — Ana sayfa haber kartlarından tarih ve saatlerin kaldırılması

- İstek: Ana sayfadaki haber kartlarının başlıkları altında görünen bütün tarih ve saat bilgilerinin kaldırılması.
- Yapılanlar: Manşet altı ve Son Haberler kartlarının altındaki yayın tarihi/saat satırları kaldırıldı. Ana manşete artık kullanılmayan yayın zamanı verisi gönderilmiyor. Son Dakika ve Günün Akışı içindeki kronolojik akış saatleri, haber kartı alt bilgisi olmadığı ve akış sırasını anlattığı için korundu.
- Değişen ana dosyalar: `app/page.tsx`, `tests/rendered-html.test.mjs`, `AGENTS.md`.
- Doğrulama: Yeni regresyon testi uygulamadan önce kart altındaki tarih etiketini yakalayarak beklenen biçimde başarısız oldu. Uygulamadan sonra `npm test` production build ile **82 test geçti; 0 başarısız, 0 atlandı**. `npx tsc --noEmit` başarılı; `npm run lint` **0 hata**, mevcut ham `<img>` yüzeyleri için **37 bilinen performans uyarısı** ile tamamlandı. Gerçek tarayıcıda 1440, 900 ve 390 px görünümlerde Manşet altı ve Son Haberler kartlarındaki `<time>` sayısı sıfır, yatay taşma ve kırık görsel sıfır, konsol hatası sıfır olarak doğrulandı; Günün Akışı'nın altı kronolojik saati korundu.
- Kalan karar veya risk: Değişiklikler henüz commit edilmedi, GitHub'a gönderilmedi ve Hetzner staging ortamına dağıtılmadı. Ham `<img>` performans uyarıları ayrı optimizasyon işidir.

### 2026-09-10 — Kompakt mobil manşet, dörderli Son Haberler ve Foto Galeri

- İstek: Mobil manşetin fotoğraf altındaki aşırı büyük siyah alanının küçültülmesi; üst menüdeki Video/Videolar ve Yazarlar bağlantılarının kaldırılıp tek **Video Merkezi** bağlantısı bırakılması; ana sayfa Son Haberler yanındaki koyu Canlı Akış kutusunun kaldırılması; Son Haberler'in dörderli kartlarla sunulması; ana haberin yanına ve üst menüye MedyaTava benzeri fakat Koza TV'ye özgü bir Foto Galeri eklenmesi ve tamamının staging ortamına gönderilmesi.
- Yapılanlar: Mobil manşet yüksekliği 390 px ekranda 530 px'ten yaklaşık 353 px'e, fotoğraf altındaki koyu başlık/kontrol alanı yaklaşık 157 px'e indirildi. Masaüstü Son Haberler yapısı bir ana haber ile 12 karttan oluşacak biçimde 13 habere çıkarıldı; 12 kart dörderli üç sıra halinde yerleşiyor. Eski koyu **Canlı Akış / Son Dakika** yan sütunu tamamen kaldırıldı. Ana haberin yanına üç görsel hikâyelik kompakt Koza TV Foto Galeri vitrini yerleştirildi. `/foto-galeri` liste ve `/foto-galeri/[slug]` ayrıntı sayfaları; çoklu haber görsellerini sıralı gösterim, fotoğraf sayacı, haber bağlantısı, mobil düzen, metadata ve sitemap kayıtlarıyla eklendi. Yer tutucu veya tekrarlanan görseller galeriye alınmıyor. Üst ve mobil menüden eski Yazarlar, Video ve Videolar öğeleri kaldırıldı; **Foto Galeri** ile tek **Video Merkezi** bağlantısı bırakıldı. Ana Sayfa Düzeni açıklaması 1 ana + 12 kart sınırını açıkça anlatacak biçimde güncellendi.
- Değişen ana dosyalar: `app/page.tsx`, `app/site-chrome.tsx`, `app/site-client.tsx`, `app/sitemap.ts`, `app/foto-galeri/*`, `app/admin/homepage-layout.tsx`, `db/photo-gallery-model.mjs`, `db/index.ts`, `app/globals.css`, `tests/rendered-html.test.mjs`, `AGENTS.md`.
- Doğrulama: `npm test` production build ile **83 test geçti; 0 başarısız, 0 atlandı**. Yeni regresyonlar galeri görsel seçimi/tekrar/yer tutucu davranışını, liste ve ayrıntı rotalarını, 404'ü, sitemap'i, menü sadeleştirmesini, kaldırılan Canlı Akış sütununu, 13 haber sınırını, dörderli masaüstü ızgarasını, kart tarihlerinin kaldırılmasını ve kompakt mobil manşet sözleşmesini kapsıyor. `npx tsc --noEmit` başarılı; `npm run lint` **0 hata**, yeni galeri görselleri dahil **43 ham `<img>` performans uyarısı** ile tamamlandı; `npm audit --omit=dev` **0 production açığı** bildirdi. Gerçek Chrome'da 1512 px masaüstünde Son Haberler dört sütun, ana haber yanında Foto Galeri ve kaldırılmış canlı akış; 992 px tablette iki sütun; 390 px mobilde 353 px manşet, tek sütun, görünür galeri ve sıfır yatay taşma doğrulandı. Foto Galeri masaüstünde üç, mobilde tek sütun; ayrıntıda üç sıralı fotoğraf ve haber bağlantısı; bütün kontrollerde kırık görsel sayısı sıfırdı.
- Kalan karar veya risk: Mevcut haberlerin her biri kapak fotoğrafıyla galeride yer alabilir; habere içerik görsel blokları eklendiğinde aynı galeri otomatik olarak çok fotoğraflı olur. Ham `<img>` uyarıları ayrı Next Image/CDN optimizasyon işidir. Staging dağıtım sonucu sonraki yayın kaydında yer alacaktır.

### 2026-09-10 — Yeni Son Haberler ve Foto Galeri sürümünün staging dağıtımı

- İstek: Kompakt mobil manşetin, sade üst menünün, dörderli Son Haberler düzeninin ve yeni Foto Galeri deneyiminin test edilip canlı staging sunucusuna gönderilmesi.
- Yapılanlar: `c53a626` uygulama commit'i fast-forward olarak `main` branch'ine gönderildi. GitHub Actions temiz kurulum, production bağımlılık denetimi, test/build, lint, SSH aktarımı, sunucuda ikinci test, atomik sürüm değişimi ve dış smoke adımlarının tamamını başarıyla bitirdi.
- Değişen ana dosyalar: Dağıtılan uygulama sürümü `c53a626c56472ffdca5a406c1ca7bd003c355fb2`; yayın kaydı için `AGENTS.md`.
- Doğrulama: GitHub Actions çalışması `34527150030` **2 dakika 5 saniyede başarılı** tamamlandı. Dağıtım hattında production build ile **83 test geçti; 0 başarısız, 0 atlandı**. Dış kontrolde `/`, `/foto-galeri`, bir galeri ayrıntısı, `/videolar` ve `/admin/giris` HTTP 200; `/admin` HTTP 307 ve `/api/auth/me` HTTP 401 döndürdü. Staging ana sayfasında 12 küçük Son Haberler kartı dörderli üç sıra, yanında Foto Galeri, kaldırılmış koyu canlı akış ve yalnız **Foto Galeri / Video Merkezi** üst menü bağlantıları doğrulandı. Gerçek staging Chrome kontrolünde 1512 px masaüstünde dört sütun; 390 px mobilde 353 px manşet, tek sütun ve sıfır yatay taşma; galeri indeksinde 23 kart, aktif menü durumu ve sıfır kırık görsel görüldü.
- Kalan karar veya risk: Yayın Hetzner staging IP'sinde HTTP çalışmaktadır; canlı marka alan adı/DNS/HTTPS geçişi bu kapsamda değildir. Galeriye özel kapak ve sıralama yönetimi istenirse daha sonra Ana Sayfa Düzeni benzeri ayrı bir editör ekranı eklenebilir. Ham `<img>` performans uyarıları ayrı optimizasyon işidir.

### 2026-09-11 — Foto Galeri fotoğraf sayacının kaldırılması

- İstek: Foto Galeri kartlarının görselleri üzerinde kırmızı kutu içinde gösterilen **1 FOTOĞRAF** benzeri fotoğraf sayılarının kaldırılması.
- Yapılanlar: Foto Galeri liste sayfasındaki büyük ve küçük kartlardan, ana sayfadaki Foto Galeri vitrininden ve galeri ayrıntı başlığından metinsel fotoğraf sayıları kaldırıldı. Görseller, galeri bağlantıları ve ayrıntı içindeki sıralı kare göstergesi korundu. Artık kart görselinin üzerinde kırmızı sayaç bulunmuyor.
- Değişen ana dosyalar: `app/foto-galeri/page.tsx`, `app/foto-galeri/[slug]/page.tsx`, `app/page.tsx`, `app/globals.css`, `tests/rendered-html.test.mjs`, `AGENTS.md`.
- Doğrulama: `npm test` production build ile **83 test geçti; 0 başarısız, 0 atlandı**. Yeni regresyon kontrolleri Foto Galeri liste, ayrıntı ve ana sayfa vitrininde `N FOTOĞRAF` metninin yeniden görünmemesini doğruluyor. `npm run lint` **0 hata**, mevcut ham `<img>` yüzeyleri için **43 bilinen performans uyarısıyla** tamamlandı. Gerçek Chrome motorunda 1440 px masaüstü, 900 px tablet ve 390 px mobil Foto Galeri görünümleri incelendi; kırmızı fotoğraf sayacı üç görünümde de yok.
- Kalan karar veya risk: Değişiklikler henüz commit edilmedi, GitHub'a gönderilmedi ve Hetzner staging ortamına dağıtılmadı. Ham `<img>` performans uyarıları ayrı optimizasyon işidir.

### 2026-09-11 — Ana sayfada boşluksuz Foto Galeri mozaiği

- İstek: Ana sayfadaki Foto Galeri alanında oluşan büyük koyu boşluğun kaldırılması; farklı boyutlarda görseller kullanan, daha şık ve tasarım kurallarına uygun bir görsel vitrin hazırlanması.
- Yapılanlar: Foto Galeri vitrini tam **üç haber** gösterecek biçimde düzenlendi: üstte bir büyük odak haberi, altında yan yana iki küçük haber bulunuyor. Büyük görsel 16:9 çerçevede `object-fit: contain` ile gösterildiği için fotoğrafın hiçbir bölümü kesilmiyor; farklı oranlı kaynaklarda da görselin tamamı korunuyor. Haber başlıkları ve kategoriler kontrollü koyu geçiş üzerinde okunur biçimde yerleştirildi; ana kareye sade **Galeriyi aç** yönlendirmesi ve galeri simgesi eklendi. Yalnız bir veya iki galeri bulunduğunda boş hücre oluşmasını engelleyen güvenli düzenler korundu.
- Değişen ana dosyalar: `app/page.tsx`, `app/globals.css`, `tests/rendered-html.test.mjs`, `AGENTS.md`.
- Doğrulama: Yeni mozaik regresyonu önce eski kırpan yerleşimi yakalayarak beklendiği gibi başarısız oldu; düzeltmeden sonra `npm test` production build ile **83 test geçti; 0 başarısız, 0 atlandı**. `npx tsc --noEmit` başarılı; `npm run lint` **0 hata**, mevcut ham `<img>` yüzeyleri için **43 bilinen performans uyarısıyla** tamamlandı. Üç galerili ayrı test verisiyle gerçek Chrome motorunda 1440 px masaüstü, 900 px tablet ve 390 px mobil ana sayfa görsel olarak incelendi; büyük görselin tamamı kesilmeden göründü, iki küçük haber altında yer aldı, boş koyu alan, fotoğraf sayacı, kırık görsel ve yatay taşma oluşmadı.
- Kalan karar veya risk: Değişiklikler henüz commit edilmedi, GitHub'a gönderilmedi ve Hetzner staging ortamına dağıtılmadı. Ham `<img>` performans uyarıları ayrı optimizasyon işidir.

### 2026-09-11 — Referans uyumlu kompakt Foto Galeri kolajı

- İstek: İlk üçlü mozaikteki büyük, ayrı **Foto Galeri** başlık bloğunun ve gereksiz koyu alanın kaldırılması; verilen asimetrik kolaj referansları gibi farklı boyutlu görsellerden oluşan daha şık ve kompakt bir düzen hazırlanması.
- Yapılanlar: Ayrı yer kaplayan başlık satırı kaldırıldı; **Foto Galeri** adı ile **Tümünü gör** bağlantısı ana görselin üstünde küçük ve yarı saydam bir katmana dönüştürüldü. Vitrin üç haberle sınırlandı: 16:9 oranında ve kırpılmadan gösterilen ana fotoğrafın altında iki daha kısa yatay görsel bulunuyor. Alt başlıklar iki satırla sınırlandı, büyük başlık ve iç boşluklar küçültüldü; ana fotoğrafta hover büyütmesi kaldırılarak fare üzerine gelince de görselin kesilmemesi sağlandı.
- Değişen ana dosyalar: `app/globals.css`, `tests/rendered-html.test.mjs`, `AGENTS.md`.
- Doğrulama: Yeni regresyon testi önce ayrı başlık alanını ve uzun alt kareleri yakalayarak beklendiği gibi başarısız oldu. Uygulamadan sonra `npm test` production build ile **83 test geçti; 0 başarısız, 0 atlandı**. Üç galerili ayrı test verisiyle gerçek Chrome motorunda 1440 px masaüstü, 900 px tablet ve 390 px mobil ana sayfa incelendi; başlık için ayrılmış boş satır kalmadı, ana görselin tamamı göründü, iki kısa görsel altta farklı ölçüde yer aldı ve yatay taşma oluşmadı.
- Kalan karar veya risk: Son düzeltme henüz commit edilmedi ve staging ortamına dağıtılmadı. Ham `<img>` performans uyarıları ayrı optimizasyon işidir.

### 2026-09-11 — Kompakt Foto Galeri kolajının staging dağıtımı

- İstek: Verilen asimetrik görsel kolaj referanslarına göre küçültülen Foto Galeri vitrininin staging ortamına gönderilmesi.
- Yapılanlar: Fotoğraf sayaçlarını kaldıran `f983ff9` sürümünün ardından ayrı başlık boşluğunu kaldırıp başlığı görsele bindiren, ana fotoğrafı kırpmayan ve iki alt görseli kısa mozaik parçalarına dönüştüren `8658412` düzeltmesi fast-forward olarak `main` branch'ine gönderildi. GitHub Actions temiz kurulum, production bağımlılık denetimi, test/build, lint, sunucu aktarımı, atomik sürüm değişimi ve dış smoke adımlarını başarıyla tamamladı.
- Değişen ana dosyalar: Dağıtılan son uygulama sürümü `86584123dc3aa392f9f20bcd2948e3c830a39426`; yayın kaydı için `AGENTS.md`.
- Doğrulama: GitHub Actions çalışması `34569747505` **2 dakika 3 saniyede başarıyla** tamamlandı. Production build ile **83 test geçti; 0 başarısız, 0 atlandı**; typecheck başarılı, lint **0 hata/43 bilinen ham görsel uyarısı** verdi. Dış kontrolde `/`, `/foto-galeri` ve `/admin/giris` HTTP 200, `/admin` HTTP 307 döndürdü. Staging HTML/CSS kontrolünde üç galeri bağlantısı, sıfır fotoğraf sayacı, görsel üzerine bindirilmiş başlık, 16:7 kısa alt kareler ve ana fotoğrafta `object-fit:contain` doğrulandı.
- Kalan karar veya risk: Yayın Hetzner staging IP'sinde HTTP çalışmaktadır; canlı marka alan adı/DNS/HTTPS geçişi bu kapsamda değildir. Ham `<img>` performans uyarıları ayrı optimizasyon işidir.

### 2026-09-11 — Foto Galeri vitrininin dikey ortalanması

- İstek: Ana sayfadaki Foto Galeri vitrininin yanındaki yüksek ana haber kartı nedeniyle üstte kalmaması; kendi sütununda üst ve alt boşluğu dengeli biçimde ortalanması ve alttaki iki fotoğrafın biraz büyütülmesi.
- Yapılanlar: Foto Galeri kutusu masaüstü Son Haberler satırında `align-self:center` ile kendi sütununun dikey merkezine alındı. 1051 px üzerindeki geniş ekranlarda üst ve alta eşit, duyarlı dış boşluk eklendi; böylece boşluk yalnız vitrinin altında toplanmıyor. Alttaki iki görselin oranı `16/7`den `2/1`e çevrilerek yaklaşık yüzde 14 daha yüksek ve daha okunaklı hale getirildi. Tablet ve mobilde galeri doğal tek sütun akışında bırakıldı.
- Değişen ana dosyalar: `app/globals.css`, `tests/rendered-html.test.mjs`, `AGENTS.md`.
- Doğrulama: Regresyon testi değişiklikten önce eski `align-self:start` kuralını yakalayarak beklendiği gibi başarısız oldu; düzeltmeden sonra production build ile `npm test` **83 test geçti; 0 başarısız, 0 atlandı**. `npx tsc --noEmit` başarılı; `npm run lint` **0 hata**, mevcut ham `<img>` yüzeyleri için **43 bilinen performans uyarısı** ile tamamlandı. Gerçek tarayıcı motorunda 1280 px masaüstünde galeri satırının üst ve alt boşluğu ayrı ayrı **34 px**, alt iki görsel **221×111 px** ölçüldü. 900 px tablet ve 390 px mobil görünümler görsel olarak incelendi; galeri doğal akışta kaldı ve her iki görünümde de yatay taşma **0 px** oldu.
- Kalan karar veya risk: Değişiklik staging ortamına gönderilmek üzere commit kapsamındadır. Ham `<img>` performans uyarıları ayrı Next Image/CDN optimizasyon işidir.

### 2026-09-11 — Ortalanmış Foto Galeri vitrininin staging dağıtımı

- İstek: Foto Galeri kutusunun dikey ortalama ve büyütülmüş alt görsel düzeltmesinin staging ortamına gönderilmesi.
- Yapılanlar: `b580a87` uygulama commit'i fast-forward olarak `main` branch'ine gönderildi. GitHub Actions temiz kurulum, production bağımlılık denetimi, test/build, lint, SSH aktarımı, sunucuda ikinci test, atomik sürüm değişimi ve dış smoke adımlarını başarıyla tamamladı.
- Değişen ana dosyalar: Dağıtılan uygulama sürümü `b580a8702f28a7c1e478d83bc0f32d7cfa5211d3`; yayın kaydı için `AGENTS.md`.
- Doğrulama: GitHub Actions çalışması `34571316130` **2 dakika 6 saniyede başarılı** tamamlandı. Dış kontrolde `/`, `/foto-galeri` ve `/admin/giris` HTTP 200, `/admin` HTTP 307 döndürdü. Staging CSS'inde `align-self:center`, masaüstü için dengeli `margin-block` ve alt iki görsel için `aspect-ratio:2` doğrulandı. Gerçek staging tarayıcısında galeri alt görsellerinin büyüdüğü ve 992 px tablet görünümünde yatay taşma oluşmadığı görüldü.
- Kalan karar veya risk: Yayın Hetzner staging IP'sinde HTTP çalışmaktadır; canlı marka alan adı/DNS/HTTPS geçişi bu kapsamda değildir. Ham `<img>` performans uyarıları ayrı optimizasyon işidir.

### 2026-09-11 — Ana sayfaya özel Foto Galeri seçimi ve duyarlı manşet yazısı

- İstek: Yeni Haber ekranına varsayılan kapalı bir Foto Galeri seçimi eklenmesi; seçimin yalnız ana sayfadaki Foto Galeri mozaiğini etkilemesi; ayrı `/foto-galeri` sayfası ve galeri detaylarının aynı biçimde çalışmaya devam etmesi; sliderdaki beyaz başlığın masaüstünde 45 px ve sans-serif olması, mobil görünümün bozulmaması.
- Yapılanlar: Haber modeline ve SQLite şemasına `is_homepage_gallery` alanı eklendi. **Ana sayfada Foto Galeri’de göster** kutusu Yeni Haber ve Yayın Stüdyosu ekranlarında yalnız ana sayfa vitrini olarak açıklandı; yeni haberde kapalı başlıyor, kaydediliyor ve sonradan kaldırılabiliyor. Ana sayfa yalnız işaretli ve gerçek görselli yayınları seçerken `/foto-galeri` listesi ile `/foto-galeri/[slug]` ayrıntıları bu işaretten tamamen bağımsız bırakıldı. Slider başlığı masaüstünde açık `font-size:45px` ve `font-family:sans-serif` kuralına geçirildi; 760 px altında 27 px, 500 px altında 22 px duyarlı kuralları korundu.
- Değişen ana dosyalar: `app/admin/panel.tsx`, `app/admin/workflow-studio.tsx`, `app/page.tsx`, `app/globals.css`, `db/article-model.mjs`, `db/index.ts`, `db/photo-gallery-model.mjs`, `db/seed.ts`, `tests/rendered-html.test.mjs`, `AGENTS.md`.
- Doğrulama: Yeni regresyonlar varsayılan kapalı kutuyu, SQLite kalıcılığını, işaretli haberin ana sayfa vitrinine girişini, işaret kaldırıldığında yalnız ana sayfadan çıkmasını ve bağımsız Foto Galeri sayfası/detayının korunmasını doğruladı. `npm test` production build ile **83 test geçti; 0 başarısız, 0 atlandı**. `npx tsc --noEmit` başarılı; `npm run lint` **0 hata**, mevcut ham `<img>` yüzeyleri için **43 bilinen performans uyarısı** ile tamamlandı. Gerçek Chrome motorunda başlık 1280 ve 900 px görünümde **45 px / sans-serif**, 390 px mobilde **22 px / sans-serif** ölçüldü; üç görünümde de `scrollWidth == clientWidth`. Yönetim panelinde kutunun yeni haberde kapalı başladığı, tıklanınca açıldığı ve **Yeni Haber** işlemiyle yeniden kapalı duruma döndüğü doğrulandı.
- Kalan karar veya risk: Mevcut üretim haberleri yeni alanda varsayılan olarak seçili değildir; ana sayfa Foto Galeri mozaiğinde gösterilmesi istenen yayınları editör açıkça işaretlemelidir. Değişiklikler bu kullanıcı isteğiyle commit ve staging dağıtımı kapsamına alındı. Ham `<img>` performans uyarıları ayrı optimizasyon işidir.

### 2026-09-11 — Günün Akışı ve imza vitrininin ana sayfadan kaldırılması

- İstek: Ana sayfadaki **Günün Akışı** ile **Köşe Yazarları & Haber Servisleri** bölümlerinin şimdilik kaldırılması.
- Yapılanlar: İki bölümün ana sayfa JSX'i kaldırıldı; artık kullanılmayan yazar sorgusu ve yalnız bu vitrine ait yardımcılar ana sayfa sunucu yükünden çıkarıldı. Yazar kayıtları, yazar arşivi ve `/yazarlar` sayfası silinmedi. Foto Galeri ile Video Merkezi bölümleri aynı veri ve tasarımla korunuyor.
- Değişen ana dosyalar: `app/page.tsx`, `tests/rendered-html.test.mjs`, `AGENTS.md`.
- Doğrulama: Yeni regresyon beklentisi önce eski derlemedeki **Günün Akışı** alanını yakalayarak başarısız oldu; uygulamadan sonra `npm test` production build ile **83 test geçti; 0 başarısız, 0 atlandı**. `npx tsc --noEmit` başarılı; `npm run lint` **0 hata**, mevcut ham `<img>` yüzeyleri için **43 bilinen performans uyarısı** ile tamamlandı. Gerçek Chrome motorunda 1280 px masaüstü, 900 px tablet ve 390 px mobil ana sayfa kontrol edildi; iki kaldırılan bölümün DOM sayısı sıfır, Foto Galeri ve Video Merkezi birer adet, her üç görünümde yatay taşma sıfırdı. Galeriden videoya geçiş aralığı masaüstünde 188 px, tablet ve mobilde 154 px ölçülerek düzenin kırılmadığı doğrulandı.
- Kalan karar veya risk: Bölümler yalnız ana sayfa görünümünden kaldırıldı; tekrar istenirse verileri kaybetmeden geri getirilebilir. Değişiklikler bu kullanıcı isteğiyle commit ve staging dağıtımı kapsamına alındı. Ham `<img>` performans uyarıları ayrı optimizasyon işidir.

### 2026-09-11 — Üç manşet alanından Son Haberler'e kalıcı otomatik sıra

- İstek: Slider, manşet yanı ve manşet altına yeni haber eklenince görünür kapasitenin dışına çıkan haberlerin kaybolmadan, düşme sırasıyla Son Haberler'in başına alınması.
- Yapılanlar: İlk kök neden, taşan haberin `latest` konumuna geçirilmesine rağmen eski yayın tarihi ve eski konum sırasıyla listelenmesiydi; eski haber ziyaretçideki 13 ve paneldeki 60 kayıt sınırının dışında kalıyordu. Yayın tarihinden bağımsız `homepage_latest_at` giriş sırası ve indeksleri eklendi. En son düşen haber ilk sıraya gelir, önceki girişler sırayla aşağı iner; aynı milisaniyedeki işlemler de kalıcı saat sayacıyla sıralanır. Slider 5, manşet yanı 2, manşet altı 4 kapasitesi; doğrudan yayın, editoryal yayın, zamanı gelen planlı yayın, ana sayfa düzeni kaydı, eski vitrin eşitlemesi ve başlangıçta fazla kalmış kayıtlar için aynı kuyrukla uygulanır. Panel ve ziyaretçi aynı sıralama kuralını kullanır. Bir kayıtta yapılan birden fazla taşımanın önizleme sırası sunucuya iletilir; yalnız gerçek çıkışlar sıraya eklenir. Metin düzeltme ve aynı düzeni tekrar işleme giriş sırasını yenilemez. Otomatik düşmeler denetim kaydına yazılır; düşen kaydın sürümü artırılarak açık eski editör formunun konumu geri yazması engellenir. İşlemler transaction içinde tamamlanır. İkinci kök neden, açılır listeden dolu alana seçilen haberin sona eklenip kendisini dışarı atmasıydı; varsayılan giriş birinci sıraya, dolu alanın sonuna bırakma ise son görünür sıraya düzeltildi.
- Değişen ana dosyalar: `db/homepage-latest.mjs`, `db/homepage-order.mjs`, `db/index.ts`, `app/admin/homepage-layout-model.mjs`, `app/admin/homepage-layout.tsx`, `tests/rendered-html.test.mjs`, `AGENTS.md`.
- Doğrulama: Eski tarihle kaybolma regresyonu önce başarısız oldu (beklenen düşen haber yerine yeni tarihli başka kayıt geldi); açılır liste regresyonu da düzeltmeden önce başarısız oldu. Düzeltmelerden sonra `npm test` production build ile **86 test geçti; 0 başarısız, 0 atlandı**. Yeni kapsam 2020 tarihli manşetlerin 65 yeni haberin önüne düşmesini, üç alanı ve art arda düşmeleri, aynı milisaniyeyi, yayın tarihinin korunmasını, taslak/gelecek yayın engelini, planlı ve editoryal yayını, çoklu taşımanın kaydını, tekrar çalıştırılabilir şema/başlangıç düzeltmesini, eski vitrin eşitlemesini, rollback'i ve 400/401/409 reddinde sıranın korunmasını kapsar. `npx tsc --noEmit` ve bağımsız `npm run build` başarılı; `npm run lint` **0 hata/43 mevcut ham görsel uyarısı** verdi. İlk lint çalışmasında test regex'indeki gereksiz kaçış bulundu ve giderildi. Ayrı yerel veritabanında gerçek uygulama tarayıcısıyla 1440, 900 ve 390 px ziyaretçi/panel görünümleri kontrol edildi: 13 Son Haberler kartı, doğru ilk üç kayıt, sıfır yatay taşma ve sıfır konsol hatası. Mobil panelde açılır listeden dolu slidera gerçek seçim yapıldı; kayıt düğmesi etkinleşti, kayıttan sonra önceki beşinci haber Son Haberler'in ilk sırasında kaldı ve ziyaretçi bağlantısıyla açıldı. Test sunucusu yeniden başlatıldığında giriş sırası korundu. Ana sayfa, Gündem, Son Dakika, canlı yayın, yazarlar, admin giriş ve düşen haber rotaları HTTP 200; `/admin` 307; oturumsuz yerleşim/oturum uçları 401 döndürdü. Haber metadata'sında özgün 2020 yayın tarihi korundu.
- Kalan karar veya risk: Değişiklikler commit edilmedi, push edilmedi ve staging ortamına dağıtılmadı. Yeni alan mevcut haberleri silmez; önceden düşmüş kayıtların tutulmamış düşme zamanını tahmin etmez, bundan sonraki girişlerin sırasını kalıcı kaydeder. Son Haberler'in görünür sınırı 13 olarak korunur. Ham `<img>` uyarıları ayrı optimizasyon işidir.

### 2026-09-11 — Ana sayfa ve Foto Galeri düzenlemelerinin staging dağıtımı

- İstek: Ana sayfaya özel Foto Galeri seçiminin, duyarlı manşet yazısının ve ana sayfadan kaldırılan Günün Akışı/imza vitrininin staging ortamına gönderilmesi.
- Yapılanlar: `075b246` uygulama commit'i fast-forward olarak `main` branch'ine gönderildi. GitHub Actions temiz kurulum, production bağımlılık denetimi, test/build, lint, SSH aktarımı, sunucuda ikinci test, atomik sürüm değişimi ve dış smoke adımlarının tamamını başarıyla bitirdi.
- Değişen ana dosyalar: Dağıtılan uygulama sürümü `075b246eafb8fb52dc1aee284c3a3ada0fc4a6da`; yayın kaydı için `AGENTS.md`.
- Doğrulama: GitHub Actions çalışması `34579585900` **2 dakika 6 saniyede başarıyla** tamamlandı. Production build ile **83 test geçti; 0 başarısız, 0 atlandı**; typecheck başarılı, lint **0 hata/43 bilinen ham görsel uyarısı** verdi. Dış kontrolde `/`, `/foto-galeri`, `/videolar` ve `/admin/giris` HTTP 200; `/admin` HTTP 307 ve `/api/auth/me` HTTP 401 döndürdü. Gerçek staging tarayıcısında 1280 px masaüstü ve 390 px mobilde Günün Akışı ile imza vitrini DOM sayısı sıfır, Video Merkezi bir adet ve sayfa yatay taşması sıfırdı. Ana sayfa Foto Galeri alanı, mevcut staging haberlerinin hiçbiri yeni kutuyla işaretlenmediği için beklendiği gibi gösterilmedi; bağımsız `/foto-galeri` sayfası çalışmaya devam etti.
- Kalan karar veya risk: Ana sayfa Foto Galeri mozaiğinin yeniden görünmesi için yönetici, gösterilecek haberlerde **Ana sayfada Foto Galeri’de göster** kutusunu açmalıdır. Yayın Hetzner staging IP'sinde HTTP çalışmaktadır; canlı marka alan adı/DNS/HTTPS geçişi bu kapsamda değildir. Ham `<img>` performans uyarıları ayrı optimizasyon işidir.

### 2026-09-11 — Son Haberler ilk kartında taşma ve galeriye göre yerleşim

- İstek: Foto Galeri boşken Son Haberler'in ilk görselinin yazının üstüne taşması ve başlığın kaybolmasının giderilmesi; galeri varsa başlığın görsel üzerinde, yoksa kartın tam genişlikte gösterilmesi.
- Yapılanlar: Sabit görsel yüksekliğiyle devralınan en-boy oranının çakışması giderildi; görsel kendi alanının genişliğine sınırlandı. Galeri yokken 820 px sınırı kaldırıldı, kart satırın tamamında görsel ve metni yan yana gösteriyor; mobilde görselin altında başlık ve spot yer alıyor. Galeri varken başlık görselin üzerinde beyaz, koyu geçişli zeminde gösteriliyor. Kart uzun başlıkla büyüyebiliyor, boşluksuz metinler sarılıyor; görünür klavye odağı korunuyor.
- Değişen ana dosyalar: `app/globals.css`, `tests/rendered-html.test.mjs`, `AGENTS.md`.
- Doğrulama: Yeni regresyon testi önce eski 820 px sınırında başarısız oldu; düzeltmeden sonra galeri kapalı/açık HTML, uzun başlık ve taşmayı önleyen stil kurallarıyla geçti. `npm test` production build ile **87 test geçti; 0 başarısız, 0 atlandı**. `npx tsc --noEmit` ve bağımsız `npm run build` başarılı; `npm run lint` **0 hata/43 mevcut ham görsel uyarısı** verdi. Ayrı yerel veritabanında uzun Türkçe başlıkla galeri kapalı ve açık durumların her biri 1440, 900 ve 390 px gerçek tarayıcıda kontrol edildi: altı görünümde sayfa/kart yatay taşması ve kırık görsel sıfır; başlığın tamamı görünür. Galeri yokken kart masaüstünde 1145 px satırın tamamını kullanıyor; galeri varken başlık beyaz ve görselin üzerinde kalıyor. Mobilde başlık metninin bütün sınırları kart içinde, klavye odağı 3 px ve Enter ile doğru haber sayfasına geçiş başarılı; konsol hatası yok. Ana sayfa ve ilgili haber rotası başarıyla açıldı. Mevcut azaltılmış hareket kuralları korunuyor.
- Kalan karar veya risk: Değişiklikler commit edilmedi, push edilmedi ve staging ortamına dağıtılmadı. Önceki otomatik manşet düşürme ve sıralama değişiklikleri korunmuştur. Ham `<img>` uyarıları ayrı optimizasyon işidir.

### 2026-09-11 — Otomatik haber sırası ve taşma düzeltmesinin staging dağıtımı

- İstek: Üç manşet alanından Son Haberler'e otomatik geçişin ve galeriye göre değişen ilk haber kartı düzeltmesinin yayınlanması.
- Yapılanlar: Yayın öncesinde SQLite ve medya için `20260911T090408Z` yedeği alındı; iki dosyanın SHA-256 doğrulaması ve yedek veritabanının `quick_check` sonucu başarılı oldu. İki düzeltme `59cba05` commit'iyle fast-forward olarak `main` branch'ine gönderildi. GitHub Actions temiz kurulum, production bağımlılık denetimi, test/build, lint, sunucu aktarımı, sunucuda yeniden test, atomik sürüm değişimi ve dış smoke adımlarını tamamladı.
- Değişen ana dosyalar: Dağıtılan uygulama sürümü `59cba05e469bb508781d884d96259cec9a90bd33`; yayın kaydı için `AGENTS.md`.
- Doğrulama: GitHub Actions çalışması `34582426435` **2 dakika 14 saniyede başarıyla** tamamlandı. Yerel yayın kapılarında `npm test` production build ile **87 geçti, 0 başarısız, 0 atlandı**; `npx tsc --noEmit` ve bağımsız `npm run build` başarılı; `npm run lint` **0 hata/43 bilinen ham görsel uyarısı**; `npm audit --omit=dev` **0 açık**. Aktif sunucu sürümü commit SHA'sıyla eşleşti; uygulama ve Caddy aktif, yalnız dört uygulama sürümü tutuluyor. Veritabanındaki **7.833 haber** korundu; slider 5, yan 2, alt 4 kaydı değişmedi; yeni `homepage_latest_at` alanı ve iki indeksi kuruldu, `quick_check` sonucu `ok`. Ana sayfa, Gündem, Son Dakika, canlı yayın, yazarlar, Foto Galeri, videolar, admin giriş ve ilk Son Haberler detayı HTTP 200; `/admin` 307; oturumsuz `/api/auth/me` ve `/api/homepage-layout` 401 döndürdü. Gerçek staging tarayıcısında 1440, 900 ve 390 px kontrol edildi: 13 Son Haberler kartı, sıfır yatay taşma, sıfır kırık görsel ve sıfır konsol hatası. Masaüstünde 710 px karttan 921 px'e taşan görsel artık 710 px ile sınırlı; başlık beyaz, görsel üzerinde ve bütün kırılımlarda tamamen görünür. Galeri açık ve kapalı durumlarını altı görünümde kapsayan yerel kabul sonucu önceki kayıtta yer alır.
- Kalan karar veya risk: Yayın Hetzner staging adresindedir; canlı marka alan adı/DNS/HTTPS geçişi bu kapsamda değildir. Önceki düşmelerin kaydedilmemiş zamanı geriye dönük tahmin edilmez; bundan sonraki düşme sırası kalıcı tutulur. Ham `<img>` uyarıları ayrı optimizasyon işidir.

### 2026-09-11 — Manşet altında yatay yayın akışı

- İstek: Manşet sliderının altındaki boşluğa sitenin tasarımına uygun, şık ve yatay bir yayın akışı eklenmesi.
- Yapılanlar: Manşetle aynı sütunda koyu kurumsal zemin, ince kırmızı üst çizgi, belirgin saatler, mevcut program vurgusu ve ilerleme çizgisi taşıyan yayın şeridi eklendi. Şu anki program ile sonraki iki başlangıç paneldeki mevcut günlük yayın akışından okunuyor. İstanbul saatine göre dakika başında ve sekmeye dönüşte güncelleniyor; gece yarısından sonraki programlarda **Yarın** etiketi kullanılıyor. Tek program çoğaltılmıyor, uzun metinler kart sınırlarında kalıyor; mobilde programlar kendi içinde yatay kayıyor ve klavyeyle erişilebiliyor. **İzle** canlı yayın sayfasına, **Tüm akış** doğrudan o sayfadaki program listesine açılıyor. Sağ manşet görsellerinin doğal yüksekliğinin satırı gereksiz büyütmesi giderildi; şerit ile sağ sütunun alt sınırı eşitlendi. Yan haber olmadığında boş sağ sütun ayrılmıyor. Site Ayarları açıklamasında bu listenin iki yüzeyi de beslediği ve Türkiye saatiyle tekrar eden günlük program kuralı belirtildi.
- Değişen ana dosyalar: `app/broadcast-strip.tsx`, `db/broadcast-schedule.mjs`, `app/page.tsx`, `app/globals.css`, `app/canli/page.tsx`, `app/admin/site-settings.tsx`, `tests/rendered-html.test.mjs`, `AGENTS.md`.
- Doğrulama: Yeni entegrasyon regresyonu uygulamadan önce manşet altında yayın şeridi bulunmadığı için başarısız oldu. Son durumda `npm test` production build ile **89 test geçti; 0 başarısız, 0 atlandı**. Yeni kapsam program başlangıç sınırları, İstanbul saati, gece yarısı/ertesi gün, boş-geçersiz-tek-tekrarlanan saatli akış, panelden kalıcılık, metin kaçışı, 400/401/403 koruması, yan haber olmaması ve hedef bağlantıları içerir. `npx tsc --noEmit` ve bağımsız `npm run build` başarılı; `npm run lint` **0 hata/43 mevcut ham görsel uyarısı** verdi. İlk lintte listeye gereksiz odak atanması kaldırıldı; dinamik sunucu saatinin ilk istemci renderına aynen aktarılması için satıra sınırlı, açıklamalı sunucu istisnası yazıldı. Gerçek uygulama tarayıcısında 1440, 900 ve 390 px görünümler kontrol edildi: sıfır sayfa taşması, sıfır kırık görsel ve sıfır konsol hatası. Masaüstünde manşet ile şerit arasında 14 px, şeridin ve sağ sütunun alt sınırları eşit; mobil akış 323 px alanda 754 px iç kaydırmayla çalışıyor. Gerçek yatay kaydırma ve Tab ile üçüncü programa erişim, 2 px beyaz klavye odağı doğrulandı. Tek uzun programlı/yan habersiz görünüm boş kart ve yatay taşma oluşturmadı. Açık sayfada saat 12:32 olduğunda Gündem Özel'den Öğle Bülteni'ne yenilemesiz geçiş görüldü. **Tüm akış** tıklaması `/canli#yayin-akisi` adresine gidip program listesini ekranın 24 px altına getirdi.
- Kalan karar veya risk: Değişiklikler yereldedir; commit, push veya staging dağıtımı yapılmadı. Şerit panelde tanımlanan günlük programı gösterir; fiilî yayın otomasyonundan veri alınmaz. Ham `<img>` uyarıları ayrı optimizasyon işidir.

### 2026-09-11 — Yatay yayın akışının staging dağıtımı

- İstek: Manşet altındaki yeni yatay yayın akışının yayınlanması.
- Yapılanlar: `72b6e3b` uygulama commit'i fast-forward olarak `main` branch'ine gönderildi. GitHub Actions temiz kurulum, production bağımlılık denetimi, test/build, lint, sunucu aktarımı, sunucuda yeniden test, atomik sürüm değişimi ve dış smoke adımlarını başarıyla tamamladı. Paneldeki mevcut program listesi değiştirilmeden yeni şeritte gösterildi.
- Değişen ana dosyalar: Dağıtılan uygulama sürümü `72b6e3b121824ee30e484c49d4bc8628d6b94de7`; yayın kaydı için `AGENTS.md`.
- Doğrulama: GitHub Actions çalışması `34586027700` **2 dakika 24 saniyede başarıyla** tamamlandı. Yayın öncesi `npm test` production build ile **89 geçti, 0 başarısız, 0 atlandı**; `npx tsc --noEmit` ve bağımsız `npm run build` başarılı; `npm run lint` **0 hata/43 bilinen ham görsel uyarısı**; `npm audit --omit=dev` **0 açık**. Aktif sunucu sürümü uygulama SHA'sıyla eşleşti, Koza TV ve Caddy servisleri aktif. Ana sayfa, canlı yayın, Gündem, Son Dakika, Foto Galeri, videolar, yazarlar ve admin giriş HTTP 200; `/admin` 307; oturumsuz `/api/auth/me` ve `/api/settings` 401 döndürdü. Gerçek staging tarayıcısında 1440, 900 ve 390 px kontrollerinde sayfa taşması, kırık görsel ve konsol hatası sıfır. Şu anki **Öğle Bülteni**, sonraki **Piyasa Saati** ve **Ana Haber Bülteni** doğru saatleriyle gösterildi. Masaüstünde 801 px şerit manşetle aynı genişlikte, sağ haber sütunuyla aynı alt sınırda; tablette 680 px. Mobilde 323 px alanda 754 px akış kaydırıldı, `scrollLeft` 254,5 px oldu ve sayfa taşmadı. **Tüm akış** bağlantısı `/canli#yayin-akisi` adresindeki program listesini ekranın 24 px altına getirdi.
- Kalan karar veya risk: Yayın Hetzner staging adresindedir; canlı marka alan adı/DNS/HTTPS geçişi bu kapsamda değildir. Şerit paneldeki günlük yayın takvimini kullanır; fiilî yayın otomasyonuna bağlı değildir. Ham `<img>` uyarıları ayrı optimizasyon işidir.

### 2026-09-12 — Site başlığına sunucu fotoğraflı yayın akışı

- İstek: Hafta içi altı programın (Sabah Mesaisi/Sinem Gündem, Odak Noktası/Remziye Demirkol, Günün Ortasında/Buket Güler, Haber Masası/Gamze Dondurmacı, Dünyanın İşi/Enver Kaptanoğlu, Ana Haber/Seda Selek) sunucu fotoğrafı, program adı ve başlangıç saatiyle yayın akışı olarak eklenmesi; akışın logo ile aynı satırda, mottonun bulunduğu alanda durması; manşet altındaki eski dar şeridin kaldırılması; canlı yayın düğmesinin logonun altına alınıp kartların genişletilmesi.
- Yapılanlar: Yayın akışı satırı bitiş saati, sunucu fotoğrafı ve gün kapsamı (hafta içi / hafta sonu / her gün) alanları kazandı; fotoğraf yalnız sitenin kendi yolundan (`/media/…`, `/yayin-akisi/…`) kabul ediliyor, dış adres ve üst dizin reddediliyor. `selectDailySchedule` günün tamamını İstanbul saatine göre durumlarıyla döndürüyor: bugüne uyan program yoksa akışı olan ilk güne geçiyor, gece yarısını aşan program saat başa döndükten sonra da yayında sayılıyor, programlar arasında boşluk varsa hiçbiri yayında gösterilmiyor. Başlıktaki yeni şeritte her kart temiz bir portre penceresi ve altında program adı / sunucu adı / başlangıç saati taşıyor; yalnız o an yayında olan kart kırmızı çerçeve, kırmızı üst çizgi, kırmızı alt bant, **YAYINDA** yazısı ve ilerleme çizgisiyle ayrılıyor. Biten ve sıradaki programlara rozet veya gri perde uygulanmıyor. Marka sütunu logo ve altındaki canlı yayın düğmesinden oluşuyor; kalan genişliğin tamamı akışa kalıyor. Manşet altındaki eski `broadcast-strip` kaldırıldı, manşet satırı yalnız slider ve yan haberlerle hizalı bitiyor. Altı sunucu fotoğrafı tek kadrajda (baş üstü boşluğu %6,5, omuz genişliği kadrajın %91'i) kırpılıp şeffaf WebP'ye çevrildi. Panelde her satır için fotoğraf seçici, bitiş saati ve yayın günü alanı açıldı; kurulumdan gelen demo akışa hiç dokunulmamışsa bir kez gerçek akışla değiştiren migration eklendi.
- Değişen ana dosyalar: `app/broadcast-flow.tsx` (yeni), `app/broadcast-strip.tsx` (silindi), `db/broadcast-schedule.mjs`, `db/settings-model.mjs`, `db/index.ts`, `app/site-chrome.tsx`, `app/page.tsx`, `app/canli/page.tsx`, `app/admin/site-settings.tsx`, `app/globals.css`, `public/yayin-akisi/*.webp`, `tests/rendered-html.test.mjs`.
- Doğrulama: `npm test` production build ile **91 test geçti; 0 başarısız, 0 atlandı**. Yeni kapsam gün kapsamı filtresi, yayındaki/biten/sıradaki durumları, program arası boşluk, gece yarısını aşan yayın, aynı saate iki kayıt, geçersiz girdi, fotoğraf adresi güvenliği (dış adres, `..`, boşluk, tırnak, 300 karakter sınırı), 401/403/400 koruması, metin kaçışı, ana sayfanın beş manşet / iki manşet yanı / dört manşet altı sınırı ve şeridin masaüstünde alta düşmemesi kurallarını içeriyor. `npx tsc --noEmit` temiz; `npm run lint` **0 hata / 47 bilinen ham `<img>` uyarısı**; `npm audit --omit=dev` **0 açık**. Gerçek tarayıcıda 1920, 1440, 1360, 1280, 1150, 1024, 940, 901, 900, 768, 600, 430, 390 ve 360 px ölçüldü: 901 px ve üzerinde altı kartın tamamı kaydırmasız sığıyor ve şerit marka sütununun yanında kalıyor; 900 px altında şerit kendi satırına inip yatay kayıyor. Hiçbir genişlikte program veya sunucu adı kırpılmıyor, bütün kartların saat satırı aynı hizada, program adı ile sunucu adı arasındaki boşluk 1 px. Kart genişliği masaüstünde 100–156 px, başlık yüksekliği 208 px, mobilde 262 px. Canlı yayın sayfasındaki liste aynı veriyi saat aralığı, yuvarlak sunucu portresi ve YAYINDA işaretiyle gösteriyor.
- Kalan karar veya risk: Değişiklikler yereldedir; commit, push veya staging dağıtımı yapılmadı. Şerit paneldeki günlük akışı gösterir, fiilî yayın otomasyonundan veri almaz. Nav arama kutusunun 1150 px genişlikte 1 px taşması bu çalışmadan önce de vardı ve kapsam dışıdır. Ham `<img>` uyarıları ayrı optimizasyon işidir.

### 2026-09-12 — Yayın akışının staging dağıtımı ve şerit sadeleştirmesi

- İstek: Başlıktaki yayın akışının yayınlanması; ardından kartlardaki siyah alanın azaltılması, sunucuların gövdesinin daha çok görünmesi ve şeridin ne olduğunu söyleyen göze batmayan bir etiket eklenmesi.
- Yapılanlar: `f16efe4` commit'i fast-forward olarak `main` branch'ine gönderildi; GitHub Actions temiz kurulum, üretim bağımlılık denetimi, test/build, lint, sunucuya aktarım, sunucuda yeniden test, atomik sürüm değişimi ve dış smoke adımlarını tamamladı. Dağıtımdan sonra kart alt bandı 70 pikselden 57 piksele indirildi, fotoğraf penceresi 110 pikselden 129 piksele çıktı (kartın %69'u), fotoğraf altındaki karartma 30 pikselden 18 piksele indi. Şeridin soluna 24 piksel genişliğinde dikey **Yayın Akışı** etiketi eklendi; yükseklikten yer almadığı için kart boyutları küçülmedi. Saat satırı sabit yüksekliğe alındı, böylece YAYINDA rozeti taşıyan kart diğerlerinden 1 piksel kaymıyor.
- Değişen ana dosyalar: Dağıtılan uygulama sürümü `f16efe468ecfff3b4bd7d3c40eadae9b33d652c4`; sonraki sadeleştirme için `app/broadcast-flow.tsx`, `app/globals.css`, `tests/rendered-html.test.mjs`, `AGENTS.md`.
- Doğrulama: GitHub Actions çalışması `34696458295` bütün adımlarıyla **başarıyla** tamamlandı. Yayın sonrası canlı kontrolde ana sayfa, canlı yayın, Gündem, Son Dakika, Foto Galeri, videolar, yazarlar ve admin giriş HTTP 200; `/admin` 307; oturumsuz `/api/auth/me` ve `/api/settings` 401 döndürdü. Altı sunucu fotoğrafı da HTTP 200 ve beklenen boyutta sunuluyor. Eski demo akış (Öğle Bülteni, Piyasa Saati, Ana Haber Bülteni, Koza TV Günaydın) migration ile gerçek akışla değişti; sayfada kalmadı. Eski `broadcast-strip`, `masthead-claim` ve ikinci motto kopyası yayında bulunmuyor. Manşet düzeni canlıda beş slayt, iki manşet yanı ve dört manşet altı haber gösteriyor. Bugün cumartesi olduğu için hafta içi akışı **PAZARTESİ** etiketiyle listeleniyor ve hiçbir program yayında işaretlenmiyor; `/canli` listesi altı programın saat aralığını doğru gösteriyor. Sadeleştirme sonrası `npm test` production build ile **91 test geçti; 0 başarısız, 0 atlandı**; `npx tsc --noEmit` temiz; `npm run lint` **0 hata / 47 bilinen ham `<img>` uyarısı**. Ölçümde bütün kartların sunucu adı ve saat satırı birebir aynı hizada, hiçbir genişlikte metin kırpılması yok.
- Kalan karar veya risk: Yayın Hetzner staging adresindedir; canlı marka alan adı/DNS/HTTPS geçişi bu kapsamda değildir. Dağıtım öncesi elle yedek alınamadı; bu makinede sunucu SSH anahtarı yok, sunucudaki `kozatv-backup.timer` günlük yedeği sürdürüyor ve veritabanı sürüm dizinlerinin dışında olduğu için kod dağıtımından etkilenmiyor. Şerit paneldeki günlük akışı gösterir, fiilî yayın otomasyonuna bağlı değildir.

### 2026-09-12 — Sadeleştirilmiş şeridin yayına alınması ve font yükleme bulgusu

- İstek: Siyah bandı azaltılmış, dikey etiketli yayın akışı şeridinin yayınlanması.
- Yapılanlar: `b534b98` commit'i fast-forward olarak `main` branch'ine gönderildi; GitHub Actions bütün adımları başarıyla tamamladı. Yayın sonrası canlı doğrulama yapıldı.
- Değişen ana dosyalar: Dağıtılan uygulama sürümü `b534b98`; yayın kaydı için `AGENTS.md`.
- Doğrulama: GitHub Actions çalışması `34697059038` **başarısız adım olmadan** tamamlandı. Canlıda ana sayfa, canlı yayın, Gündem, Son Dakika, Foto Galeri, videolar, yazarlar, admin giriş HTTP 200; `/admin` 307; oturumsuz `/api/settings` 401. Dikey **Yayın Akışı** etiketi, altı program ve altı sunucu fotoğrafı yayında. Gerçek tarayıcıda 1440 px ölçümünde kart 138 × 188 px, fotoğraf penceresi 129 px (kartın **%69'u**), siyah alt bant 57 px; altı kartın saat satırı tek bir hizada (234 px), metin kırpılması yok, yatay kaydırma yok, sayfa taşması yok, manşet ile yan haber sütunu alt sınırı hizalı. Sayfadaki 33 görselin tamamı HTTP 200; yüklenmemiş görünen 11 görsel ekran altındaki `loading="lazy"` haber kapaklarıdır. Eski `broadcast-strip`, `masthead-claim`, YAYINLANDI ve SIRADAKİ izleri yayında bulunmuyor.
- Kalan karar veya risk: **Bu çalışmadan bağımsız, önceden var olan bir hata bulundu.** Yayındaki sayfada `@font-face` ve `preload` adresleri derleme makinesinin mutlak yolunu (`/home/runner/work/kozawebsite/kozawebsite/.vinext/fonts/…`) taşıyor; dört font dosyası 404 dönüyor ve site Inter/Merriweather yerine yedek Arial/Times ile görüntüleniyor (`document.fonts` durumu `error`). Aynı derleme yerelde doğru `/_next/static/_vinext_fonts/…` adresini üretiyor, yani sorun CI derleme ortamına özgüdür ve bu değişiklik tarafından oluşturulmamıştır. Ayrı bir iş olarak ele alınmalıdır. Yayın Hetzner staging adresindedir; canlı marka alan adı/DNS/HTTPS geçişi bu kapsamda değildir.

### 2026-09-12 — Yayın akışı etiketinin belirginleştirilmesi

- İstek: Şeridin solundaki dikey **Yayın Akışı** yazısının fark edilmesi için kalın, biraz daha büyük ve net kırmızı olması.
- Yapılanlar: Etiket puntosu 8,5 pikselden 11 piksele çıkarıldı, kırmızısı `#ff6268` yerine daha doygun `#ff4a52` yapıldı, kalınlık 900'de tutuldu ve harf aralığı 2,2 piksele çekildi. Metnin rahat durması için şerit genişliği 24 pikselden 29 piksele çıktı; bu 5 piksel altı karta bölündüğü için kart genişliği yalnız 1 piksel azaldı.
- Değişen ana dosyalar: `app/globals.css`, `AGENTS.md`.
- Doğrulama: `npm test` production build ile **91 test geçti; 0 başarısız, 0 atlandı**. `npx tsc --noEmit` temiz; `npm run lint` **0 hata / 47 bilinen ham `<img>` uyarısı**. Gerçek tarayıcıda ölçüldü: 1440 pikselde etiket 29 × 188 piksel, punto 11 piksel, kalınlık 900, renk `rgb(255, 74, 82)`; 1024 pikselde 29 × 188; 390 pikselde 29 × 167. Üç kırılımda da etiket metni taşmıyor, program ve sunucu adlarında kırpılma yok, sayfa taşması yok. 1440 ve 1024 pikselde altı kart kaydırmasız sığıyor; 390 pikselde şerit beklendiği gibi kendi içinde kayıyor.
- Kalan karar veya risk: Önceki kayıtta bildirilen CI derlemesindeki font yükleme hatası (Inter/Merriweather 404, site yedek Arial/Times ile görüntüleniyor) sürüyor ve ayrı bir iş olarak beklemektedir.

### 2026-09-12 — Şerit fotoğraflarının açılışta hazır gelmesi

- İstek: Yayında yapılan kontrolde şerit fotoğraflarının sayfa çizildikten sonra tek tek belirdiği görüldü.
- Yapılanlar: Şerit her sayfanın en üstünde durduğu için sunucu fotoğraflarındaki `loading="lazy"` kaldırılıp `loading="eager"` yapıldı. Altı görselin toplamı 180 KB olduğundan ek yük ihmal edilebilir. Regresyon testi, şeritte tembel yükleme kalmadığını doğruluyor.
- Değişen ana dosyalar: `app/broadcast-flow.tsx`, `tests/rendered-html.test.mjs`, `AGENTS.md`.
- Doğrulama: `npm test` production build ile **91 test geçti; 0 başarısız, 0 atlandı**; `npx tsc --noEmit` temiz; `npm run lint` **0 hata / 47 bilinen ham `<img>` uyarısı**.
- Kalan karar veya risk: CI derlemesindeki font yükleme hatası (Inter/Merriweather 404) ayrı bir iş olarak beklemektedir.
