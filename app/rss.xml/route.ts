import { listPublishedArticles } from "../../db";
export const dynamic = "force-dynamic";
function escapeXml(value: string) { return value.replace(/[<>&'"]/g, (char) => ({ "<": "&lt;", ">": "&gt;", "&": "&amp;", "'": "&apos;", '"': "&quot;" })[char]!); }
export async function GET() { const items = listPublishedArticles(50).map((article) => `<item><title>${escapeXml(article.title)}</title><link>https://www.kozatv.com.tr/haber/${article.slug}</link><guid>https://www.kozatv.com.tr/haber/${article.slug}</guid><description>${escapeXml(article.spot)}</description><category>${escapeXml(article.category)}</category><pubDate>${new Date(article.publishedAt ?? article.updatedAt).toUTCString()}</pubDate></item>`).join(""); const xml = `<?xml version="1.0" encoding="UTF-8"?><rss version="2.0"><channel><title>Koza TV Haber</title><link>https://www.kozatv.com.tr</link><description>Türkiye ve dünyadan son dakika haberleri</description><language>tr-TR</language>${items}</channel></rss>`; return new Response(xml, { headers: { "content-type": "application/rss+xml; charset=utf-8", "cache-control": "public, max-age=300" } }); }
