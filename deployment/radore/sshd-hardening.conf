# Koza TV production SSH policy. Install as
# /etc/ssh/sshd_config.d/00-kozatv-hardening.conf after koza-admin key and sudo
# access have both been verified from a separate session.
PermitRootLogin no
PasswordAuthentication no
KbdInteractiveAuthentication no
PubkeyAuthentication yes
MaxAuthTries 3
